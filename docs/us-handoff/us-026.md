# Handoff US-026 — Revelación progresiva y tarjetas predictivas (alcance S4)

**Estado**: planning
**Epic**: E4 Frontend y Dashboards
**Actividad**: A4 (dom 16-ago-2026) — apartado de prototipos, **50 % = 12.50 de 25 puntos**
**Sprint**: S4
**Rama**: `us-026` — **aún no creada**. Se ramifica de la punta de `us-025`. Es la convención que el usuario fijó el 10-ago-2026 (una rama por US, commits locales encadenados, sin PR); el estándar de la raíz `feature/E4-US-026-tarjetas-predictivas` queda como discrepancia RU-11, igual que en US-001, US-UX-09, US-015, US-016, US-017 y US-027
**SHA base**: **no se fija hoy y no se inventa.** Ancestro común verificado el 11-ago-2026: `115136b` (`git rev-parse HEAD`, punta de `us-002`, rama `us-001`). Entre ese commit y esta rama se encadenan `us-ux-09`, `us-006`, `us-015`, `us-016`, `us-008`, `us-017`, `us-027` y `us-025`, ninguna commiteada todavía. Al crear la rama en Fase 3 se ejecuta `git rev-parse --short HEAD` y el valor se escribe aquí. Ancla del diff: ese SHA, **nunca `HEAD~N`**
**Última fase**: 2 — planeación cerrada, 11-ago-2026. Plan completo en [`docs/us-planning/us-026.md`](../us-planning/us-026.md)
**Estimación**: 1 SP · Día: mié 12-ago-2026 · Estado esperado al cierre: **cerrada**
**Fuente normativa**: §26 del plan (líneas 2457-2483, manda durante S4) y [`docs/semana_4/plan_excelencia.md`](../semana_4/plan_excelencia.md) §6.2 (Pantalla 2, Zona C) y §5.4 (los cuatro estados transversales)

## Dominios y sub-tareas

- [ ] backend · [x] **frontend** · [ ] ml · [ ] agent · [ ] infra · [ ] db · [x] **docs**
- `ml` sin marcar aunque `scripts/verificar_historicos_tablero.sh` **lea** `data/aggregates/serie_tablero.parquet`: lo lee, no escribe ni un archivo de `ml/`.
- `infra` sin marcar aunque se cree un `scripts/verificar_*.sh`: es un guion de verificación de la propia US.
- **La US NO se reparte.** 1.0 SP, un solo dominio de código y cada capa lee la anterior: los componentes leen los tipos y el cálculo, el composable lee el cálculo y el contrato de métricas, el contenedor lee el composable. Repartirlo produciría traspasos con el mismo trabajo total más coste de contexto. **Un agente `frontend-engineer`, en un solo contexto.**
- Orden interno obligatorio: `types/prediccion.ts` → `utils/metricasTablero.ts` + `utils/proyeccion.ts` + `utils/formatoTablero.ts` → `test/proyeccion.spec.ts` → `scripts/verificar_historicos_tablero.sh` → `public/datos/historicos-tablero.json` → `i18n/locales/{es,en}.json` → `composables/usePrediccionesTablero.ts` → `SerieProyectada` y `TablaDetalleSerie` → `TarjetaPredictiva` y `PanelRevelacion` → `TableroPredicciones` → las dos líneas de la página → pruebas de componente → `docs/manual-test/us-026.md`.
- Dos reglas de orden que no son arbitrarias: **el cálculo y su prueba van antes que cualquier plantilla**, porque definen el contrato `Proyeccion` del que cuelga toda la presentación; **los catálogos i18n van antes que las plantillas**, porque escribir la plantilla primero invita a dejar una cadena literal «temporal» que sobrevive al commit.

## Archivos previstos

*(previsión de la Fase 2, no snapshot. Se sustituye por `git status --short` al cerrar la Fase 3.)*

**Crear — frontend**

- `frontend/app/types/prediccion.ts`
- `frontend/app/utils/metricasTablero.ts` — las tres métricas, contrato de UI
- `frontend/app/utils/proyeccion.ts` — **el único módulo con aritmética de negocio**
- `frontend/app/utils/formatoTablero.ts` — `Intl` con locale explícito y `timeZone: 'UTC'`
- `frontend/app/composables/usePrediccionesTablero.ts`
- `frontend/app/components/tablero/TableroPredicciones.vue`
- `frontend/app/components/tablero/TarjetaPredictiva.vue`
- `frontend/app/components/tablero/PanelRevelacion.vue`
- `frontend/app/components/tablero/SerieProyectada.vue`
- `frontend/app/components/tablero/TablaDetalleSerie.vue`
- `frontend/public/datos/historicos-tablero.json` — **generado y verificable**, 3 métricas × 24 meses
- `frontend/test/proyeccion.spec.ts`
- `frontend/test/prediccionesTablero.spec.ts`

**Modificar — frontend**

- `frontend/app/pages/exploracion/tableros.vue` — **YA EXISTE** (21 líneas). **Exactamente dos líneas**: el `import` y la etiqueta `<TableroPredicciones />` tras el `<p>` existente
- `frontend/i18n/locales/es.json` y `en.json` — **un solo bloque raíz nuevo: `forecast`**
- `frontend/test/pantallas.spec.ts` — **una línea condicional**: el doble de `useFetch`, y solo si nadie la puso antes

**Crear — guion y documentos**

- `scripts/verificar_historicos_tablero.sh`
- `docs/manual-test/us-026.md`

**Archivos existentes reutilizados, no tocados**

- `frontend/app/utils/navegacion.ts` — se importa `RUTAS_CONTRATO` **en la prueba** para verificar el destino del explorador
- `frontend/app/composables/useTituloDeRuta.ts` y `frontend/app/layouts/portal.vue` — dan el `h1` del contrato y la franja de alcance
- `frontend/app/assets/css/main.css` y `frontend/app/utils/tokens.generated.ts` — solo se consumen tokens ya emitidos (§3.5 del plan)
- `frontend/test/i18nDePrueba.ts` — se usa tal cual, **sin añadirle nada**
- `frontend/test/contratos.spec.ts` (US-UX-09) — **no se toca**, pero escanea `app/**` y actúa sobre los cinco componentes nuevos: exige que toda clase de utilidad nombre un token declarado y que toda clave punteada literal exista en los dos catálogos
- `data/aggregates/serie_tablero.parquet` (US-006) — entrada del guion de verificación

## Zonas sensibles

| Zona | Regla |
|---|---|
| `frontend/app/assets/css/main.css` · `frontend/app/utils/tokens.generated.ts` | **Generados** por `docs/entregables/generar_tokens_a4.py`. Nadie los edita a mano. Solo se consumen los tokens de la lista cerrada de §3.5 del plan; cero valores hexadecimales en componentes |
| `frontend/app/utils/navegacion.ts` | **No se toca.** US-UX-09 lo edita para `RUTA_GUIA`. Si aparece en el diff, se revierte |
| `frontend/app/components/guia/**` y la ruta `/guia` | De US-UX-09. Prohibidos, incluida `LaminaTarjetas.vue`, que se **consulta** como referencia de anatomía y no se edita |
| `frontend/app/pages/exploracion/tableros.vue` | **Compartido con US-025.** Diff máximo de esta US: **dos líneas**, en posición decidida (ver frontera abajo) |
| `frontend/test/pantallas.spec.ts` | Compartido por varias US esta semana. Diff máximo: **una línea** condicional, el doble de `useFetch` |
| `frontend/i18n/locales/{es,en}.json` | Cada US añade **su bloque raíz**. `forecast.*` es de US-026. **`screen.dashboards.description` no se toca** |
| `frontend/app/components/inicio/**` · `frontend/app/pages/inicio.vue` · `frontend/app/utils/fechas.ts` | De US-027. Prohibidos |
| `frontend/app/middleware/**` · `frontend/app/composables/useSesion.ts` · `frontend/app/components/nav/**` | De US-017 y US-015. Prohibidos: esta pantalla no lee la sesión, la guarda actúa antes |
| `frontend/app/stores/**` y Pinia | De US-025. **No se instala, no se crea y no se importa en S4** |
| `frontend/package.json` · `pnpm-lock.yaml` · `frontend/nuxt.config.ts` | **Cero dependencias nuevas.** El diff sobre estos tres debe ser vacío |
| `frontend/server/api/**` | De US-001 (proxy) y US-015 (`auth/**`). **US-026 no publica ninguna ruta `/api`** |
| `backend/**` · `db/**` · `ml/**` · `infra/**` · `docs/security.md` | Fuera del write-set por completo |

## Frontera con US-025, que se planea en paralelo

Las dos US entregan en la misma pantalla, `/exploracion/tableros`, el mismo miércoles. La auditoría del lote anterior encontró trece defectos, tres bloqueantes, **todos en fronteras como esta**. Reparto sin zonas grises, y **no se da por supuesto que la otra planeación lo declare**:

- **US-026 escribe** los diez archivos de `frontend/app/{types,utils,composables,components/tablero}` listados arriba, el JSON de `frontend/public/datos/`, sus dos specs, el guion `scripts/verificar_historicos_tablero.sh` y sus dos documentos. **Los cinco nombres de componente —`TableroPredicciones`, `TarjetaPredictiva`, `PanelRevelacion`, `SerieProyectada`, `TablaDetalleSerie`— quedan reservados por esta declaración.**
- **US-025 escribe** `frontend/app/stores/workspace.ts` y la instalación de Pinia, `package.json`/`pnpm-lock.yaml` (pinia, echarts, vue-echarts), `frontend/nuxt.config.ts` (módulo y `routeRules` SWR), su composable de serie y su componente de gráfica de 500 K, y `GET /api/metrics/series` en el backend. **US-026 no abre ninguno de esos archivos.**
- **`frontend/app/pages/exploracion/tableros.vue` es el único archivo compartido de producción.** El diff de US-026 son **dos líneas**: el `import` y `<TableroPredicciones />` **inmediatamente después del `<p>` existente** y **antes** de lo que añada US-025. El orden —predicciones arriba, serie larga abajo— es el que pide la revelación progresiva: §26 asigna a US-026 «entender el estado global de un vistazo» y a US-025 «recorrer una serie larga», que es el detalle. **Regla de fusión válida en los dos órdenes de aterrizaje:** quien llegue segundo inserta su bloque respetando esa posición relativa; nadie reordena el bloque del otro; nadie envuelve el bloque ajeno. Restricciones que ninguno puede violar en esa página: **un solo `h1`** y el **primer `<p>`** sigue siendo el de `screen.dashboards.description`.
- **`frontend/test/pantallas.spec.ts` es el único archivo compartido de pruebas.** Monta las diez páginas sin arrancar Nuxt; en cuanto la página monte un componente con `useFetch` —el mío y probablemente el de US-025— se pone rojo con un `useFetch is not defined` ajeno a lo que ese spec verifica. Regla operativa: si `grep -c "stubGlobal('useFetch'" frontend/test/pantallas.spec.ts` devuelve **≥ 1**, US-026 **no toca el archivo**; si devuelve **0**, añade **solo** el doble de `useFetch` al `beforeEach` (línea exacta en §5.5 del plan) y nada más. **Con ese doble, la página renderiza el bloque en estado cargando, que no añade `h1` ni `<p>` por delante: las cinco aserciones de ese spec siguen ciertas sin cambiar ninguna.**
- **Orden de implementación: US-025 antes que US-026.** No es dependencia técnica —US-026 no importa nada de US-025—, es coste de integración: con la página ya estable, mi diff son dos líneas en vez de obligar a mi compañero a insertar por encima. Si US-025 resbala, US-026 inserta sus dos líneas sobre la página tal como está y US-025 añade las suyas debajo.

**Lo que se le pide a US-025, y no se da por hecho:** que no añada un segundo `h1` a la página; que no envuelva el bloque de predicciones en un contenedor propio; que si necesita el doble de `useFetch` compruebe antes con el `grep` de arriba; y que si quiere alguno de los cinco nombres de componente reservados, renombre.

## Frontera con US-027 y US-017, ya declaradas por ellas y aquí acatadas

- **US-027 §2.6 fijó la frontera semántica y se adopta sin renegociar**: US-027 muestra el **presente** (valor actual, variación, fecha de corte) en `components/inicio/TarjetaIndicador.vue`; **US-026 muestra el futuro** (proyección con etiqueta de método) en `components/tablero/**`. **No se comparte componente**: una tarjeta con tres props opcionales que solo un consumidor usa es peor que dos componentes de treinta líneas.
- **`frontend/app/utils/fechas.ts` (US-027) no se abre ni se importa.** Formatea instantes ISO a día/mes/año; las entradas de US-026 son meses `YYYY-MM`. Duplicar dos envoltorios de `Intl` cuesta menos que un bloqueo cruzado entre dos US del mismo día. **Anotado para S5: fundir `utils/fechas.ts` y `utils/formatoTablero.ts` cuando las dos hayan aterrizado.**
- **US-017 entrega el estado «sin permiso» y la guarda de ruta.** US-026 **no construye ningún estado de permiso**: `/exploracion/tableros` exige scope `analista` (fila 2.4 del mapa de US-016) y quien no lo tiene no llega a renderizar esta pantalla. El `directivo` entra por jerarquía de roles.

## Las once ambigüedades del enunciado, ya resueltas en la planeación

1. **Las tarjetas viven en `/exploracion/tableros`**, Zona C de la Pantalla 2, no en `/inicio` (que es de US-027 y muestra tarjetas de **indicador**).
2. **Los niveles son tres**: tarjeta (0 clics), panel con la serie (1), tabla de detalle (2). **El enlace al explorador es la salida del nivel 3, no un cuarto nivel**: con la otra lectura, «cuatro cosas en tres niveles y dos clics» se contradice.
3. **La cifra se calcula**, no se escribe: función pura sobre histórico derivado. Escribir «+12 %» sería la deshonestidad que el propio criterio prohíbe.
4. **Sin ECharts.** 25 puntos se pintan con un `<svg>` propio. `vue-echarts` no está instalado y quien lo instala es US-025: depender de eso sería un bloqueo cruzado el mismo día. La gráfica de 500 K con `sampling: 'lttb'` y `large: true` sigue siendo de US-025.
5. **Sin Pinia en S4.** El estado «qué tarjeta está abierta» es local a un subárbol de una pantalla. No se deja un `emit` sin consumidor: un evento que nadie escucha es andamiaje. El punto de enganche de S5 queda escrito, no codificado.
6. **Sin ruta `/api/**` propia.** El histórico se sirve como activo estático. Una ruta de Nitro bajo `/api` sería una superficie de datos que `audit_scope_coverage` de US-016 **no puede ver**, que es exactamente el defecto que la auditoría anterior encontró.
7. **El estado de carga es real**, no decorado: `useFetch(..., { server: false })`, de modo que el esqueleto siempre se alcanza y el error y el vacío también.
8. **La etiqueta de método no es una cadena por tarjeta.** Es una función pura del objeto que devuelve el cálculo. Cuatro cierres en §2.5 del plan.
9. **No se reutiliza `TarjetaIndicador.vue`** de US-027.
10. **No se fuerza el «+12 %»** del ejemplo del plan. Si la regresión da +3.8 %, dice +3.8 %.
11. **`screen.dashboards.description` no se toca** (ver «acciones para el integrador»).

## El mecanismo anti-desincronización de la etiqueta de método

El defecto que cierra: alguien cambia la ventana de 12 a 6 meses y la tarjeta sigue diciendo «sobre 12 meses». Cuatro cierres, los cuatro verificables:

1. **`TarjetaPredictiva` no tiene ninguna prop de texto de método.** Solo recibe `proyeccion: Proyeccion | null`. No hay por dónde pasar un texto distinto del que el cálculo produjo.
2. **`proyectarLineal()` emite el descriptor con los parámetros que realmente usó**: `points` es la longitud del tramo empleado, no la ventana pedida; `r2` es el coeficiente calculado.
3. **`etiquetaMetodo()` es la única función que conoce `PLANTILLAS_METODO`**, y vive junto al cálculo. `grep -rn "forecast.method" frontend/app/components` → **0 líneas**.
4. **Tipo total + prueba de catálogo**: `Record<IdMetodo, string>` hace que un método nuevo sin plantilla no compile, y una prueba exige que cada plantilla exista en `es.json` y `en.json` con los mismos parámetros de interpolación.

**La prueba que caza el defecto de verdad**: montar con `ventana: 12` y con `ventana: 6` sobre el mismo histórico y comparar el texto renderizado (contiene `12` y no `6`, luego `6` y no `12`).

## El mecanismo anti-desplazamiento de maquetación, y cómo se prueba

**Reserva de dimensiones sin token nuevo y sin altura mágica** (`main.css` es generado, y un alto en píxeles se desajusta con el tamaño de fuente del lector): la tarjeta renderiza **siempre las mismas cinco cajas `[data-caja]` con el mismo atributo `class`** en los dos estados; solo cambia el contenido. El título muestra la etiqueta real ya durante la carga; el valor y la variación llevan un relleno con `&nbsp;` dentro del **mismo** párrafo tipográfico; la etiqueta de método ocupa **dos líneas en los dos estados** (`line-clamp-2` en listo, dos `span.block` en cargando); el botón existe en los dos, `disabled` mientras carga.

**Cómo se prueba, que es lo que casi nadie prueba.** `happy-dom` no calcula maquetación: una aserción de píxeles ahí no podría fallar nunca. Se prueba la **causa** con tres aserciones que sí pueden fallar —equivalencia estructural de `[data-caja]` con su `class` comparada con `toEqual`, ninguna caja con `textContent` vacío, y no-envoltura de la cifra más conteo de líneas de la etiqueta— y se **mide el efecto** una vez en navegador real con `PerformanceObserver` sobre `layout-shift` y la red limitada, con el acumulado pegado en el PR. Lo que estas pruebas no demuestran queda escrito en el plan §6.3, para no venderlas de más.

Apunte que evita una discusión previsible: la expansión del panel **no es CLS** —el navegador descuenta los desplazamientos dentro de los 500 ms posteriores a una interacción— y, aun así, el panel se renderiza **a ancho completo debajo de la rejilla**, no dentro de la tarjeta, para que expandir no estire la fila y mueva las otras dos.

## Cómo se cuentan los dos clics

Tres piezas: **marcado de contrato** (`data-accion-revelacion` con `data-nivel-destino`, y `data-nivel` en cada región); **revelación con `v-if` y no `v-show`**, de modo que el nivel no está en el DOM hasta alcanzarse y la aserción «antes del clic no existe» significa algo; y un **helper `abrirNivel(wrapper, nivel)`** declarado en el propio spec —no en `test/i18nDePrueba.ts`, que es compartido— que devuelve el número de pulsaciones. Las aserciones son `toBe(1)` y `toBe(2)`, con igualdad y no con «menor o igual»: llegar en 0 clics también sería un defecto, porque la profundidad dejaría de estar disponible sin estar impuesta. Y **equivalencia de teclado**: todo control de revelación es un `<button>` real con `aria-expanded` y `aria-controls`; un `<div @click>` pasaría el conteo y dejaría fuera al teclado.

## De dónde sale la cifra

**Origen único**: `data/aggregates/serie_tablero.parquet`, el agregado que **US-006 §2.4 congela** (500 000 filas, 2 000 días hábiles del 31-oct-2018 al 30-jun-2026, 250 series). De ahí se derivan, con **una** lectura determinista y documentada, tres métricas × 24 meses (**2024-07 a 2026-06**, todos completos por construcción):

| Tarjeta | Columna | Agregación mensual | Unidad |
|---|---|---|---|
| Cobertura de liquidez (LCR) | `ratio_lcr` | media ponderada por `n_posiciones` | porcentaje |
| Saldo disponible promedio diario | `saldo_disponible_mxn` | promedio diario del mes | millones de MXN |
| Concentración en divisa extranjera | `saldo_disponible_mxn` con `divisa != 'MXN'` | participación sobre el total del mes | porcentaje |

El resultado se publica en `frontend/public/datos/historicos-tablero.json` **sin etiquetas visibles** (los nombres físicos son datos y la agregación viaja como clave de traducción) y **sin la proyección** (publicarla abriría una segunda fuente de verdad para la misma cifra). El archivo lleva su propia procedencia, que es lo que el nivel 2 muestra al lector. `scripts/verificar_historicos_tablero.sh` recalcula la derivación y compara **valor a valor con igualdad exacta**, no con tolerancia: una tolerancia escondería justo el defecto que busca, que es una edición a mano.

## Contrato para la interfaz bilingüe

Bloque raíz **`forecast`**, con claves jerárquicas en inglés y paridad exacta. Cuatro reglas duras, con prueba viva en `test/idioma.spec.ts`:

1. **Ninguna clave nueva puede tener el mismo texto en `es` y en `en`** — la lista de excepciones está fijada con `toEqual(['brand.name', 'error.code'])`. Consecuencia: **no hay clave para el símbolo de porcentaje**, que sería idéntico en los dos catálogos; el porcentaje se emite con `Intl.NumberFormat(idioma, { style: 'percent' })`. La única clave de unidad es `forecast.unit.millionsMxn`.
2. **Los parámetros de interpolación se conservan** en las dos redacciones (`{points} {horizon} {r2}`, `{month}`, `{field} {aggregation}`, `{metric} {from} {to} {min} {max} {last} {next} {projected} {change}`, `{minimum}`).
3. **Paridad exacta de conjuntos de claves.**
4. **Los nombres físicos son datos, no claves**: `ratio_lcr` y `saldo_disponible_mxn` se interpolan tal cual.

**Los resúmenes textuales de gráfica para lector de pantalla son cadena visible** y existen en los dos catálogos: el `<desc>` del `<svg>` se comprueba no vacío y distinto en `es` y en `en`.

## Nube y datos

- **Ningún comando contra GCP y ningún gasto.** US-003 está al final de la cola por decisión del usuario del 10-ago-2026.
- **Ninguna variable de entorno nueva. Ninguna dependencia nueva.**
- El JSON viaja **dentro de la imagen** de Nuxt: sin volumen, sin bucket.
- **Sin `routeRules` ni SWR** puestos por esta US. Si US-025 los añade, el bloque de predicciones no se ve afectado: su petición es del cliente y no forma parte del HTML cacheado.

## Schema

**Ninguna migración. `db/schema.sql` no cambia ni un byte.** Y queda escrito lo que **no** debe existir, para que S5 no lo invente: **no hay tabla de proyecciones y no debe haberla**. Una proyección es función determinista de una serie; persistirla crea una segunda fuente de verdad para la misma cifra, que es la variante en base de datos del mismo defecto que el mecanismo de la etiqueta cierra en la interfaz. Cuando el histórico deje de ser activo estático, lo que se publica es el endpoint ya registrado `GET /api/metrics/series` (scope `analista`), no una tabla.

## Degradaciones declaradas de antemano (R10: el silencio no es opción)

1. **Sin Pinia y sin emisión al store `workspace`.** Es de US-025 y no se importa en S4. El enganche de S5 queda descrito, no codificado.
2. **Sin ECharts.** La serie del nivel 2 son 25 puntos en un `<svg>` propio; la gráfica de 500 K es de US-025.
3. **Sin endpoint gobernado.** El histórico es un activo estático, **legible sin sesión**, mientras la pantalla exige scope `analista`. Es aceptable —72 agregados mensuales sintéticos de un prototipo ya declarado— y preferible a una ruta Nitro que la auditoría de scopes no vería. Remediación de S5: una línea del composable apuntando a `GET /api/metrics/series`.
4. **Ventana de 24 meses**, no la serie completa de 2 000 días: las tarjetas resumen, no exploran.

## La válvula de §26.5, escrita como válvula

**Qué es**: la segunda válvula de §26.5, «el drill-down baja de tres niveles a dos».

**Criterio exacto que la dispara**: el **mié 12-ago-2026 a las 20:00 hora local**, si `pnpm --dir frontend test test/prediccionesTablero.spec.ts` **no está en verde en los tres casos del nivel 3** (`el nivel 3 es una tabla con caption y encabezados de columna`, `el enlace al explorador lleva el termino de la metrica`, `el nivel 3 esta a dos clics y ni uno mas`).

**Quién lo decide**: el **usuario**, único integrante ejecutor de la semana, a propuesta del agente que implementa y con la salida del comando pegada. El agente **no la aplica por su cuenta**, y no se adelanta «por precaución»: adelantar una válvula es regalar alcance.

**Qué cambia exactamente**: (1) `TablaDetalleSerie.vue` no se entrega y el nivel 3 desaparece del árbol; (2) el botón que llevaba a él **se retira, no se deshabilita** —un control muerto es peor que su ausencia—; (3) el **enlace al explorador sube al nivel 2**, para que la cadena siga terminando en una salida; (4) los tres casos se **borran** del spec, no se marcan `skip`, y la aserción del nivel 2 se conserva con un comentario fechado que explica por qué; (5) se declara en el PR, en este handoff y en el **pie de la figura** del PDF de A4.

**Qué NO se degrada bajo ninguna circunstancia**: las tres tarjetas, su etiqueta de método derivada del cálculo, el esqueleto sin desplazamiento y el vacío diseñado.

## Bloqueos y dependencias

- **Dependencia dura: US-006** (mar 11). Sin `data/aggregates/serie_tablero.parquet` no se puede derivar el JSON ni correr CA-6. **Degradación decidida** si a las 12:00 del mié 12 no existe tras `make data`: se publica el JSON con la misma forma y ventana desde una serie mensual de semilla fija, con `"fuente": "serie sintetica local, US-006 no disponible"` **mostrado tal cual en el nivel 2**, sin disimulo; al aterrizar US-006 se regenera con `--escribir` y CA-6 vuelve a ser exigible.
- **Dependencia blanda: US-025** (mismo día). No se importa nada suyo. Solo afecta al orden de aterrizaje sobre la página compartida.
- **Dependencia de contexto: US-017** — la guarda de ruta es la que hace que solo `analista` y superiores lleguen a esta pantalla. Si US-017 resbala, las tarjetas se ven igual; lo que falta es la restricción, y eso es suyo, no mío.
- **Sin dependencia de US-008**: el destino del explorador (`/exploracion?q=ratio_lcr`) devuelve resultados si el catálogo está sembrado y una pantalla vacía si no. La tarjeta se ve idéntica en los dos casos.
- **Esta US bloquea a US-UX-07** (sáb 15): tres de las figuras encadenadas de la Pantalla 2 —nivel 1, nivel 2 y nivel 3— más las capturas de los estados cargando, vacío y error salen de aquí.

## Acciones para el integrador

1. **`screen.dashboards.description` sigue en futuro** («Contendrá el tablero ejecutivo…») y describe una pantalla que el miércoles deja de ser un marcador de posición. US-026 **no la toca** porque el chasis de esa pantalla es de US-025 y es la única clave que las dos podríamos editar el mismo día. Redacción propuesta, para aplicarla **una sola vez** al cerrar la pantalla:
   - `es`: «Reúne las tarjetas de proyección con su método declarado, la serie larga preagregada y el detalle por mes, dentro de la misma pantalla de exploración.»
   - `en`: «Brings together the projection cards with their stated method, the long preaggregated series and the month by month detail, inside the same exploration screen.»
2. **Fundir `frontend/app/utils/fechas.ts` (US-027) y `frontend/app/utils/formatoTablero.ts` (US-026)** en S5, cuando las dos hayan aterrizado. Hoy están separados por una razón de frontera, no de diseño.
3. **Verificar que el doble de `useFetch` en `frontend/test/pantallas.spec.ts` aparece una sola vez** tras integrar US-025 y US-026.

## engram-memory

`mem_search` **no estuvo disponible** en la sesión de planeación: el inventario expuesto al subagente es Read, Write, Edit, Bash, Glob, Grep y Context7, sin herramientas `mem_*`. Se sustituyó por un barrido `grep -rn` sobre `context/`, `docs/` y `frontend/` con las palabras clave de la US. Se deja constancia en lugar de simular la consulta.

## Context7

**No consultado, y es una decisión y no una omisión.** El enunciado lo autorizaba para `vue-echarts` y las importaciones modulares de `echarts/core`; esta US **no usa ECharts** y no añade ninguna dependencia. Las APIs que toca —Vue 3, vue-i18n 11, `useFetch` de Nuxt 4.5, `Intl`— ya están en uso en el repositorio y sus firmas se leyeron del código real.

## Siguiente acción

Ejecutar la Fase 3 con **un solo agente `frontend-engineer`**, después de que US-025 cierre, siguiendo el orden interno de arriba. Primer paso concreto: crear `frontend/app/types/prediccion.ts` y `frontend/app/utils/proyeccion.ts` con las firmas congeladas en §4 del plan y correr `pnpm --dir frontend test test/proyeccion.spec.ts` **antes de escribir un solo `.vue`**. El cálculo y su etiqueta son el contrato del que cuelga toda la pantalla; si se escriben las plantillas primero, la etiqueta acaba siendo una cadena y la US pierde su razón de ser.

## Bitácora de estado

| Fecha | Fase | Estado |
|---|---|---|
| 11-ago-2026 | 2 | Planeación cerrada. Plan de once secciones en `docs/us-planning/us-026.md`. Once ambigüedades resueltas; frontera con US-025 declarada archivo por archivo, con los dos archivos compartidos acotados a dos líneas y una línea; mecanismo anti-desincronización de la etiqueta de método con cuatro cierres; mecanismo anti-CLS con tres aserciones estructurales más medición manual; conteo de clics con helper y `toBe`; válvula de §26.5 con hora, comando y decisor |
