# Handoff US-029 — Overlay de linaje explicable (alcance S4)

**Estado**: planning
**Epic**: E4 Frontend y Dashboards
**Actividad**: A4 (dom 16-ago-2026) — apartado de prototipos, **50 % = 12.50 de 25 puntos**, más tres secciones de la guía de estilos
**Sprint**: S4
**Rama**: `us-029` — **aún no creada**. Se ramifica de la punta de `us-026`. Es la convención que el usuario fijó el 10-ago-2026 (una rama por US, commits locales encadenados, sin PR); el estándar de la raíz `feature/E4-US-029-overlay-linaje` queda como discrepancia RU-11, igual que en US-001, US-UX-09, US-015, US-016, US-017, US-027, US-025 y US-026
**SHA base**: **no se fija hoy y no se inventa.** Ancestro común verificado el 11-ago-2026: `115136b` (`git rev-parse --short HEAD`, punta de `us-002`, rama activa `us-ux-09`). Entre ese commit y esta rama se encadenan `us-ux-09`, `us-006`, `us-015`, `us-016`, `us-008`, `us-017`, `us-027`, `us-025` y `us-026`, ninguna commiteada todavía. Al crear la rama en Fase 3 se ejecuta `git rev-parse --short HEAD` y el valor se escribe aquí. Ancla del diff: ese SHA, **nunca `HEAD~N`**
**Última fase**: 2 — planeación cerrada, 11-ago-2026. Plan completo en [`docs/us-planning/us-029.md`](../us-planning/us-029.md)
**Estimación**: 1 SP · Día: jue 13-ago-2026 · Estado esperado al cierre: **cerrada degradada (estado compartido estático)**
**Fuente normativa**: §26 del plan (líneas 2484-2511, manda durante S4) y [`docs/semana_4/plan_excelencia.md`](../semana_4/plan_excelencia.md) §6.3 (Pantalla 3), §7.5 (superposiciones), §7.9 (240 ms) y §13.D (jueves 13)
**Bloque derogado**: §16 líneas 1531-1553 (catálogo de julio: overlay por respuesta del agente y «Resumir vista actual»). Gana el que trae «(alcance S4)»

## Dominios y sub-tareas

- [x] **backend** · [x] **frontend** · [ ] ml · [ ] agent · [~] infra · [x] **db** · [x] **docs**
- **`ml` sin marcar, y es una decisión.** El seed son 48 filas curadas sin regla que las expanda: un emisor de Python que imprima una lista literal no compra reproducibilidad, la compra git. US-008 sí necesita generador porque expande 304 campos.
- **`infra` parcial**: solo la generalización **condicional** de `db-seed` en el `Makefile`, y solo si el objetivo de US-008 aplica un archivo fijo en lugar de recorrer el directorio.
- **La US SÍ se reparte, en dos agentes y una sola ola**: **A · dato y API** (`backend-engineer`) y **B · interfaz** (`frontend-engineer`). El motivo no es el volumen: es que la mitad de datos no depende de nada en vuelo y la mitad de interfaz depende de un contrato congelado en §4 del plan, no de un archivo. **Ninguno lee un archivo que el otro escribe.**
- Orden interno de A: migración → `make db-up` → seed → `make db-seed` → `models/lineage.py` → `services/lineage_service.py` → `api/lineage.py` → `main.py` → fila del registro → `docs/security.md` → `make permisos-ui` → pruebas.
- Orden interno de B: `types/linaje.ts` → `utils/foco.ts` → `test/foco.spec.ts` → **i18n** → `useFocoAtrapado.ts` → `gobierno/PanelLinaje.vue` → `test/panelLinaje.spec.ts` → los dos composables de datos → los cuatro componentes restantes de `gobierno/` → `contexto/PanelContextoTablero.vue` → las dos líneas de cada página → pruebas de componente.
- Dos reglas de orden que no son arbitrarias: **la función pura de foco y su prueba van antes que el componente**, porque el ciclo de tabulación es la lógica de la que cuelga todo el criterio de accesibilidad; y **los catálogos i18n van antes que las plantillas**, porque escribir la plantilla primero invita a dejar una cadena literal «temporal» que sobrevive al commit.

## Archivos previstos

*(previsión de la Fase 2, no snapshot. Se sustituye por `git status --short` al cerrar la Fase 3.)*

**Crear — dato y API (agente A)**

- `db/migrations/<ts>_create_catalog_lineage.sql` — tabla `catalog_lineage_step`, un índice único que además es el de recuperación, cuatro `COMMENT ON`. **Cero `ALTER TABLE` sobre tablas de US-008**
- `db/seeds/catalog_lineage.sql` — **48 filas curadas** (12 fuentes × 4 etapas), idempotente, sin un solo id literal
- `backend/app/models/lineage.py` — `CatalogLineageStep`, `LineageStage`, `LINEAGE_TRANSFORMATION_CODES`, `LineageStepOut`, `FieldLineageResponse`. **Importa** `SourceRef`, `OwnerRef`, `Validity` y `Facets` de `models/catalog.py`
- `backend/app/services/lineage_service.py` — `get_field_lineage`, `build_presentation_step`, `resolve_owner`, `FieldNotFoundError`
- `backend/app/api/lineage.py` — router propio con `prefix="/catalog"`. **No se abre `api/catalog.py`**
- `tests/backend/test_lineage_endpoint.py` · `tests/backend/test_lineage_integracion.py`

**Modificar — dato y API (agente A)**

- `backend/app/main.py` — **una línea**: monta el router
- `backend/app/core/permissions.py` — **una fila**: `GET /api/catalog/{entry_id}/lineage` con `scopes=()`, dueño US-029, estado `vigente`
- `docs/security.md` — **solo el bloque generado** entre `<!-- matriz-permisos:inicio -->` y `<!-- matriz-permisos:fin -->`, con el comando publicado por US-016
- `scripts/generar_permisos_ui.py` — **dos líneas**: la rama 3.2 sale de `SCOPE_EXPLICITO` y entra en `MAPA_RAMA_ENDPOINTS` con la ruta nueva
- `frontend/app/utils/permisos.generated.ts` — **REGENERADO** con `make permisos-ui`. Nadie lo escribe a mano
- `db/schema.sql` — **REGENERADO** con `make db-up`, siempre por el servicio `dbmate` de Compose
- `Makefile` — **condicional**, tres líneas como máximo: generalizar `db-seed` a un recorrido ordenado de `db/seeds/*.sql`. Si ya lo recorre, no se toca

**Crear — interfaz (agente B)**

- `frontend/app/types/linaje.ts`
- `frontend/app/utils/foco.ts` — `enfocables()`, `siguienteEnfocable()`, **funciones puras**
- `frontend/app/composables/useFocoAtrapado.ts` — el ciclo completo: guardar disparador, entrar, interceptar en captura, cerrar, devolver, limpiar
- `frontend/app/composables/useBusquedaCatalogo.ts` — `GET /api/catalog/search`, `immediate: false`
- `frontend/app/composables/useLinajeCampo.ts` — `GET /api/catalog/{id}/lineage`, **bajo demanda**
- `frontend/app/components/gobierno/PanelLinaje.vue` — la superposición de §7.5: `<dialog>` nativo con `showModal()`, velo por `backdrop:`, hoja inferior en móvil y panel lateral en escritorio
- `frontend/app/components/gobierno/DiccionarioCampos.vue` · `TarjetaCampo.vue` · `OverlayLinaje.vue` · `RecorridoLinaje.vue`
- `frontend/app/components/gobierno/AccesoBitacora.vue` — **absorbido de US-018/019**: enlace a `/administracion` visible solo con `alcanza('admin')`
- `frontend/app/components/contexto/PanelContextoTablero.vue`
- `frontend/test/foco.spec.ts` · `panelLinaje.spec.ts` · `diccionarioCampos.spec.ts` · `contextoTablero.spec.ts`

**Modificar — interfaz (agente B)**

- `frontend/app/pages/gobierno.vue` — **YA EXISTE** (21 líneas). Un `import` y `<DiccionarioCampos />` tras el `<p>`
- `frontend/app/pages/asistente.vue` — **YA EXISTE**. Un `import` y `<PanelContextoTablero />` tras el `<p>`
- `frontend/i18n/locales/es.json` y `en.json` — **tres bloques raíz nuevos**: `catalog.facet.*` (34 claves), `lineage.*` —incluido `lineage.crossAccess.*`— y `sharedContext.*`; más la reescritura en presente de `screen.governance.description`. **Sin conflicto con `admin.*` de US-018/019**, verificado contra su plan
- `frontend/test/pantallas.spec.ts` — **dos líneas condicionales**: el doble de `useFetch` y `createPinia()`, cada una solo si su `grep` previo devuelve 0

**Crear — documentos**

- `docs/manual-test/us-029.md` · `docs/us-planning/us-029.md` · `docs/us-handoff/us-029.md`

**Archivos existentes reutilizados, no tocados**

- `backend/app/models/catalog.py` (US-008) — se **importan** `SourceRef`, `OwnerRef`, `Validity`, `Facets`. Un segundo vocabulario para el mismo hecho es la divergencia que la auditoría castigó
- `backend/app/api/catalog.py`, `db/seeds/catalog.sql`, `ml/data/**`, `docs/catalogo-semantico.md` (US-008 y US-006)
- `frontend/app/stores/workspace.ts` y `frontend/app/types/tablero.ts` (US-025) — se **consumen en lectura**: `contextoAgente`, `hayFiltros`, `serializarVista()`, `ultimaInteraccion`, `origen`
- `frontend/app/composables/usePermisos.ts` (US-017) — `expirarSesion()` en la rama del 401, `alcanza()` donde haga falta
- `frontend/app/assets/css/main.css` y `frontend/app/utils/tokens.generated.ts` — **GENERADOS**; solo se consumen tokens ya emitidos
- `frontend/app/components/guia/inventarioIconos.ts` (US-UX-09) — se **lee** para saber qué iconos están empaquetados; no se le añade ninguno
- `frontend/app/layouts/portal.vue` y `frontend/app/components/nav/**` (US-001, US-017)
- `frontend/test/i18nDePrueba.ts` — se usa tal cual, **sin añadirle nada**
- `frontend/test/contratos.spec.ts` e `idioma.spec.ts` (US-UX-09) — **no se tocan**, pero actúan sobre todo lo nuevo

## Zonas sensibles

| Zona | Regla |
|---|---|
| `frontend/app/assets/css/main.css` · `frontend/app/utils/tokens.generated.ts` | **Generados** por `docs/entregables/generar_tokens_a4.py`. Nadie los edita. El overlay se maqueta solo con tokens ya emitidos: `radius-lg`, `shadow-flotante`, `breakpoint-sm`, los neutros y los semánticos |
| `frontend/app/components/guia/**`, **incluido `inventarioIconos.ts`** | De US-UX-09. **Prohibidos.** Consecuencia verificada: `lucide:x` no está empaquetado, así que **el botón de cierre lleva texto, no icono** |
| `frontend/app/utils/permisos.generated.ts` | **Generado** por `make permisos-ui`. Se regenera, jamás se edita. Lo produce el agente A porque exige el registro del backend |
| `docs/security.md` | De US-016. **Solo se regenera el bloque entre marcadores** con el comando publicado; ni una línea de prosa alrededor |
| `backend/app/core/permissions.py` | De US-016. Diff máximo: **una fila** en `SCOPE_REGISTRY`. Sin ella `assert_scope_coverage` impide que la aplicación arranque |
| `scripts/generar_permisos_ui.py` | De US-017. Diff máximo: **dos líneas**. La rama 3.2 no puede declarar a la vez endpoints y scope explícito: el generador aborta |
| `backend/app/main.py` | Compartido por todas las US que publican router. Diff máximo: **una línea** |
| `frontend/app/pages/gobierno.vue` | **De esta US, en exclusiva.** US-018/019 declaró que no la abre y esta US absorbe su acceso cruzado a la bitácora (ver frontera) |
| `frontend/app/components/administracion/**` · `frontend/app/pages/administracion.vue` · `backend/app/api/users.py` · `backend/app/services/user_service.py` · `types/usuarios.ts` · `composables/useUsuarios.ts` · el bloque i18n `admin.*` · la migración de `app_user` | De US-018/019. **Prohibidos**, y así queda comprometido con ellos |
| `frontend/app/pages/asistente.vue` | **Compartido con US-023/024/028 (viernes).** Diff de esta US: **dos líneas**. `screen.assistant.description` **no se toca** |
| `frontend/test/pantallas.spec.ts` | Compartido por varias US esta semana. Diff máximo: **dos líneas condicionales**, cada una precedida de su `grep` |
| `frontend/i18n/locales/{es,en}.json` | Cada US añade **su bloque raíz**. `catalog.*`, `lineage.*` y `sharedContext.*` son de US-029. `authz.*` es de US-017, `workspace.*` de US-027, `dashboard.*` de US-025 |
| `frontend/app/stores/**` · `types/tablero.ts` · `components/tablero/**` · `components/inicio/**` · `utils/fechas.ts` | De US-025, US-026 y US-027. **Solo lectura, y solo el store** |
| `db/seeds/catalog.sql` · `ml/**` · `data/README.md` · `docs/catalogo-semantico.md` | De US-008 y US-006. **Prohibidos** |
| Migraciones ya aplicadas | **Jamás se editan.** Si hiciera falta una columna en una tabla de US-008, se pide en este handoff y la emite una migración correctiva de US-008 |

## Frontera con US-018/019, que se planea en paralelo

Las dos US entregan el mismo jueves 13. La auditoría del lote anterior encontró trece defectos, tres bloqueantes, **todos en fronteras como esta**. Reparto sin zonas grises, **y no se da por supuesto que la otra planeación lo declare**:

**Su plan ya existe en el repositorio** (`docs/us-planning/us-018.md`, leído el 11-ago) y ya decidió sobre los dos puntos calientes. **Manda el repositorio**: las dos decisiones se adoptan y aquí queda escrita la mitad que toca a US-029.

**1. El primitivo de superposición — resuelto, y la resolución es suya.** Su §2.6 cierra el asunto con la frase correcta: «el primitivo compartido es la plataforma», es decir `<dialog>` con `showModal()`, que da atrapado de foco, `Esc` vía el evento `cancel`, capa superior y devolución del foco. **Se adopta.**

- **Ninguna de las dos crea un componente compartido de superposición**, y **ninguna importa nada de la otra**: la suya vive en `components/administracion/DialogoConfirmacion.vue`, la mía en `components/gobierno/PanelLinaje.vue`. El contrato compartido no es un archivo: son el elemento de la plataforma y las cinco reglas de §7.5.
- **Lo propio de US-029 son `composables/useFocoAtrapado.ts` y `utils/foco.ts`**, el ciclo de foco explícito. Existe porque **los criterios de aceptación de esta US son el foco** y `happy-dom@20.11.2` implementa `showModal()` como un `setAttribute('open','')` —hallazgo suyo, acreditado en §2.6 del plan—, de modo que sin ciclo propio los cuatro criterios centrales no tendrían una sola verificación automática. Su US no lo necesita: su criterio es la confirmación explícita. **Nadie tiene que consumirlo**; si acabaran necesitándolo, lo consumen y no escriben un segundo.
- **Cambio respecto del borrador anterior de este handoff, y su razón**: la superposición **deja de vivir en `components/comun/`**. Un componente en territorio compartido con un solo consumidor es generalidad especulativa, prohibida igual que el andamiaje. Vive en `components/gobierno/`, junto al resto de su pantalla, que además es la convención del repositorio (`inicio/`, `administracion/`, `nav/`). **Cuando aparezca un tercer consumidor, se promueve.**

**2. El acceso cruzado desde `/gobierno` a la bitácora — lo absorbe US-029.** Su §2.7 lo deja fuera de su write-set con un argumento que se acepta entero (la funcionalidad es US-041, 3 SP, roadmap) y lo declara bloqueo abierto con dueño propuesto —quien escriba `pages/gobierno.vue`— y vencimiento el sáb 15.

- **US-029 lo entrega**, en `frontend/app/components/gobierno/AccesoBitacora.vue`, con su CA-14 y su prueba. Son un enlace y tres claves i18n: cuesta menos que coordinar dos ramas el viernes.
- **Se acata su contrato tal como lo escribieron**: destino `/administracion`, visible **solo** para quien alcanza el módulo 4, y para el resto la copia existente `authz.noPermission.requestTo` de US-017. Es el defecto RU-04 que US-027 identificó y no se reintroduce.
- **Su contingencia del vie 14 queda sin efecto**: nadie tiene que escribir en `pages/gobierno.vue` después de US-029.
- **Lo que no se absorbe**: la tabla, el endpoint y la pantalla de bitácora. Son US-041 y siguen en roadmap.

**3. Los bloques de i18n — sin conflicto, verificado contra su plan.** US-018/019 reserva `admin.*` y reescribe `screen.administration.description`; US-029 reserva `catalog.*`, `lineage.*` y `sharedContext.*` y reescribe `screen.governance.description`. Los cuatro conjuntos son ajenos y cada clave reescrita es de la pantalla de su dueño.

**Lo que se le pide a US-018/019:** que **no** escriba en `frontend/app/pages/gobierno.vue`; que no abra `components/gobierno/**`, `components/contexto/**`, `composables/{useFocoAtrapado,useBusquedaCatalogo,useLinajeCampo}.ts`, `utils/foco.ts` ni los tres bloques de i18n reservados; y que si necesita `frontend/test/pantallas.spec.ts` compruebe antes con los dos `grep` del plan. **Lo que US-029 se compromete a no tocar:** `backend/app/api/users.py`, `backend/app/services/user_service.py`, `frontend/app/pages/administracion.vue`, `components/administracion/**`, `types/usuarios.ts`, `composables/useUsuarios.ts`, la migración de `app_user` y el bloque `admin.*`.

**Orden de aterrizaje: indiferente**, y eso es el resultado de haber cerrado bien la frontera. Ninguna importa un archivo de la otra, ninguna escribe en la página de la otra y sus bloques de i18n son ajenos. El único archivo compartido de la ola es `frontend/test/pantallas.spec.ts`, con su regla de dos líneas condicionales válida en cualquier orden.

**Hallazgo heredado de su plan que esta US confirma y no resuelve**: Elena Ruiz, la persona de la tarea de autorización de US-UX-07, está sembrada como `operativo` y el módulo 4 exige `admin`, así que **no verá el enlace** que US-UX-07 quiere medir el sáb 15. El componente no ofrece una puerta cerrada —oculta y explica—, pero la contradicción de fondo toca a US-015, US-017 y US-UX-07 y **hay que decidirla antes del sáb 15**, no durante la pre-validación.

## De dónde salen los datos de linaje

**El catálogo sembrado es de US-008 y esta US no le añade ni una columna.** Verificado sobre su §8.3: `catalog_source`, `catalog_field` y `catalog_tribal_note` **no tienen ninguna columna de linaje**, y su plan tampoco lo promete —su §5.2 enumera lo que deja fuera y el linaje no aparece ahí porque nadie se lo pidió—.

**Lo que sí aporta US-008 y se consume sin duplicar**: el sistema de origen (`catalog_source.system_of_record`), el propietario (`owner_area`, `owner_name`, `catalog_field.steward`) y la vigencia (`valid_from`, `valid_to`). **Lo que falta**: el tramo intermedio, con **su propietario y su vigencia por salto** —el dueño de la extracción no es el dueño de la fuente, y la vigencia de una regla de conversión no es la de la definición del campo—. Es una relación uno-a-muchos que no cabe en columnas de `catalog_field`.

**La frontera propuesta, y que este plan asume:**

1. **Tabla nueva `catalog_lineage_step`** en migración propia (`create_catalog_lineage`), de **propiedad exclusiva de US-029**, con FK a `catalog_source` y `ON DELETE CASCADE`. **Cero `ALTER TABLE`** sobre tablas ajenas.
2. **Seed propio y curado**, `db/seeds/catalog_lineage.sql`. Sin emisor en `ml/`: 48 filas sin regla que las expanda no necesitan un generador que imprima una lista literal.
3. **El nombre ordena la dependencia**: `catalog_lineage.sql` va después de `catalog.sql` alfabéticamente, que es el orden en que deben aplicarse.
4. **Si al implementar hiciera falta una columna en una tabla de US-008, no se añade aquí**: se registra como petición y se resuelve con una migración correctiva de US-008.

**El recorrido son cinco pasos: cuatro sembrados y uno derivado.**

| # | Etapa | De dónde sale | `stored` |
|---|---|---|---|
| 1 | `origen` | Sembrado; reproduce el `system_of_record` de la fuente | `true` |
| 2 | `extraccion` | Sembrado; el detalle nombra el trabajo de extracción | `true` |
| 3 | `transformacion` | Sembrado; código cerrado más el detalle concreto | `true` |
| 4 | `calidad` | Sembrado; es la mitad «y calidad» de la rama 3.2 del mapa de A3 | `true` |
| 5 | `presentacion` | **Compuesto en el servicio** desde `catalog_field` y el propietario ya resuelto | `false` |

**El paso 5 no se guarda a propósito**: duplicaría columnas que `catalog_field` ya tiene, y a la primera corrección de una definición el linaje diría una cosa y el diccionario otra **sin que nada lo detectara**. Es el mismo argumento con el que US-026 se negó a persistir sus proyecciones. Que sea derivado **se dice en el contrato (`stored: false`) y se muestra en pantalla**.

## Cómo se atrapa el foco, y con qué prueba

**`<dialog>` nativo abierto con `showModal()`, más un ciclo de foco propio encima.** La primera mitad es la decisión de US-018/019, adoptada sin renegociar. La segunda es lo que esta US necesita y la suya no: `happy-dom@20.11.2` implementa `showModal()` como `this.setAttribute('open','')`, así que delegarlo todo en la plataforma dejaría **los cuatro criterios centrales de esta US sin una sola verificación automática**. El manejador propio llama a `preventDefault()`: en el navegador **sustituye** al movimiento por omisión en lugar de sumarse a él, y en `happy-dom` **es** el comportamiento, de modo que la prueba mide código de producción y no la plataforma.

Dos simplificaciones que trae adoptar `<dialog>`: **no hace falta `Teleport`** —el elemento se promueve a la capa superior desde donde esté— y **el velo es `::backdrop`**, pintado con la variante `backdrop:` de Tailwind v4, sin `z-index` que negociar contra la barra lateral fija y **sin tocar `main.css`, que es generado**.

**Sin dependencia nueva**: `@vueuse/integrations` habría que instalarlo y `@vueuse/core` no trae trampa de foco. Lo propio es una **función pura** (`utils/foco.ts`) y un **composable** (`useFocoAtrapado.ts`), que es el patrón que el repositorio ya usa para la guarda de rutas.

Las seis reglas: (1) al abrir se llama a `showModal()` y se guarda `document.activeElement`, que es el disparador; (2) el foco entra al `h2` del panel, con `tabindex="-1"`, para que el lector de pantalla anuncie el nombre del diálogo; (3) `Tab` y `Shift+Tab` se interceptan **en fase de captura** sobre `document`, con `preventDefault()` —en burbuja el navegador ya movió el foco y la trampa llega un nodo tarde—, y la lista de enfocables **se recalcula en cada pulsación**, porque el panel cambia de contenido entre cargando, listo y error; (4) `Esc` se atiende por el evento **`cancel`** —que es lo que emite el navegador— **y** por la tecla en el manejador de captura, que es lo que hace la regla verificable; (5) el clic fuera es un `click` cuyo `target` es el propio `<dialog>`, técnica estándar con este elemento y la misma que usa mi compañero, y un clic en el contenido no cierra; (6) al cerrar se llama a `close()` y el foco vuelve al disparador, o al encabezado del diccionario si ese nodo ya salió del documento.

**Limpieza obligatoria**: los oyentes se retiran cuando `abierto` pasa a falso **y** en `onBeforeUnmount`. Un panel desmontado con el botón atrás dejaría un manejador global capturando `Tab` en toda la aplicación.

**Las pruebas que lo fijan** —`frontend/test/panelLinaje.spec.ts`, montado con `attachTo: document.body`, porque en happy-dom un nodo fuera del documento no recibe foco—: el foco entra al título al abrir; `Tab` desde el último devuelve al primero; `Shift+Tab` desde el primero lleva al último (el caso que caza la trampa que solo funciona hacia adelante); `Esc` y el evento `cancel` cierran y devuelven; un `click` cuyo `target` es el diálogo cierra; **un clic en el contenido no cierra**; `document.body` no gana estilo, clase ni hijo persistente; y desmontar abierto no deja oyentes. Y `frontend/test/foco.spec.ts` prueba la función pura: orden de documento, descarte de deshabilitados y de `tabindex` negativo, envoltura en los dos extremos, y lista vacía sin lanzar.

**El fondo inerte deja de ser una limitación pendiente**, y es la segunda ganancia de adoptar `<dialog>`: `showModal()` hace inerte todo el documento salvo el diálogo, **sin tocar `layouts/portal.vue`**, que es de US-017 y era el anclaje que no existía. Queda una limitación menor y declarada: `happy-dom` no implementa esa inercia, así que la comprobación con lector de pantalla y teclado real va al manual test.

## Cómo viaja el contexto del tablero al asistente

**Por el store Pinia `workspace` de US-025, con su nombre y su forma exactos**: símbolo `useWorkspaceStore`, id `'workspace'`, archivo `frontend/app/stores/workspace.ts`. Se leen tres cosas suyas y no se escribe ninguna: `contextoAgente: ComputedRef<ContextoAgente>`, `hayFiltros: ComputedRef<boolean>` y `serializarVista(): string`. **No se pide ninguna clave nueva**: `contextoAgente` es suficiente tal como US-025 lo definió.

`frontend/app/components/contexto/PanelContextoTablero.vue` se monta en `/asistente` y muestra tres niveles a dos clics: chips del contexto activo (0 clics), `<details>` con la última interacción y su procedencia más la ficha de origen de la serie (1 clic), y `<details>` con el **JSON exacto** que acompañaría la consulta, tomado de `serializarVista()` (2 clics).

**«Estático» significa un solo sentido, y es verificable, no una promesa**: (a) `components/contexto/**` no llama a ninguna de las seis acciones de escritura del store —hay un `grep` en los criterios—; (b) no importa `useChatStream`, que además no existe hasta US-023; (c) el aviso de la degradación **se renderiza** en los dos idiomas siempre que hay contexto.

**Dos limitaciones declaradas**: en una carga completa de `/asistente` (F5 o entrada directa) el store arranca por omisión y el panel muestra su **estado vacío diseñado** con la instrucción de abrir el tablero; y **«Resumir vista actual» NO está en el alcance de S4** —§26 la eliminó de los criterios y de las tres tareas técnicas, y exigiría `useChatStream`, de US-023—. Lo que queda listo es su insumo.

## El overlay en móvil

Un solo componente y el mismo DOM; **solo cambian las clases de posición**, para que las pruebas no dependan del ancho de ventana.

| Ancho | Forma | Por qué |
|---|---|---|
| < 768 px | **Hoja inferior**, `max-h-[80dvh]`, anclada abajo, esquinas superiores redondeadas, desplazamiento interno con `overscroll-contain` | Un diálogo a pantalla completa es, perceptivamente, una **navegación**: el lector pierde el contexto y necesita un «volver», que es justo lo que el criterio prohíbe. Con 80 % de la ventana quedan visibles el encabezado y la primera ficha, y el panel se sigue leyendo como una capa |
| ≥ 768 px (`sm:`) | **Panel lateral** derecho de `28rem`, a toda la altura | Es el «panel lateral» que la guía nombra en §7.5, con `shadow-flotante` y `radius-lg` |

Cuatro detalles decididos porque se descubren tarde: **los tres reinicios de `<dialog>`** (`m-0`, `w-full`, `max-w-none`), sin los cuales el agente de usuario centra y estrecha la hoja; **`dvh` y no `vh`** (en móvil la barra del navegador se retrae y `vh` deja la hoja cortada justo donde está el botón de cerrar); **nada de `overflow: hidden` en `body`** (en escritorio quita la barra de desplazamiento, el contenido se reajusta y la pantalla **no** queda como estaba, y además `showModal()` ya impide interactuar con el fondo); y **solo se animan `opacity` y `transform`**, 240 ms al entrar —la duración que §7.9 inventaría para esta misma animación— bajo `motion-safe:`.

## Contrato para la interfaz bilingüe

Tres bloques raíz nuevos: **`catalog.facet.*`** (34 claves), **`lineage.*`** y **`sharedContext.*`**. Cuatro reglas duras, las cuatro con prueba viva ya existente:

1. **Ninguna clave nueva puede tener el mismo texto en `es` y en `en`**: `idioma.spec.ts` fija con `toEqual(['brand.name','error.code'])` que solo dos mensajes pueden coincidir. Eso descarta las traducciones obvias y obliga a redactar `unit.MXN` como «Pesos mexicanos (MXN)» / «Mexican pesos (MXN)», `dataType.decimal` como «Número decimal» / «Decimal», y tres más que están resueltas en §2.8 del plan.
2. **Los parámetros de interpolación se conservan**: `{detail}` en las diez plantillas de transformación, `{count}` en el conteo de resultados, `{field}` en el título del panel.
3. **Paridad exacta de conjuntos de claves** entre los dos catálogos.
4. **Lo que es dato no es clave**: nombres físicos, nombres de negocio, definiciones, códigos de sistema, detalles de transformación y el JSON del contexto viajan **tal cual** en los dos idiomas, por la misma razón que US-008 dio para las definiciones del catálogo.

**Corrección declarada, para que no se creen claves gemelas.** US-008 §2.5 propuso la raíz **`catalogo.facetas.*`**, en español, y dijo que la escribiría US-UX-07. Dos cambios con su razón: **las escribe esta US**, porque US-UX-07 es el documento de A4 (sáb 15 y dom 16) y no escribe pantallas, mientras que la primera pantalla que las necesita es `/gobierno` el jueves 13; y **la raíz pasa a `catalog.facet.*`, en inglés**, porque `frontend/AGENTS.md` —que manda dentro de `frontend/`— exige claves jerárquicas en inglés y las ~120 existentes lo son sin excepción. La raíz en español **nunca llegó a existir en código**: US-008 no escribe ni un archivo de `frontend/`. Y el número correcto es **34, no 30**: se cuentan contra los `CHECK` de su propia migración (`domain` 8 + `dataType` 6 + `sensitivity` 3 + `refreshFrequency` 4 + `certification` 3 + `unit` 5 + `aggregation` 5).

## Nube, dependencias y variables

- **Ningún comando contra GCP y ningún gasto.** US-003 está al final de la cola por decisión del usuario del 10-ago-2026.
- **Cero dependencias nuevas** en `frontend/package.json` y en `backend/pyproject.toml`. El atrapado de foco se implementa sin biblioteca; el diff sobre los dos candados debe ser **vacío**.
- **Ninguna variable de entorno nueva**, ni en `backend/.env.example` ni en `frontend/.env.example`.
- **Sin `routeRules` ni SWR** puestos por esta US.

## Schema

**Una migración: `create_catalog_lineage`.** Crea `catalog_lineage_step` con FK a `catalog_source` y **`ON DELETE CASCADE`, que es obligatorio y no cosmético**: el seed de US-008 abre con `TRUNCATE ... CASCADE` y una FK sin cascada dejaría `make db-seed` roto para todo el equipo. Hay una prueba de integración dedicada a eso. El `down` es un `DROP TABLE IF EXISTS`; los índices y las restricciones caen con la tabla. `db/schema.sql` se regenera en el mismo commit, siempre por el servicio `dbmate` de Compose.

Y queda escrito **lo que no debe existir**, para que S5 no lo invente: **no hay tabla de pasos de presentación y no debe haberla.** Ese paso es función determinista de `catalog_field`; persistirlo crea una segunda verdad para el mismo hecho, que es la variante en base de datos del defecto que este plan cierra en la interfaz.

## Degradaciones declaradas de antemano (el silencio no es opción)

1. **Estado compartido con payload estático** (recorte #12): los filtros viajan al asistente como contexto; **el agente no muta el tablero**. Se declara en la pantalla, en los dos idiomas, y en el documento de A4 con la frase literal de §9 del plan.
2. **«Resumir vista actual» fuera del alcance de S4.** §26 la eliminó de los criterios; necesita `useChatStream`, que es de US-023. El insumo (`contextoAgente`) queda listo.
3. **El paso terminal del recorrido es derivado, no almacenado**, y la interfaz lo dice con `stored: false`.
4. **El contenido del linaje es sintético y curado**, como el resto del prototipo, y la franja de alcance ya lo declara en las diez rutas.
5. **La inercia del fondo la da `showModal()` en el navegador y no `happy-dom`**: la prueba automática no puede afirmarla y la comprobación con lector de pantalla va al manual test. No es una degradación de producto sino del alcance de la verificación, y por eso se dice.

## Bloqueos y dependencias

- **Dependencia dura: US-008** (mar 11). Sin catálogo sembrado no hay fuentes que sembrar ni campos que listar. **Degradación decidida**: si a las 10:00 del jue 13 `SELECT count(*) FROM catalog_field` devuelve 0, se entregan esquema, seed, endpoint e interfaz completa con sus cuatro estados, y la pantalla queda en «vacío diseñado». **No se siembran campos falsos en el frontend**: sería una segunda fuente de verdad.
- **Dependencia dura: US-015 y US-016** (mar 11). `get_current_user`, `Scope` y `SCOPE_REGISTRY` son la condición para que el endpoint arranque.
- **Dependencia media: US-017** (mar 11). `expirarSesion()` es el gancho del 401 y `scripts/generar_permisos_ui.py` es lo que se edita en dos líneas. Si resbala, el 401 no llama a nada y la regeneración del mapa se difiere; las dos cosas se anotan.
- **Dependencia dura de media US: US-025** (mié 12). **Decisión con hora: jue 13, 12:00.** Si `frontend/app/stores/workspace.ts` no existe, el panel del asistente **no se construye** —no se escribe un store paralelo— y se declara como pendiente en el PR, en este handoff y en la tabla de alcance del PDF. El overlay se entrega igual, que es el título de la US.
- **Dependencia blanda: US-026** (mié 12). Solo el punto de ramificación y el doble de `useFetch` en el spec compartido.
- **Sin dependencia de US-006**: el linaje no lee ningún Parquet.
- **Bloqueo heredado y cerrado**: US-018/019 dejó el **acceso cruzado desde `/gobierno`** fuera de su write-set, con vencimiento el sáb 15. **Esta US lo absorbe** (CA-14) y su contingencia del vie 14 queda sin efecto.
- **Bloqueo heredado y NO cerrado, que se traslada al usuario**: Elena Ruiz está sembrada como `operativo` y el módulo 4 exige `admin`, así que no verá el enlace que US-UX-07 quiere medir el sáb 15. Toca a US-015, US-017 y US-UX-07. **Decisión necesaria antes del sáb 15.**
- **Esta US bloquea a US-UX-07** (sáb 15): las figuras de la Pantalla 3 —diccionario, overlay en escritorio, overlay en móvil y los cuatro estados— salen de aquí.

## Acciones para el integrador

1. **Verificar que en `frontend/test/pantallas.spec.ts` el doble de `useFetch` aparece una sola vez** tras integrar US-025, US-026 y US-029, y que `createPinia()` aparece una sola vez.
2. **US-UX-07 no debe crear las claves `catalogo.facetas.*` en español**: ya existen como `catalog.facet.*`. Conviene actualizar la tabla de §2.5 de `docs/us-planning/us-008.md` para que el próximo lector no las duplique.
3. **`screen.assistant.description` sigue en futuro** y describe una pantalla que el viernes deja de ser marcador de posición. La reescribe quien cierre `/asistente` (US-023), una sola vez.
4. **Comprobar el reparto de superposiciones tras integrar US-018/019**: `grep -rn "showModal" frontend/app/components` debe devolver **dos** archivos, cada uno en la carpeta de su pantalla, y **cero** componentes de superposición en `components/comun/`. Si aparece un tercer consumidor, promover `PanelLinaje` a `comun/` en esa US, no antes.
5. **Verificar el puente de códigos entre lenguajes**: `LINEAGE_TRANSFORMATION_CODES` (Python), el `CHECK` de la migración y `CODIGOS_TRANSFORMACION` (TypeScript) deben nombrar los mismos nueve códigos sembrables. Es un paso explícito de `docs/manual-test/us-029.md`, no una suposición.

## engram-memory

`mem_search` **no estuvo disponible** en la sesión de planeación: el inventario expuesto al subagente es Read, Write, Edit, Bash, Glob y Grep, sin herramientas `mem_*`. Se sustituyó por un barrido `grep -rn` sobre `context/`, `docs/` y `frontend/` con las palabras clave de la US (`linaje`, `lineage`, `overlay`, `foco`, `workspace`, `contexto`, `superposicion`). Se deja constancia en lugar de simular la consulta. Los seis hallazgos que cambiaron una decisión están tabulados en la lectura previa del plan.

## Context7

**No consultado, y es una decisión y no una omisión.** Esta US no añade ninguna dependencia y no usa ninguna API nueva ni cambiada de versión: Vue 3.5, vue-i18n 11, `useFetch` de Nuxt 4.5, Pinia (que instala US-025), FastAPI, SQLModel y PostgreSQL 15 ya están en uso y sus firmas se leyeron del código y de los planes reales. El único punto que lo habría justificado —el atrapado de foco— se resuelve **sin biblioteca**, por la razón escrita arriba.

## Siguiente acción

Ejecutar la Fase 3 con **dos agentes en una ola**, después de que US-026 cierre.

- **Agente A (`backend-engineer`)**, primer paso concreto: `make db-new SLUG=create_catalog_lineage`, escribir el SQL de §8.2 del plan, correr `make db-up && make db-rollback && make db-up` y **verificar el ciclo antes de escribir una línea de Python**. Una migración que no se sabe revertir es una migración que nadie se atreve a corregir.
- **Agente B (`frontend-engineer`)**, primer paso concreto: `frontend/app/utils/foco.ts` y `frontend/test/foco.spec.ts`, y correr `pnpm --dir frontend test test/foco.spec.ts` **antes de escribir un solo `.vue`**. El ciclo de tabulación es el contrato del que cuelga todo el criterio de accesibilidad; si se escribe el componente primero, la trampa acaba siendo un `if` dentro de una plantilla y deja de poder probarse.

## Fase 3 y 4 — Programacion, verificacion y QA (13-ago-2026)

**SHA base**: `106c58b`. Ancla del diff: `git diff --name-only 106c58b`, nunca `HEAD~N`.

Las dos olas corrieron **en paralelo** y la interseccion de sus write-sets fue vacia. La ola B
programo contra el **contrato congelado de la seccion 4**, no contra los archivos de la ola A, y la
prueba de que el contrato estaba bien escrito es que al aparecer el endpoint real **no hizo falta un
solo ajuste de mapeo**: los tipos `PasoCrudo` y `LinajeCrudo` encajaron tal cual.

> **Interrupcion**: la sesion de Claude Code se cayo con las dos olas en vuelo. Se reanudaron desde
> su transcripcion, conservando su contexto. Lo unico que habia aterrizado era la migracion, ya
> aplicada. No se perdio trabajo.

### Verificacion de los criterios, ejecutada

| CA | Como se verifico | Resultado |
|---|---|---|
| CA-1, CA-1b | API real y navegador | **5 pasos** en orden `origen, extraccion, transformacion, calidad, presentacion`; `data-derivado` es `false` en los cuatro sembrados y `true` en el terminal; `transformacion`, `propietario` y `vigencia` presentes y no vacios en los cinco |
| CA-2, CA-2b | `psql` contra la base sembrada | 48 filas, 12 fuentes distintas, ninguna fuente sin pasos, los 9 codigos sembrables usados de verdad |
| CA-4b, CA-5 | Navegador, con el foco puesto en el disparador antes de activarlo | El foco **entra** al panel sobre su encabezado, `Escape` cierra y el foco **vuelve al nodo exacto** que lo abrio |
| CA-6b | Navegador, `/asistente` sin filtros | `data-estado="sin-contexto"`, **cero chips** y la instruccion de como construir el contexto |
| CA-7 | `grep` de las seis acciones de escritura sobre `components/contexto/` | **0 lineas** |
| CA-9 | Lectura de donde caen las clases en el `<dialog>` | Movil `mt-auto max-h-[80dvh] rounded-t-lg`; solo bajo `sm:` pasa a `sm:h-full sm:w-[28rem]`. Sin `h-screen` ni `inset-0` |
| CA-10, CA-11 | API real | 401 con reto; **200** con los cinco pasos; **404 tipado como objeto**: `{"codigo":"campo_no_encontrado","entry_id":999999}` |
| CA-12 | `git diff --stat` | 10 paginas intactas; `pantallas.spec.ts` con **+7 lineas**, un solo bloque condicional |
| CA-13 | `make check && make test` | Limpio. **451 de backend** (17 omitidas de integracion) y **655 de frontend** |
| CA-14 | Navegador, con dos sesiones | Con `operativo` el nodo **no esta en el DOM** y se muestra la copia de a quien pedirlo; con `admin` aparece «Ir a Administracion» con destino `/administracion` |

Consola del navegador: **0 errores y 0 avisos** en `/gobierno` y en `/asistente`.

### CA-3: divergencia declarada, no tapada

CA-3 mide que el `outerHTML` de `[data-diccionario]` sea **identico** antes de abrir y despues de
cerrar. En navegador real **no lo es**: el `<dialog>` vive dentro del diccionario y conserva el
nombre del campo en su encabezado tras cerrarse.

**Se deja como esta, y es una decision.** Vaciarlo al cerrar haria que el titulo parpadee vacio
durante los 240 ms de la transicion de salida: un defecto visible a cambio de satisfacer una
comparacion de cadenas invisible. Lo que CA-3 protege de verdad se verifico por separado y si se
cumple: al cerrar, `document.body` no gana atributo `style`, ni clase, ni hijo persistente, y el
dialogo queda cerrado e inerte.

### Un falso positivo del propio QA, anotado para que no vuelva

La primera medicion del ciclo de foco dio que **el foco no volvia al disparador**, que es justo el
defecto que CA-5 existe para impedir. Era el instrumento: se abrio el panel con un `.click()` desde
JavaScript, **que no da el foco al boton**, asi que el composable restauraba correctamente el foco a
lo que lo tenia — `document.body`. Repetida enfocando el disparador antes de activarlo, la
comprobacion pasa. **Una prueba de accesibilidad que abre con `.click()` sintetico no mide lo que
cree medir.**

### Divergencias que otras US necesitan

- **Tokens**: el plan nombra `shadow-flotante` e `ink-strong`, que **no existen** en el `@theme`
  generado. Los reales son **`shadow-dialogo`** y **`backdrop:bg-ground-alt/80`**. Importa porque un
  token inexistente en Tailwind **no falla: desaparece**, y habria dejado el dialogo sin sombra y sin
  velo sin que ninguna prueba lo viera. **US-UX-07 lee ese plan el sabado para la guia de estilos.**
- **`pages/gobierno.vue`**: se **sustituyo** el cartel `EstadoPendiente us="US-029"` por el
  diccionario. Dejarlo encima de la funcionalidad que esta misma US entrega seria mentir en una
  pantalla que se captura para A4. En `/asistente` el cartel de US-023 **se queda**: ahi todavia dice
  la verdad.
- **`AccesoBitacora.vue`** no estaba en la lista que el orquestador paso, pero el write-set era
  `gobierno/**` y **CA-14 lo exige**. Se entrega reutilizando la copia `authz.noPermission.requestTo`
  que ya existia, sin claves i18n nuevas.
- **`scripts/seed_catalogo.sh` y `Makefile`**: el plan suponia el archivo del seed fijado en el
  `Makefile`; esta dentro del guion, y la cabecera del `Makefile` prohibe logica de shell en las
  recetas. Make calcula ahora `SEEDS := $(sort $(wildcard db/seeds/*.sql))` y el guion itera.

### Pendiente que esta US no resuelve

**Elena Ruiz esta sembrada como `operativo`** y el modulo de administracion exige `admin`: en la
pre-validacion del sabado no vera el enlace a la bitacora. Lo hereda quien prepare esos recorridos.

---

### US-029 — Ola A (dato y API). Informe de cierre

Rama `us-029`, SHA base `106c58b`. **Sin commit**, como se pidió. Todo verificado contra el stack
real (PostgreSQL en `127.0.0.1:55432`, API en Docker `127.0.0.1:8000`).

---

#### (a) Archivos

**Creados**

| Archivo | Qué es |
|---|---|
| `db/migrations/20260812121501_create_catalog_lineage.sql` | Tabla `catalog_lineage_step`, sus `CHECK`, el índice único y cuatro `COMMENT ON`. Cero `ALTER TABLE` |
| `db/seeds/catalog_lineage.sql` | 48 filas curadas (12 fuentes × 4 pasos), idempotente, sin un solo id literal |
| `backend/app/models/lineage.py` | `LineageStage`, `LINEAGE_TRANSFORMATION_CODES`, `FIELD_PUBLISH_CODE`, `LINEAGE_STEP_COUNT`, `LineageErrorCode`, `ErrorLinaje`, `CatalogLineageStep`, `LineageStepOut`, `FieldLineageResponse` |
| `backend/app/services/lineage_service.py` | `get_field_lineage`, `build_presentation_step`, `resolve_owner`, `in_force`, `FieldNotFoundError` |
| `backend/app/api/lineage.py` | Router propio, `prefix="/api/catalog"`, `tags=["linaje"]` |
| `tests/backend/test_lineage_endpoint.py` | 15 casos con la sesión doblada |
| `tests/backend/test_lineage_integracion.py` | 7 casos con marcador `integracion` contra PostgreSQL real |

**Modificados**

| Archivo | Diff |
|---|---|
| `db/schema.sql` | **Regenerado** por `make db-up` (servicio `dbmate` de Compose), +107 líneas |
| `backend/app/main.py` | Dos líneas: el `import` y `application.include_router(lineage.router)` |
| `backend/app/core/permissions.py` | **Una fila nueva** en `SCOPE_REGISTRY` |
| `docs/security.md` | Solo el bloque entre marcadores (una fila) **+ una fila de bitácora en §12** |
| `scripts/generar_permisos_ui.py` | Las dos líneas de la rama 3.2 + actualización del recuento en su comentario (ver divergencia D-2) |
| `frontend/app/utils/permisos.generated.ts` | **Regenerado** con `make permisos-ui`; jamás editado a mano |
| `Makefile` | Generalización condicional de `db-seed` (variable `SEEDS` + argumento en la receta) |
| `scripts/seed_catalogo.sh` | **Divergencia declarada D-1**: es donde vivía el archivo fijo |

**No tocado**: `ml/**` (cero archivos), `backend/app/api/catalog.py`, `models/catalog.py`,
`db/seeds/catalog.sql`, y **nada** bajo `frontend/app/{types,composables,components,pages}` ni
`frontend/i18n/` (las modificaciones que se ven ahí en `git status` son de la ola B).

---

#### (b) Decisiones y por qué

1. **Tabla propia colgada de `catalog_source`, no de `catalog_field`.** El tramo aguas arriba es el
   mismo trabajo de plataforma para todos los campos de una fuente. 48 filas curadas contra ~1200
   generadas por regla, que serían linaje inventado disfrazado de dato.
2. **El paso 5 se compone en el servicio (`stored: false`).** Persistirlo duplicaría columnas que
   `catalog_field` ya tiene. `presentacion` **no está en el `CHECK` de `stage`** y `field_publish`
   **no está en el `CHECK` de `transformation_code`**: una restricción que admite un valor que nadie
   inserta es una invitación a sembrar el paso derivado «para completar».
3. **El detalle del paso terminal es `physical_name`.** Es el dato que la plantilla `{detail}`
   interpola y responde «esta es la columna detrás de la cifra». Sistema: `KRS-Portal` /
   `Karisma Data` (constantes exportadas, no literales sueltos).
4. **`ON DELETE CASCADE` verificado, no supuesto.** Hay una prueba de integración que trunca el
   catálogo en cascada dentro de una transacción revertida: es lo que impide romper `make db-seed`
   para todo el equipo. En la corrida real se ve el `NOTICE: truncate cascades to table
   "catalog_lineage_step"`.
5. **Ruta nueva, no `GET /api/catalog/{entry_id}`.** Esa fila es de US-008 y su plan la difiere a S5.
   El linaje es bajo demanda; colgarlo de la ficha lo haría viajar en cada búsqueda.
6. **Un solo índice**, `catalog_lineage_step_source_order_key`, que es a la vez restricción de
   unicidad e índice de recuperación (`source_id` filtra, `step_order` ordena: es su prefijo exacto).
7. **Forma del error: `detail.codigo`.** Es la convención más reciente del repositorio (US-025,
   `ErrorSerie.as_detail()`, que `useSerieTablero.ts` ya lee) y es lo que pide CA-11. US-008 devuelve
   `detail` como cadena; se eligió la de US-025 porque es la que el frontend ya sabe leer.
8. **Dos consultas y una composición**, nunca una por paso. El orden lo pone el SQL (`ORDER BY
   step_order`), y hay una prueba de integración que inserta los cuatro saltos **al revés** para que
   un servicio que ordenara por `id` salga en rojo.
9. **`resolve_owner` en el servidor**: steward del campo primero, propietario de la fuente después.
   Es la misma regla que US-008 fijó para el panel de metadatos; el cliente nunca escribe un
   `coalesce`.
10. **Privacidad (trampa 6)**: el endpoint **no recibe texto libre**, solo un entero de ruta. No hay
    nada que hashear; el log emite `entry_id`, `fuente`, `pasos_sembrados` y `pasos`. La identidad la
    añade el binding de contextvars de US-015, igual que en el resto de la API.
11. **Pruebas que NO escribí, y por qué**: el plan pedía
    `test_ruta_registrada_y_cobertura_de_scopes`. Ya existen
    `tests/backend/permisos/test_scope_coverage.py::test_la_aplicacion_real_no_tiene_violaciones` y
    `::test_toda_ruta_viva_esta_declarada`, y `test_permission_matrix.py` se parametriza **sola**
    desde `SCOPE_REGISTRY`: mi fila ya se interroga por los cuatro roles y sin token. Escribir una
    tercera copia habría comprado cobertura sin significado.

---

#### (c) CONTRATO EXACTO DEL ENDPOINT (para la ola B)

```
GET /api/catalog/{entry_id}/lineage
```

- **Parámetro de ruta**: `entry_id`, entero, `ge=1`. Es el `field_id` que publica
  `GET /api/catalog/search`. **No hay parámetros de consulta**, ni `q`, ni paginación.
- **Autorización**: `Security(get_current_user, scopes=[])` → **cualquier sesión válida**, los cuatro
  roles. Sin token: **401** con `WWW-Authenticate: Bearer realm="karisma"`. No hay 403 posible.
- **Content-Type**: `application/json`. Sin cabeceras propias (ni ETag ni Cache-Control).

**200 — cuerpo (`snake_case`, tal cual lo emite FastAPI):**

```json
{
  "field_id": 1,
  "physical_name": "cli_ref",
  "business_name": "Referencia de cliente",
  "source": {
    "code": "creditos",
    "display_name": "Cartera de credito",
    "system_of_record": "SIC-Core",
    "has_extract": true
  },
  "owner": { "area": "Direccion de Credito", "steward": "Sofia Aranda" },
  "validity": { "valid_from": "2024-10-01", "valid_to": null, "is_current": true },
  "facets": {
    "domain": "cliente",
    "data_type": "texto",
    "sensitivity": "interna",
    "refresh_frequency": "mensual",
    "certification": "certificado",
    "unit": null,
    "metric_agg": "count"
  },
  "steps": [
    {
      "order": 1,
      "stage": "origen",
      "system_code": "SIC-Core",
      "system_name": "Core bancario SIC",
      "transformation_code": "origin_capture",
      "transformation_detail": "SIC-Core.CRE_CONTRATO",
      "owner": { "area": "Tecnologia de Core", "steward": "Alberto Nunez" },
      "effective_from": "2018-01-01",
      "effective_to": null,
      "is_current": true,
      "stored": true
    }
  ]
}
```

**Reglas del arreglo `steps`** (payload real verificado contra la API en Docker):

- Siempre **5 elementos** cuando la fuente está sembrada, con `order` = 1..5 y `stage` en este orden
  exacto: `origen`, `extraccion`, `transformacion`, `calidad`, `presentacion`.
- Los cuatro primeros llevan `stored: true`; **el último lleva `stored: false`**, `stage:
  "presentacion"`, `transformation_code: "field_publish"`, `system_code: "KRS-Portal"`,
  `system_name: "Karisma Data"` y `transformation_detail` = el `physical_name` del campo.
- `owner` de cada paso es **suyo** y difiere del `owner` de la cabecera; `effective_from` nunca es
  nulo, `effective_to` puede serlo, e `is_current` ya viene calculado (no lo recalcules en el
  cliente).
- Degradación honesta: si una fuente no tuviera pasos sembrados, el arreglo trae **solo el derivado**
  con `order: 1`. No es un caso posible con el seed actual (hay prueba de integración que lo impide),
  pero el contrato no miente.

**Códigos de transformación que la interfaz debe saber traducir** — exactamente estos diez, y los
nueve primeros son los del `CHECK` de la base (`LINEAGE_TRANSFORMATION_CODES`):

```
origin_capture · batch_extract · stream_extract · type_normalization · currency_conversion
deduplication · business_rule · reconciliation · quality_rule        (los nueve sembrables)
field_publish                                                        (solo el paso derivado)
```

Los diez se usan de verdad: el seed emplea los nueve (verificado,
`count(DISTINCT transformation_code) = 9`) y el servicio emite `field_publish` en el paso 5.

**Errores:**

| HTTP | Cuerpo | Cuándo |
|---|---|---|
| 401 | `{"detail": "credenciales_ausentes"}` (+ `WWW-Authenticate`) | Sin token o token inválido |
| 404 | `{"detail": {"codigo": "campo_no_encontrado", "entry_id": 999999}}` | Ningún `catalog_field` con ese id |
| 422 | Validación estándar de FastAPI | `entry_id` no entero o `< 1` |

> **Lee el código en `detail.codigo`** (objeto, no cadena), igual que en `useSerieTablero.ts` de
> US-025. El `entry_id` viaja de vuelta para que la pantalla pueda nombrarlo en su estado de error.

**Mapa de permisos**: `ENDPOINTS_POR_RAMA['3.2']` ya contiene `'GET /api/catalog/{entry_id}/lineage'`
y `SCOPE_POR_RAMA['3.2']` sigue siendo `null` (cualquier sesión). `permisos.generated.ts` está
regenerado; **no lo abras**.

---

#### (d) La migración

**Nombre exacto**: `db/migrations/20260812121501_create_catalog_lineage.sql`

`migrate:up` crea **una sola tabla**, `catalog_lineage_step`:

| Columna | Tipo y restricción |
|---|---|
| `id` | `BIGSERIAL PRIMARY KEY` |
| `source_id` | `BIGINT NOT NULL REFERENCES catalog_source(id) **ON DELETE CASCADE**` |
| `step_order` | `SMALLINT NOT NULL CHECK (BETWEEN 1 AND 9)` |
| `stage` | `TEXT NOT NULL CHECK IN ('origen','extraccion','transformacion','calidad')` — sin `presentacion`, a propósito |
| `system_code`, `system_name` | `TEXT NOT NULL`, cada uno con `CHECK (<> '')` |
| `transformation_code` | `TEXT NOT NULL CHECK IN (los nueve códigos)` |
| `transformation_detail` | `TEXT NOT NULL CHECK (<> '')` |
| `owner_area`, `owner_name` | `TEXT NOT NULL` |
| `effective_from` | `DATE NOT NULL` |
| `effective_to` | `DATE` nullable |
| `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT now()` |
| — | `CONSTRAINT catalog_lineage_step_validity_chk CHECK (effective_to IS NULL OR effective_to >= effective_from)` |

Más **cuatro `COMMENT ON`** (tabla, `stage`, `transformation_code`, `transformation_detail`) y **un
índice**: `CREATE UNIQUE INDEX catalog_lineage_step_source_order_key ON catalog_lineage_step
(source_id, step_order)`.

`migrate:down`: `DROP TABLE IF EXISTS catalog_lineage_step;` — **sin `CASCADE`** (regla de US-002);
índices y restricciones caen con la tabla.

Cero `ALTER TABLE` sobre tablas de US-008. `db/schema.sql` regenerado, nunca editado.

---

#### (e) Divergencias plan / repositorio

**D-1 (importante). `scripts/seed_catalogo.sh` está en el diff, y no estaba en el write-set.**
El plan (§3.1) decía: «si `db-seed` aplica un archivo fijo, se generaliza a un recorrido ordenado de
`db/seeds/*.sql`; el diff del `Makefile` son tres líneas como máximo». En el repositorio real **el
archivo fijo no está en el `Makefile`**: la receta es `@bash scripts/seed_catalogo.sh` y el
`catalog.sql` literal vive dentro del script. Y la cabecera del `Makefile` prohíbe explícitamente
meter lógica de shell en una receta («cada receta es un comando de una sola línea… funciona igual con
cmd.exe»), así que un bucle en la receta habría roto `make` desde PowerShell.

Solución adoptada, que respeta las dos reglas: **make calcula el recorrido**
(`SEEDS := $(sort $(wildcard db/seeds/*.sql))`) y lo pasa **como argumentos** al script, que ahora
itera sobre `"$@"`. El `Makefile` cambia en 3 líneas efectivas; el script gana el bucle y una guarda
(`db/*`, que es lo único que el servicio dbmate monta). Sin argumentos sigue aplicando solo
`catalog.sql`, así que ninguna invocación previa cambia de comportamiento. **Un seed nuevo ya no
tocará ninguno de los dos archivos.**

Hallazgo que costó una corrida y queda escrito en el propio script: **MSYS (Git Bash en Windows)
reescribe una variable de entorno cuyo valor parece una ruta POSIX absoluta** antes de pasársela a
`docker.exe`. Pasando `/db/seeds/catalog.sql` por `-e SEED_FILE`, psql dentro del contenedor recibía
`C:/Program Files/Git/db/seeds/catalog.sql`. La ruta viaja ahora **sin la barra inicial** y el
contenedor la reconstruye como `/db/$SEED_FILE`.

**D-2 (menor). `scripts/generar_permisos_ui.py`: tres líneas, no dos.** Las dos del plan (sacar `3.2`
de `SCOPE_EXPLICITO`, meterla en `MAPA_RAMA_ENDPOINTS`) más la corrección del recuento de su propio
comentario, que decía «siete de las dieciséis ramas… las otras cuatro son `None`» y con este cambio
son seis y tres. Un comentario que miente en el archivo que aborta cuando la declaración miente era
peor que la línea de más.

**D-3 (menor). `PermissionRule` usa `rule`/`status`, no `regla`/`estado`.** El plan y el handoff
escribían los nombres en español; el módulo de US-016 los tiene en inglés. Manda el repositorio.

**D-4 (menor). Prefijo del router.** El plan proponía `APIRouter(prefix="/catalog")` +
`include_router(..., prefix="/api")`. `api/catalog.py` y `api/metrics.py` usan
`APIRouter(prefix="/api/...")` y `include_router` sin prefijo; se siguió la del repositorio. La ruta
final es idéntica.

**D-5 (menor). Forma del 404.** Ver decisión 7: `detail.codigo` (US-025) y no `detail` como cadena
(US-008). CA-11 lo pedía así.

**D-6 (informativo). Una prueba del plan no se escribió** porque ya existe en US-016 y se
parametriza sola. Detalle en la decisión 11.

**Petición a US-008: ninguna.** No hizo falta ni una columna de sus tablas.

---

#### (f) Salida real de los gates

**1. Ciclo de migración y estabilidad de `schema.sql`**

```
$ make db-up && make db-rollback && make db-up
Applied: 20260812121501_create_catalog_lineage.sql in 22.739545ms
Rolled back: 20260812121501_create_catalog_lineage.sql in 7.999607ms
Applied: 20260812121501_create_catalog_lineage.sql in 22.739545ms
Writing: ./db/schema.sql
$ diff <copia previa> db/schema.sql
OK ciclo final: schema.sql estable          (sin diferencias)
```

**2. `make db-seed` idempotente**

```
$ make db-seed                       # dos veces seguidas
psql:/db/seeds/catalog.sql:21: NOTICE:  truncate cascades to table "catalog_lineage_step"
 fuentes | campos | notas
      12 |    304 |    30
$ psql -Atc "SELECT count(*), min(id), max(id) FROM catalog_lineage_step"
48|1|48                              # mismas filas y mismos identificadores
$ psql -Atc "SELECT count(*), count(DISTINCT source_id) FROM catalog_lineage_step"
48|12
$ psql -Atc "SELECT count(*) FROM catalog_source s WHERE NOT EXISTS (...)"
0                                    # ninguna fuente sin pasos
$ psql -Atc "SELECT count(DISTINCT transformation_code) FROM catalog_lineage_step"
9                                    # los nueve codigos sembrables, usados
```

**3. Lint y tipos (dentro de `backend/`)**

```
$ poetry run ruff check .          -> All checks passed!
$ poetry run ruff format --check . -> 29 files already formatted
$ poetry run mypy app              -> Success: no issues found in 28 source files
```

**4. mypy sobre `backend/app`, `scripts` y `tests`**

```
$ poetry -P backend run mypy --config-file backend/pyproject.toml --exclude '^tests/ml/' backend/app scripts tests
Success: no issues found in 58 source files
```

(Y `ruff check`/`format --check` con `--config backend/pyproject.toml` sobre `backend/app tests
scripts`: `All checks passed!` / `65 files already formatted`.)

**5. Suite**

```
$ poetry -P backend run pytest -c backend/pyproject.toml tests/backend tests/ml -q
451 passed, 17 skipped, 1 warning in 27.66s
Required test coverage of 70% reached. Total coverage: 97.62%

$ KARISMA_TEST_DATABASE_URL=... (misma orden)
468 passed, 3 warnings in 32.33s
```

Antes: **424 passed, 10 skipped**. Ahora **451 passed, 17 skipped** (+27 pruebas; los 7 skips nuevos
son las de integración, que corren en verde con la variable definida). Cobertura de los tres módulos
nuevos: `api/lineage.py` 100 %, `models/lineage.py` 100 %, `services/lineage_service.py` 100 %.

**6. Mapa de permisos regenerado**

```
$ make permisos-ui
Escrito frontend/app/utils/permisos.generated.ts
$ make permisos-ui && diff <copia previa> frontend/app/utils/permisos.generated.ts
OK: regenerar no deja diff (idempotente)
```

Diff frente a `HEAD` (el legítimo de esta US):

```
-  '3.2': [],
+  '3.2': [
+    'GET /api/catalog/{entry_id}/lineage',
+  ],
```

**7. Arranque real sin `ScopeCoverageError`**

```
$ poetry run python <script de arranque>
violaciones: ()
['GET /api/catalog/{entry_id}/lineage']

$ docker compose up -d --build api ; docker compose ps
api   Up 13 seconds (healthy)
db    Up 26 minutes (healthy)
```

Prueba en vivo contra `127.0.0.1:8000`:

```
sin token                    -> 401
GET /api/catalog/1/lineage   -> 200
  pasos: [(1,'origen',True), (2,'extraccion',True), (3,'transformacion',True),
          (4,'calidad',True), (5,'presentacion',False)]
  terminal: field_publish cli_ref
GET /api/catalog/999999/lineage -> 404
  {"detail":{"codigo":"campo_no_encontrado","entry_id":999999}}
```

Log estructurado emitido (sin texto libre, sin datos sensibles):

```
{"entry_id": 1, "fuente": "creditos", "pasos_sembrados": 4, "pasos": 5,
 "event": "linaje_de_campo", "rol": "operativo", "usuario": "lmendez", "level": "info", ...}
```

**8. Secretos**

```
$ bash scripts/verificar_gitleaks.sh
CA-7b en verde: el escaneo detecta lo que debe y no marca lo que no.
```

Sin excepciones nuevas en `.gitleaks.toml`. Ningún hexadecimal de 64 caracteres en este informe.

**No ejecuté `make check` completo**: incluye eslint/vitest de `frontend/`, que la ola B está
editando ahora mismo; su ruido no sería mío. Todas sus partes de backend, db y secretos están arriba
en verde.

---

#### Para el integrador

1. El paso «puente de códigos entre lenguajes» del manual test ya tiene sus dos extremos:
   `LINEAGE_TRANSFORMATION_CODES` (Python, 9) y el `CHECK` de la migración (9) **se comparan en una
   prueba de integración**, no de palabra. Falta cruzar el tercero, `CODIGOS_TRANSFORMACION`
   (TypeScript, 10 = 9 + `field_publish`), que es de la ola B.
2. La fila de bitácora de `docs/security.md` §12 lleva fecha **13-ago-2026**.
3. La base queda sembrada y consistente: 12 fuentes, 304 campos, 30 notas, 48 pasos de linaje.

---

### US-029 · OLA B (interfaz) — informe del agente frontend

Rama `us-029`, SHA base `106c58b`. **Sin commit.** Ningún archivo fuera del write-set de §5.1.

#### (a) Archivos

**Creados**

- `frontend/app/types/linaje.ts` — tipos de §4.4, formas de cable (`CampoCrudo`, `BusquedaCruda`, `PasoCrudo`, `LinajeCrudo`), `CODIGOS_TRANSFORMACION`, `ETAPAS_LINAJE`, `CLAVE_ETAPA`, `CLAVE_TRANSFORMACION`, `CODIGOS_FACETA`, `CLAVE_GRUPO_FACETA`, `claveDeFaceta()`
- `frontend/app/composables/useFocoAtrapado.ts`
- `frontend/app/composables/useBusquedaCatalogo.ts`
- `frontend/app/composables/useLinajeCampo.ts`
- `frontend/app/components/gobierno/PanelLinaje.vue`
- `frontend/app/components/gobierno/RecorridoLinaje.vue`
- `frontend/app/components/gobierno/OverlayLinaje.vue`
- `frontend/app/components/gobierno/TarjetaCampo.vue`
- `frontend/app/components/gobierno/DiccionarioCampos.vue`
- `frontend/app/components/gobierno/AccesoBitacora.vue`
- `frontend/app/components/contexto/PanelContextoTablero.vue`
- `frontend/test/foco.spec.ts` (6) · `panelLinaje.spec.ts` (12) · `diccionarioCampos.spec.ts` (17) · `contextoTablero.spec.ts` (8)

**Modificados** (`git diff --numstat`)

| Archivo | + | − |
|---|---|---|
| `frontend/app/utils/foco.ts` | 81 | 0 (ampliación pura; los cuatro anillos de US-001 intactos) |
| `frontend/i18n/locales/es.json` | 197 | 7 |
| `frontend/i18n/locales/en.json` | 197 | 7 |
| `frontend/app/pages/gobierno.vue` | 2 | 13 |
| `frontend/app/pages/asistente.vue` | 2 | 0 |
| `frontend/test/pantallas.spec.ts` | 7 | 0 |

#### (b) Decisiones y por qué

1. **Tokens que el plan nombra y no existen.** `shadow-flotante` y `ink-strong` **no están** en el `@theme` generado. Se usan `shadow-dialogo` (el token de «Dialogo y panel lateral») y `backdrop:bg-ground-alt/80`. Un token inexistente no falla: Tailwind no emite regla y `contratos.spec.ts` tampoco lo caza, porque su cabeza (`flotante`) no es cabeza declarada y la clase se toma por un default. Un `bg-black/50` literal está prohibido y además no voltea con el modo; `ground-alt` sí (claro `#EAEEF4`, oscuro `#131519`).
2. **Ciclo de foco propio sobre `<dialog>` nativo.** Adoptada la decisión de US-018/019. `useFocoAtrapado` guarda el disparador, llama `showModal()`, mete el foco en el `h2[tabindex="-1"]`, intercepta `Tab`/`Shift+Tab`/`Escape` **en captura sobre `document`** con `preventDefault()`, atiende `cancel`, cierra con clic cuyo `target` es el propio `<dialog>` (oyente en el elemento, no en `document`) y devuelve el foco al disparador o al respaldo. Retira oyentes al cerrar **y** en `onBeforeUnmount`.
3. **`EstadoPendiente us="US-029"` fuera de `/gobierno`.** Dejar «esta pantalla contendrá…» encima de lo que esta misma US entrega es mentir en una pantalla que se captura para A4. Consecuencia: las cuatro claves `screen.governance.capability.*` quedaban huérfanas y se eliminaron (parte del bloque de la pantalla que esta US posee). En `/asistente` el cartel `us="US-023"` **se queda**: ahí la funcionalidad no existe todavía.
4. **`AccesoBitacora.vue` entregado (CA-14)** con `alcanza('admin')`; sin ese alcance el enlace **no está en el DOM** y se muestra `authz.noPermission.requestTo` de US-017 — sin claves nuevas para esa copia.
5. **Cuarto estado no feliz = «sin permiso» del guard.** Igual que `serie/Estado.vue` de US-025: la puerta cerrada la pinta el middleware en lugar de la pantalla, y el diccionario dibuja inicial / cargando / vacío / error. El 401 llama a `usePermisos().expirarSesion()` y navega (probado).
6. **Mapeo snake_case→camelCase una sola vez, en el `transform` de `useFetch`.** `useFetch<T>(...)` genérico no tipa con `default`, y el patrón del repositorio (US-025) es `transform` + `default`. `mapearCampo`, `mapearPagina`, `mapearPaso` y `mapearLinaje` son puras y exportadas.
7. **`watch: false` en los dos `useFetch`.** La consulta es reactiva y la petición explícita: sin eso, teclear dispararía una petición por pulsación contra un mínimo de dos caracteres, y el término vacío saldría como 422.
8. **Estado `cargando` cuando el payload que hay es de otro campo** (`data.fieldId !== campo.fieldId`): evita atribuir el recorrido de un campo al siguiente durante el vuelo.
9. **Revelación progresiva controlada** en el panel del asistente: `<details>` con `:open` y `v-if` en el cuerpo, para que «a dos clics» sea verificable (antes del clic el nodo **no existe**).
10. **DRY sin archivo nuevo**: se importa `codigoDelFallo` de `useSerieTablero` (US-025), `CLAVE_METRICA/AGRUPACION/DENSIDAD` de `utils/etiquetasTablero.ts` y el componente `serie/Origen.vue` para la ficha de procedencia del nivel 2. Cero duplicación de vocabulario.
11. **Iconos**: solo literales ya empaquetados (`database`, `git-branch`, `shield-check`, `book-open`, `search`, `inbox`, `circle-alert`, `circle-check`, `triangle-alert`, `refresh-cw`, `eye`, `info`, `gauge`). El botón de cierre lleva **texto**, no icono.
12. **Animación**: solo `opacity` y `transform` (más `overlay`/`display` para que `allow-discrete` permita la salida), 240 ms, `ease-out`, todo bajo `motion-safe:`. La salida comparte los 240 ms: el inventario de §7.9 solo fija la apertura, y una variante `not-open:` sin prueba que la ejercite sería una clase muerta.

#### (c) Claves i18n nuevas — 134, una por una

**`catalog.facet.*` (41 = 34 del plan + 7 nombres de grupo)**

`catalog.facet.group.{domain, dataType, sensitivity, refreshFrequency, certification, unit, aggregation}` — **7 añadidas sobre el plan**: un chip que solo dice «Cartera» no dice de qué faceta habla, y el lector de pantalla oiría una lista de palabras sueltas. Van en `sr-only` + `title`.

- `catalog.facet.domain.cartera` · `.riesgo` · `.liquidez` · `.mercado` · `.cliente` · `.contable` · `.operacion` · `.regulatorio` (8)
- `catalog.facet.dataType.entero` · `.decimal` · `.texto` · `.fecha` · `.booleano` · `.categoria` (6)
- `catalog.facet.sensitivity.publica` · `.interna` · `.restringida` (3)
- `catalog.facet.refreshFrequency.intradia` · `.diaria` · `.semanal` · `.mensual` (4)
- `catalog.facet.certification.certificado` · `.en_revision` · `.obsoleto` (3)
- `catalog.facet.unit.MXN` · `.USD` · `.porcentaje` · `.dias` · `.conteo` (5)
- `catalog.facet.aggregation.sum` · `.mean` · `.count` · `.max` · `.min` (5)

Redacciones no obvias, por la prueba de US-UX-09: `unit.MXN` = «Pesos mexicanos (MXN)» / «Mexican pesos (MXN)»; `unit.USD` = «Dólares estadounidenses (USD)» / «US dollars (USD)»; `dataType.decimal` = «Número decimal» / «Decimal»; `aggregation.count` = «Conteo» / «Count».

**`lineage.*` (62)**

- `lineage.dictionary.title` · `.hint` · `.searchLabel` · `.searchPlaceholder` · `.searchAction` · **`.searchHint`** (añadida: el mínimo de dos caracteres del endpoint) · `.domainsLabel` · **`.domainAll`** (añadida: el chip «todos») · `.results` (`{count}`)
- `lineage.dictionary.state.initial.title` · `.initial.body` · `.loading` · `.empty.title` · `.empty.body` · `.error.title` · `.error.body` · `.error.retry` — el plan pedía cuatro nodos «con causa y acción»; se abren en título/cuerpo/acción para poder escribirlas
- `lineage.card.physicalName` · `.businessName` · **`.definition`** · `.source` · `.validity` · `.openLineage`
- `lineage.panel.title` (`{field}`) · `.closeLabel` · `.ownerLabel` · `.validityLabel` · **`.sourceLabel`** · **`.journeyLabel`** · `.currentYes` · `.currentNo` · `.derivedNote`
- `lineage.panel.state.loading` · `.error.title` · `.error.body` · `.error.retry` — **añadidas**: el overlay tiene sus propios estados no felices, distintos de los del diccionario
- `lineage.stage.origen` · `.extraccion` · `.transformacion` · `.calidad` · `.presentacion`
- `lineage.transformation.origin_capture` · `.batch_extract` · `.stream_extract` · `.type_normalization` · `.currency_conversion` · `.deduplication` · `.business_rule` · `.reconciliation` · `.quality_rule` · `.field_publish` — **las diez interpolan `{detail}`**
- `lineage.step.ownerOf` · `.effectiveFrom` · `.effectiveTo` · **`.openEnded`** · `.open` · `.technicalDetail` · **`.stored`** · **`.derived`** (las cuatro añadidas: sin fecha de término y las dos insignias guardado/derivado)
- `lineage.crossAccess.title` · `.hint` · `.link`

**`sharedContext.*` (31)**

- `sharedContext.title` · `.hint` · `.static.notice` · `.empty.title` · `.empty.hint` · `.empty.action`
- `sharedContext.chip.metric` · `.grouping` · `.businessUnit` · `.currency` · `.maturityBucket` · `.series` · `.dateRange` · `.window` · `.density` · `.level` · `.visibleSeries`
- **`sharedContext.value.window`** (`{start}`, `{end}`) y **`sharedContext.value.dateRange`** (`{from}`, `{to}`) — añadidas: el valor del chip es una frase, no un número suelto
- `sharedContext.details.lastInteraction` · `.origin` · `.payload` · `.payloadHint` · **`.moment`** · **`.dimension`** · **`.value`** · **`.provenance`** (cuatro añadidas: son los rótulos de la lista de definición de la última interacción)
- `sharedContext.interaction.grafica` · `.tabla` · `.leyenda` · `.control`

**Reescrita**: `screen.governance.description` (presente, es/en). **Eliminadas**: `screen.governance.capability.{dictionary,lineage,owner,quality}` (quedaron huérfanas al retirar `EstadoPendiente` de `/gobierno`).

##### Qué lee exactamente del store `workspace`

Solo lectura, cuatro miembros y una función, ninguno escrito:

- `hayFiltros` — decide entre el estado `sin-contexto` y los chips
- `contextoAgente` — de él salen `filtros.{metrica, agrupacion, unidadNegocio, divisa, bucketVenc, seriesId, rangoFechas}`, `ventana.{inicio,fin}`, `densidad`, `nivel` y `seriesVisibles.length` para los chips
- `ultimaInteraccion` — nivel 2: `origen` (control usado), `dimension`, `valor`, `momento`
- `origen` — nivel 2: se pasa tal cual a `components/serie/Origen.vue`
- `serializarVista()` — nivel 3: el `<pre>` es literalmente su salida
- `RUTA_TABLERO` (constante exportada del mismo módulo) — destino del enlace del estado vacío

Verificación de sentido único: `grep -rnE "aplicarDrillDown|fijarVentana|fijarDensidad|fijarMetrica|alternarSerie|limpiarFiltros" frontend/app/components/contexto` → **0**.

#### (d) Divergencias plan/repositorio

1. **`shadow-flotante` e `ink-strong` no existen** en `design/sistema.py` ni en el `@theme` emitido (§7.5/§2.7 del plan los nombra). Sustitutos reales: **`shadow-dialogo`** y **`backdrop:bg-ground-alt/80`**. **US-UX-07 debe usar estos dos nombres en la guía de estilos del sábado**, no los del plan.
2. **`pages/gobierno.vue` no tenía un `<p>` suelto**: tenía `CabeceraPantalla` + `EstadoPendiente`. El montaje sustituye el `EstadoPendiente`, no se añade «tras el `<p>`».
3. **`pantallas.spec.ts`: una línea condicional, no dos.** `grep -c "stubGlobal('useFetch'"` = 0 → se añadió el doble de §4.9. `grep -c createPinia` = 0 **pero no hacía falta**: `test/configuracion.ts` ya instala Pinia y llama `setActivePinia` en cada `beforeEach`, y VTU concatena `config.global.plugins` con las de `mount`. Añadir un segundo Pinia habría instalado dos instancias en el mismo árbol. Verificado empíricamente: el único fallo era `useFetch is not defined`.
4. **`BusquedaCatalogo` amplía la firma de §4.7** con `dominio`, `dominios`, `codigo`, `filtrarPorDominio` y `reintentar`. Los chips de dominio que §3.4 pide necesitan el `facet_counts.domain` del endpoint y un parámetro `domain`; la firma congelada no los contemplaba. Todos los miembros congelados existen con su tipo.
5. **El paso terminal real no trae el nombre físico en `system_name`.** La API en vivo devuelve `system_name: "Karisma Data"` y `transformation_detail: "<nombre físico>"`. El fixture del spec se realineó con la respuesta real tras verificarla, así que CA-1 («el último nombra el nombre físico») se cumple por la línea de transformación y por el detalle técnico.
6. **`AccesoBitacora.vue` no estaba en la enumeración del encargo** pero sí en `gobierno/**`, en §3.4 y en CA-14. Entregado con el visto bueno del orquestador.

#### (e) Salida real de los gates

```
$ pnpm lint
$ eslint .
(sin hallazgos)

$ pnpm typecheck
$ nuxt typecheck
ℹ Nuxt Icon server bundle mode is set to local
[nuxt:icon] ✔ Nuxt Icon discovered local-installed 1 collections: lucide
ℹ Nuxt Icon client bundle consist of 61 icons with 16.18KB(uncompressed) in size
(sin errores)

$ pnpm vitest run
 Test Files  34 passed (34)
      Tests  655 passed (655)          <- eran 611; +44 de esta US, cero rojas

$ pnpm test  (con cobertura)
Statements   : 86.63% ( 1841/2125 )
Branches     : 80.48% ( 1023/1271 )
Functions    : 87.88% ( 479/545 )
Lines        : 86.51% ( 1726/1995 )
 app/components/contexto |   93.22 |    63.88 |     100 |   92.59
 app/components/gobierno |   96.96 |    84.52 |   91.17 |   96.87

Paridad i18n:
hojas es 686 | hojas en 686
solo en es: []
solo en en: []
identicas es/en: ["brand.name","error.code","guide.lineage.conflict.canonical.system",
                  "guide.lineage.hop.ingesta.system","guide.lineage.hop.publicacion.system",
                  "guide.lineage.hop.transformacion.system"]
(las cuatro de guide.* son de US-UX-09 y son códigos de sistema: idioma.spec las excluye
 por forma; esta US no añadió ni un par idéntico nuevo)
```

**Medición de los 240 ms** — CSS que el servidor de desarrollo entrega de verdad
(`curl http://127.0.0.1:3000/_nuxt/assets/css/main.css`):

```
@media (prefers-reduced-motion: no-preference) {
  .motion-safe\:transition-\[opacity\,transform\,overlay\,display\] {
    transition-property: opacity,transform,overlay,display;
  }
  .motion-safe\:transition-discrete { transition-behavior: allow-discrete; }
  .motion-safe\:duration-240 { --tw-duration: 240ms; transition-duration: 240ms; }
  .motion-safe\:ease-out { transition-timing-function: var(--ease-out); }
}
.backdrop\:bg-ground-alt\/80::backdrop {
  background-color: color-mix(in oklab, var(--color-ground-alt) 80%, transparent);
}
.open\:opacity-100:is([open], :popover-open, :open) { opacity: 100%; }
.max-h-\[80dvh\] { max-height: 80dvh; }
.shadow-dialogo { --tw-shadow: 0 16px 40px -12px ...rgb(10 10 12 / 0.22); }
.sm\:w-\[28rem\] { width: 28rem; }
```

240 ms exactos, solo `opacity`/`transform` (ni width, ni height, ni top, ni left), y toda la
animación dentro de `prefers-reduced-motion: no-preference`. Además queda probado en
`panelLinaje.spec.ts` («anima los 240 ms de la guia y solo opacidad y transformacion»).

**Verificación de que las pruebas no están vacías (mutación, y restaurado después)**

| Mutación | Fallan | Resultado |
|---|---|---|
| `siguienteEnfocable` sin módulo (`lista[indice + paso]`) | 3 | `envuelve en los dos extremos`, `Tab desde el ultimo`, `Shift+Tab desde el primero` |
| `data-derivado` fijo a `"false"` | 1 | `el paso derivado se distingue del sembrado` |
| Quitar el `v-if="payloadAbierto"` del nivel 3 | 1 | `el detalle esta a dos clics` |

#### (f) ¿Existía el endpoint de la ola A?

**Sí, y se verificó contra la API viva en lugar de suponerlo.** Programé primero contra el contrato de §4 (tipos, atributos de datos y claves i18n) sin abrir ni un archivo de `backend/`, `db/`, `scripts/` ni `Makefile`; al llegar a las pruebas comprobé con `curl` sobre `http://127.0.0.1:8000`:

- `GET /api/catalog/1/lineage` sin token → **401** (coherente con `scopes=()` + sesión válida).
- Con token de demostración (`POST /api/auth/demo`), `GET /api/catalog/189/lineage` → **5 pasos**: `origen/extraccion/transformacion/calidad/presentacion`, cuatro con `stored: true` y el terminal con `stored: false`, cada uno con `system_code`, `system_name`, `transformation_code`, `transformation_detail`, `owner{area,steward}`, `effective_from`, `effective_to`, `is_current`.
- `GET /api/catalog/999999/lineage` → **404** con `{"detail":{"codigo":"campo_no_encontrado","entry_id":999999}}` — que es el código que la pantalla pinta en `[data-codigo-error]`.
- `GET /api/catalog/search?q=saldo` → claves `query/tsquery/total/limit/offset/results/facet_counts`, con `source{code,display_name,system_of_record,has_extract}`, `owner{area,steward}`, `validity{valid_from,valid_to,is_current}` y `facets{domain,data_type,...}`.

Las cuatro respuestas coinciden con `CampoCrudo`, `BusquedaCruda`, `PasoCrudo` y `LinajeCrudo` **sin un solo ajuste** en el mapeo; lo único que cambié tras verlas fue el fixture del spec del recorrido, para que mida la forma real y no una inventada.

#### Pendiente para el orquestador

- `docs/manual-test/us-029.md` y el handoff los integra el orquestador (no escribí en `docs/`).
- Sin commit y sin push, según la convención.
- Queda abierto, tal como lo heredó el plan: **Elena Ruiz está sembrada como `operativo` y el módulo 4 exige `admin`**, así que no verá el enlace de la bitácora en la pre-validación del sábado.

## Bitácora de estado

| Fecha | Fase | Estado |
|---|---|---|
| 11-ago-2026 | 2 | Planeación cerrada. Plan de once secciones en `docs/us-planning/us-029.md`. Nueve ambigüedades resueltas; frontera con US-008 declarada con propuesta de tabla propia y cero `ALTER TABLE`; atrapado de foco decidido en fase de captura, sin biblioteca y con diez casos de prueba; contexto tablero-asistente resuelto sobre el store `workspace` de US-025 en modo lectura; comportamiento móvil decidido como hoja inferior a `80dvh` con su razón; raíz i18n de facetas corregida a inglés con la razón anotada; degradación del payload estático declarada con la frase que va al PDF |
| 11-ago-2026 | 2 (revisión) | **Reconciliación con `docs/us-planning/us-018.md`, que ya estaba en el repositorio.** Tres cambios, los tres porque manda el repositorio: (1) se adopta su decisión de que el primitivo de superposición es `<dialog>` nativo —ninguna de las dos US crea componente compartido ni importa a la otra— y el ciclo de foco propio se conserva y se justifica con el hallazgo de `happy-dom` que ellos verificaron; (2) la superposición sale de `components/comun/` y pasa a `components/gobierno/PanelLinaje.vue`, porque un componente compartido con un solo consumidor es generalidad especulativa; (3) **US-029 absorbe el acceso cruzado a la bitácora** que su §2.7 dejó sin dueño, con CA-14 y prueba, y su contingencia del vie 14 queda sin efecto. Se traslada al usuario el único bloqueo que ninguna de las dos puede resolver: el scope de Elena Ruiz frente al módulo 4 |
| 13-ago-2026 | 3 y 4 — programacion, verificacion y QA | `cerrada degradada (estado compartido con payload estatico)`. Dos olas en paralelo con interseccion vacia; el contrato congelado aguanto sin un solo ajuste de mapeo. 451 pruebas de backend y 655 de frontend en verde, `make check` limpio, consola del navegador sin errores. **CA-3 queda con divergencia declarada**: el dialogo conserva el nombre del campo al cerrarse, y vaciarlo haria parpadear el titulo durante la transicion de salida |
