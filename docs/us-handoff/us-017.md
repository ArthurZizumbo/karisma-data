# Handoff US-017 — Guarda de sesión y ocultamiento por rol (alcance S4)

**Estado**: cerrada con salvedades (dos gates rojos por defectos heredados, ninguno de esta US)
**Epic**: E2 (Motor analítico, seguridad y usuarios), con entrega íntegra en `frontend/`
**Actividad**: A4 (dom 16-ago-2026) — apartado 3 de la rúbrica, prototipos, **50 % = 12.50 de 25 puntos**
**Sprint**: S4
**Rama**: `us-017`, encadenada sobre la punta de `us-008`. El usuario fijó el 10-ago-2026 una rama por US con commits locales encadenados y sin PR; el estándar de la raíz (`feature/E2-US-017-guarda-sesion`) queda como discrepancia RU-11, igual que en US-001, US-UX-09, US-015 y US-016
**SHA base**: **`c311390`** (`feat(E1): catalogo semantico que encuentra el dato por su nombre criptico`, punta de `us-008`, 12-ago-2026). Fijado al abrir la Fase 3 con `git rev-parse --short HEAD`. Ancla del diff: ese SHA, nunca `HEAD~N`. La planeación anotó `115136b` como ancestro provisional porque `us-ux-09`, `us-006`, `us-015`, `us-016` y `us-008` seguían sin commitear; las cinco están commiteadas hoy
**Estimación**: 0.5 SP · **Día**: mié 12 · **Estado esperado al cierre**: cerrada
**Última fase**: 3 — programación, 12-ago-2026. Plan completo en [`docs/us-planning/us-017.md`](../us-planning/us-017.md)

## Dominios y sub-tareas tocados

- [ ] backend  - [x] frontend  - [ ] ml  - [ ] agent  - [~] infra  - [ ] db  - [x] docs
- `infra` va parcial y solo por herramienta: `scripts/generar_permisos_ui.py`,
  `scripts/verificar_permisos_ui.sh` y dos objetivos del `Makefile`. **Sin nube, sin Compose, sin
  Terraform.**
- `db`: **ninguna migración.** `db/schema.sql` intacto. Lo que esta US consume —el `CHECK` de roles y
  la columna `disabled` de `app_user`— lo entrega US-015 con la restricción que le impone US-016.
- **Esta US NO se reparte entre agentes paralelos, y es una decisión, no un olvido.** Son 0.5 SP y
  catorce archivos encadenados: generador → archivo generado → tipos → función pura → composable →
  middleware → componente → layout y barra → cadenas → pruebas → smoke → documentos. Cada eslabón lee
  lo que escribió el anterior, y la regla del proyecto manda secuencial en ese caso. Repartirlo
  obligaría a congelar a mano la forma del archivo generado, que es justo la divergencia que el
  mecanismo de §2.3 del plan existe para impedir.
- **Un agente**, perfil `frontend-engineer`, con `portal-frontend-composables`,
  `portal-frontend-components`, `portal-ux-patterns` y `portal-testing` cargadas.

## Frontera con US-027, que se planea en paralelo — declarada por nombre de archivo

Regla dura, la misma del handoff de US-016: **quien escribe va primero, quien lee va después.**
Esta tabla la escribe US-017 sin dar por hecho que US-027 escribirá la suya.

| Archivo | Dueño en esta ola | Regla |
|---|---|---|
| `frontend/app/components/nav/BarraLateral.vue` | **US-017** | Criterio literal de §26: *«los módulos sin permiso no se muestran en la barra lateral»*. US-027 no la abre |
| `frontend/test/BarraLateral.spec.ts` | **US-017** | Se reescribe la aserción de conteo. US-027 no la toca |
| `frontend/app/composables/usePermisos.ts` | **US-017** | Archivo nuevo. US-027 lo **consume** —`alcanza()` es su primitivo para las tres composiciones de `/inicio`— y no lo edita |
| `frontend/app/composables/useSesion.ts` | **US-017**, write-set de **dos líneas** | (a) la docstring que hoy anuncia que US-017 extenderá el composable, cosa que no ocurre; (b) el transporte de `cargarSesion` a `useRequestFetch()` si US-015 no lo dejó así. **Ninguna altera la firma pública de §4.10 de US-015.** US-027 no escribe este archivo en esta ola |
| `frontend/app/utils/sesion.ts` (`destinoPorRol`) | **US-027** | US-017 lo **lee** para el enlace de salida del estado «sin permiso» y **no lo escribe** |
| `frontend/app/pages/inicio.vue` · `frontend/app/components/inicio/**` | **US-027** | US-017 no los abre |
| `frontend/app/pages/index.vue` | **US-017** | Un bloque de aviso sin sesión, para que el evaluador entienda el rebote |
| `frontend/app/layouts/portal.vue` | **US-017** | La bifurcación del `<main>`. US-027 trabaja dentro del `<slot/>`, no alrededor |
| `frontend/app/utils/permisos.generated.ts` | **US-017, y GENERADO** | Nadie lo edita a mano. Se cambia editando `scripts/generar_permisos_ui.py` y corriendo `make permisos-ui` |
| `frontend/i18n/locales/{es,en}.json` | **compartido: el riesgo real** | US-017 añade `authz.*` y `nav.session.*`. Espacios de nombres disjuntos y ediciones aditivas |
| `frontend/test/` | **compartido, archivos disjuntos** | US-017: `guarda.spec.ts`, `permisos.spec.ts`, `estadoSinPermiso.spec.ts`, `BarraLateral.spec.ts`, `smokeRutas.spec.ts`. **Ningún archivo de prueba se comparte** |

**Frontera hacia atrás, con US-015** (implementa antes): `pages/acceso.vue`, `utils/sesion.ts`,
`types/sesion.ts`, `server/api/auth/**` y `server/utils/sesion.ts` son suyos y **US-017 no escribe
ninguno**. Lo que consume de ahí —`SesionUsuario`, `RolUsuario`, `destinoPorRol`, `esRolUsuario` y el
contrato `/acceso?motivo=expirada`— está congelado en §4.9 y §4.10 de `docs/us-planning/us-015.md`.

**Frontera con US-UX-09** (cerrándose hoy): **no se escribe en** `frontend/app/assets/css/main.css`,
`frontend/app/utils/tokens.generated.ts`, `frontend/app/components/guia/**` ni
`frontend/app/pages/guia.vue`. Los dos primeros son generados por `docs/entregables/generar_tokens_a4.py`.

## Las nueve ambigüedades del enunciado, ya resueltas en la planeación

1. **Visibilidad de módulo = mínimo sobre sus ramas; exigencia de rama = máximo sobre sus
   endpoints.** Con el máximo, el módulo de Exploración exigiría `analista` y el perfil operativo
   perdería la consulta puntual, que es su trabajo.
2. **Rama sin endpoints declara scope explícito con motivo escrito**, y el generador **aborta** si
   una rama declara endpoints y scope explícito a la vez. Hoy quedan **tres** literales de rol
   escritos a mano en todo el frontend (`admin` en 4.2, 4.3 y 4.4) y los tres desaparecen solos
   cuando US-018 y US-019 declaren sus endpoints.
3. **«Sin permiso» se pinta en su sitio, sin cambiar la URL.** La dirección sigue compartible, el
   botón «atrás» funciona y la barra lateral queda como salida que no es un reintento.
4. **Layout, no `createError(403)`.** `error.vue` sacaría al usuario del portal para decirle que le
   falta un permiso. `portal.vue` conserva `<FranjaAlcance/>` —que el smoke exige— y la navegación.
5. **El rol sale de `GET /api/auth/me`**, con la firma exacta de §4.7 de US-015, vía
   `useSesion().cargarSesion()`. Cero endpoints nuevos, cero decodificación de JWT en el cliente.
6. **No se revalida en cada navegación.** Solo cuando el estado local está vacío. Cookie y token
   caducan a la vez a los 1800 s; la expiración la detecta la primera llamada de datos que da 401.
7. **Tres rutas públicas**: `/`, `/acceso` y `/guia`, compuestas de constantes ya exportadas. Motivo:
   el índice y la guía de estilos son artefactos que la rúbrica de A4 califica, y poner un formulario
   de acceso delante del entregable es poner una puerta delante de lo que se evalúa.
8. **La barra lateral tiene un estado «sin sesión»** —marca, explicación y enlace a `/acceso`—
   porque `/guia` es pública y usa el layout `portal`. Quinto estado no feliz, gratis.
9. **No se vuelve a la ruta pretendida tras el re-login.** No lo pide ningún criterio y exigiría
   editar `pages/acceso.vue`, que es de US-015. Backlog `05-destino-tras-re-login.md`.

## Hallazgo de la planeación: el mecanismo que impide que la matriz se desincronice

Es la pregunta que justifica planear esta US con detalle. **Tres barreras**, ninguna dependiente de
que alguien se acuerde:

| Barrera | Qué es | Qué defecto atrapa | Dónde corre |
|---|---|---|---|
| **1 · Generación** | `frontend/app/utils/permisos.generated.ts` se emite desde `SCOPE_REGISTRY` **importado** por `scripts/generar_permisos_ui.py`. Ni un scope tecleado en TypeScript salvo los tres explícitos | US-018 cambia `GET /api/users` de `admin` a `directivo` y el frontend sigue ocultando el módulo a un directivo | `make permisos-ui` |
| **2 · Diferencia** | `scripts/verificar_permisos_ui.sh` regenera y ejecuta `git diff --exit-code` | Alguien edita el archivo generado a mano: **el defecto número uno de la auditoría del lote anterior** | `make verificar` |
| **3 · Contraste** | `frontend/test/permisos.spec.ts` lee el bloque `<!-- matriz-permisos -->` de `docs/security.md` y compara endpoint por endpoint; además exige que toda rama y toda ruta de `navegacion.ts` tengan entrada | Una US añade un endpoint y nadie regenera; o US-027 añade una rama al mapa de sitio y nadie le asigna exigencia | `pnpm test` → `make test` → CI |

La barrera 3 es la importante: `docs/security.md` **también** se genera desde `SCOPE_REGISTRY` con
`render_permission_matrix()`, así que comparar el mapa del frontend contra el documento es comparar
dos proyecciones independientes de la misma fuente **sin ejecutar Python durante `pnpm test`**. Eso
la mete en el gate diario en vez del gate del viernes.

**Alternativa rechazada**, para que no se reabra: un endpoint `GET /api/ui/modules` que calcule la
visibilidad en el servidor. Obligaría a añadir una fila a `backend/app/core/permissions.py`, del que
el handoff de US-016 dice literalmente *«Nadie más lo edita»*, y a pasar por `assert_scope_coverage`.
El generador no toca ni un byte del backend.

## Dos preguntas del enunciado, resueltas contra el código instalado y no contra la documentación

**1 · Middleware global e i18n `no_prefix`: no hay orden que negociar.**
`frontend/node_modules/@nuxtjs/i18n/dist/module.mjs:1764` define
`__I18N_ROUTING__ = pages && strategy !== "no_prefix"`, y
`dist/runtime/plugins/route-locale-detect.js` registra el middleware global `locale-changing`
**solo** si `__I18N_ROUTING__`. Con `strategy: 'no_prefix'` el módulo **no registra ninguno**:
`auth.global.ts` será el único middleware global del proyecto, y CA-1 lo fija como criterio
comprobable para el día que alguien añada un segundo. El locale no viaja en la URL —esa es la razón
por la que US-UX-09 eligió `no_prefix`—, viaja en la cookie `karisma_locale` con `path: '/'`, así que
ninguna redirección lo pierde. Con `detectBrowserLanguage: false` no hay detección que interfiera.

**2 · SSR: la trampa concreta.** Durante SSR la cookie del navegador **no viaja sola**.
`$fetch('/api/auth/me')` en servidor abriría una petición anónima, el backend devolvería 401 y **la
guarda rebotaría a `/acceso` a un usuario con sesión válida en cada recarga completa**. La pieza
correcta está en el Nuxt instalado (`nuxt/dist/app/composables/ssr.js`): `useRequestFetch()` devuelve
`useRequestEvent()?.$fetch`, que reenvía las cabeceras entrantes. **Contrato impuesto**:
`grep -n 'useRequestFetch' frontend/app/composables/useSesion.ts` → ≥ 1 línea. Ningún test unitario
del proyecto puede detectar este fallo, así que además va como paso 3 de `docs/manual-test/us-017.md`.

## Zonas sensibles

| Archivo | Por qué |
|---|---|
| `frontend/app/utils/permisos.generated.ts` | **Generado.** Se regenera con `make permisos-ui`. Editarlo a mano es exactamente el defecto que la auditoría del lote anterior contabilizó primero |
| `scripts/generar_permisos_ui.py` | **Única declaración de qué endpoint consume cada rama del mapa de A3.** Cambiar una entrada cambia la barra lateral de los cuatro perfiles. Aborta si una rama declara endpoints y scope explícito a la vez |
| `frontend/app/components/nav/BarraLateral.vue` | **Ya existe y ya usa i18n: se MODIFICA, no se crea.** Su prueba vigente afirma que los cuatro módulos son visibles en toda ruta, y esa afirmación deja de ser cierta por diseño |
| `frontend/test/BarraLateral.spec.ts` | La aserción de conteo se **sustituye** por la tabla rol → módulos visibles, que es más fuerte. No se «ajusta el número para que pase» |
| `scripts/smoke_rutas.sh` | **La guarda lo rompe**: siete de las diez rutas pasarían a 302. Se le añade un tarro de cookies con sesión `admin`. **`RUTAS=(...)` y `TOTAL_ESPERADO` no se tocan**, porque `smokeRutas.spec.ts` los fija y US-UX-09 acaba de subirlos |
| `frontend/test/idioma.spec.ts` | Exige **exactamente dos** mensajes idénticos entre `es` y `en`. Las once cadenas nuevas se revisaron par a par para no añadir una tercera coincidencia |
| `frontend/app/types/navegacion.ts` | Su `RolSugerido` pasa a ser alias de `RolUsuario`. El cambio propaga a `utils/navegacion.ts:159` y a `BotonPrototipo.vue:20`; `pnpm typecheck` es quien lo señala |
| `frontend/app/layouts/portal.vue` | `<FranjaAlcance/>` debe quedar **fuera** de la bifurcación del `<main>`: el smoke la busca con `grep` en todas las rutas, incluida la bloqueada |

## Divergencia heredada, con dueño: `administrador` contra `admin`

US-016 la detectó y **asignó la corrección a US-017 por nombre**. `frontend/app/types/navegacion.ts:19`
declara `RolSugerido` con `'administrador'` bajo una docstring que afirma reflejar los scopes del
JWT; el scope es `admin`. Se corrige aquí en tres líneas —el tipo, el dato de `utils/navegacion.ts` y
la clave del `Record` de `BotonPrototipo.vue`— haciendo que la docstring **sea verdad** en vez de
reescribirla: `RolSugerido` pasa a ser alias de `RolUsuario`, el vocabulario canónico del frontend
que entrega US-015.

**Excepción declarada**: `frontend/app/components/guia/LaminaCampos.vue:148` contiene
`rol: 'administrador'`. Es de US-UX-09, está en vuelo y ese literal es la clave interna de un array
de demostración **sin tipar** contra `RolSugerido`, así que el cambio de tipo no lo rompe. **No se
toca**, queda como excepción nombrada en el comando de CA-10 y como
`docs/us-backlog/06-insignia-de-rol-en-la-guia.md` con dueño US-UX-09.

## Nube y datos

**Ninguna acción.** US-017 no ejecuta nada contra GCP, no toca Terraform ni `docker-compose.yml`.
US-003 se movió al final de la cola el 10-ago y no hay nada desplegado.

Tres efectos indirectos anotados para cuando US-003 corra: la guarda resuelve en SSR con un 302 del
contenedor `web` **sin** despertar el `api`, lo que importa con escalado a cero; las tres rutas
públicas —las primeras que el evaluador abrirá— no provocan arranque en frío del backend; y la marca
`Secure` de `karisma_sesion` (de US-015) funciona tras el HTTPS de Cloud Run pero inutiliza la cookie
en `http://localhost`, diagnóstico que queda escrito por si el smoke falla en local.

**Datos sintéticos**: ninguno. Los siete usuarios sembrados son de US-015 (§4.0 C-3 de su plan).

## Schema

**Ninguna migración propia.** Verificación al cierre: `git diff --name-only <SHA base> -- db/` sin
salida. Si un agente propone una migración dentro de esta US, está fuera de alcance: se rechaza y se
anota aquí.

Lo que esta US **consume** y no duplica: `app_user.role text NOT NULL CHECK (role IN
('operativo','analista','directivo','admin'))`, que es lo que hace imposible que llegue al frontend un
rol que el mapa generado no conozca; y `app_user.disabled`, cuyo 401 `sesion_revocada` (decisión 5 de
US-016) esta guarda trata como sesión ausente y redirige a `/acceso`, **no** como «sin permiso»: sin
sesión no hay rol que comparar.

## Bloqueos y dependencias

- **Bloqueo duro: US-016.** Sin `backend/app/core/permissions.py` ni `docs/security.md` no hay
  generador ni contraste. El paso 1 del orden interno es una puerta explícita:
  `test -f backend/app/core/permissions.py && test -f docs/security.md`.
  **Degradación acordada** si el miércoles a primera hora no está: se implementa sin el mapa generado,
  con `permisos.provisional.ts` escrito a mano y cabecera «PROVISIONAL», estado `cerrada degradada` y
  entrada de backlog con fecha límite. **No se fabrica un `SCOPE_REGISTRY` de juguete** para poner el
  generador en verde: sería una prueba sobre andamiaje, prohibida por la regla NON-NEGOTIABLE.
- **Bloqueo duro: US-015.** `useSesion`, `SesionUsuario`, `RolUsuario`, `destinoPorRol`,
  `esRolUsuario` y la pantalla `/acceso` con su estado `sesion-expirada`. Sin ella no hay sesión que
  guardar.
- **Dependencia blanda: US-UX-09.** Debe estar commiteada antes de tocar `scripts/smoke_rutas.sh` y
  `frontend/test/smokeRutas.spec.ts`, que ella modifica hoy para añadir `/guia`. Verificación previa:
  `grep -n 'TOTAL_ESPERADO' scripts/smoke_rutas.sh`.
- **Esta US bloquea a**: US-027 (que consume `usePermisos().alcanza()` y no debe abrir la barra
  lateral), US-018 y US-019 (cuya pantalla de administración solo es alcanzable por `admin` gracias a
  esta guarda) y a la captura del viernes de los cuatro estados transversales (figura 10 del §12.2 del
  plan de excelencia).
- **Aviso de recaptura**: la barra lateral de `/guia` pasa al estado «sin sesión». Las capturas están
  planificadas para el viernes 14, después de esta US, así que no hay rework; queda anotado para que
  nadie compare contra una captura previa.
- **Riesgo de calendario (R7 del plan)**: la regla de oro puede desviar el miércoles hacia A4. El
  orden interno está diseñado para degradar por el final: los pasos 1-8 entregan los tres criterios de
  §26 y son autosuficientes; los pasos 9-11 —vocabulario, smoke y documentos— se pueden empujar al
  jueves sin dejar la rama a medias.

## Siguiente acción

Ejecutar la Fase 3 con un solo agente `frontend-engineer`, secuencial, en el orden de §5.2 del plan.
**Primer paso, y es una puerta, no un trámite**: comprobar que US-016 aterrizó con
`test -f backend/app/core/permissions.py && test -f docs/security.md`. Si el resultado es falso, se
activa la degradación de R1 **antes** de escribir la primera línea, no a mitad de la jornada.
Segundo paso: `scripts/generar_permisos_ui.py` y `make permisos-ui`, con la puerta de salida de que la
tabla de §1.1 del plan coincida celda por celda con el archivo emitido y de que dos corridas seguidas
dejen el árbol limpio.

---

## Fase 3 — Programación (12-ago-2026)

**SHA base**: `c311390` — `feat(E1): catalogo semantico que encuentra el dato por su nombre
criptico`, punta de `us-008`. Toda comparación de esta fase se ancla ahí:
`git diff c311390 -- <ruta>`.

Un solo agente `frontend-engineer`, secuencial, en el orden interno de §5.2 del plan. El paso 1 —la
puerta `test -f backend/app/core/permissions.py && test -f docs/security.md`— estaba cumplido al
empezar: US-016 aterrizó commiteada en `180e241` con `SCOPE_REGISTRY` de 14 rutas y `ROLE_HIERARCHY`
en `backend/app/core/scopes.py`. **La degradación de R1 no se activó**: el mapa de permisos del
frontend es generado, no provisional.

### (b) Archivos creados o modificados

**Frontera de generación**

| | Archivo | Qué es |
|---|---|---|
| `C` | `scripts/generar_permisos_ui.py` | Importa `SCOPE_REGISTRY` y `ROLE_HIERARCHY` del backend, **lee el mapa de A3 de `frontend/app/utils/navegacion.ts`** y emite el módulo TypeScript. 470 líneas con sus docstrings |
| `C` | `scripts/verificar_permisos_ui.sh` | Regenera, difiere con `git diff --exit-code`, comprueba idempotencia y cabecera. POSIX sh |
| `G` | `frontend/app/utils/permisos.generated.ts` | Salida del generador. `ROLES_EN_ORDEN`, `SCOPE_POR_RAMA` (20 entradas), `SCOPE_POR_RUTA` (7) y `ENDPOINTS_POR_RAMA` (17) |
| `M` | `Makefile` | Objetivo `permisos-ui`, entrada en `.PHONY` y una línea nueva en `verificar` |

**Frontend — lógica**

| | Archivo | Qué es |
|---|---|---|
| `C` | `frontend/app/types/guarda.ts` | `MotivoDeSalida`, `DecisionGuarda` (unión discriminada), `ContextoGuarda`, `BloqueoDeRuta` |
| `C` | `frontend/app/utils/guarda.ts` | `RUTAS_PUBLICAS`, `esRutaPublica`, `rolAlcanza`, `decidirGuarda`. Función pura, cero imports de `#app` |
| `C` | `frontend/app/composables/usePermisos.ts` | Proyección del mapa sobre la sesión: `rol`, `alcanza`, `modulosVisibles`, `asistenteVisible`, `scopeExigidoPor`, `puedeVerRuta`, `bloqueo`, `marcarBloqueo`, `limpiarBloqueo`, `expirada`, `expirarSesion`, `olvidarExpiracion` |
| `C` | `frontend/app/middleware/auth.global.ts` | Único middleware global del proyecto. Primer archivo de `app/middleware/` |

**Frontend — presentación**

| | Archivo | Qué es |
|---|---|---|
| `C` | `frontend/app/components/comun/EstadoSinPermiso.vue` | Cuarto estado no feliz. `data-estado="sin-permiso"`, `role="status"`, `h1` con `tabindex="-1"` enfocado al montar, cero `<button>` |
| `M` | `frontend/app/components/nav/BarraLateral.vue` | Itera `modulosVisibles`; el asistente cuelga de `asistenteVisible`; estado «sin sesión» con enlace a `/acceso`; rótulo del perfil activo; las facetas transversales solo con sesión. **Ni una clase de Tailwind cambiada en lo que ya existía** |
| `M` | `frontend/app/layouts/portal.vue` | El `<main>` bifurca entre `EstadoSinPermiso` y `<slot/>`. `<BarraLateral/>` y `<FranjaAlcance/>` intactos y fuera de la bifurcación |
| `M` | `frontend/app/pages/index.vue` | Aviso de una línea, visible solo sin sesión, con enlace a `/acceso` |

**Frontend — vocabulario y cadenas**

| | Archivo | Qué es |
|---|---|---|
| `M` | `frontend/app/types/navegacion.ts` | `RolSugerido` pasa a ser alias de `RolUsuario`; se añade el `import type` |
| `M` | `frontend/app/utils/navegacion.ts` | Una línea: `rolSugerido: 'administrador'` → `'admin'` |
| `M` | `frontend/app/components/nav/BotonPrototipo.vue` | Una línea: la clave `administrador:` del `Record` pasa a `admin:` |
| `M` | `frontend/app/composables/useSesion.ts` | **Las dos ediciones enumeradas en §3.4 y ninguna más**: la docstring que anunciaba que US-017 extendería el composable, y el transporte de `cargarSesion` a `useRequestFetch()`. La firma pública de §4.10 de US-015 no cambia |
| `M` | `frontend/i18n/locales/es.json` · `en.json` | 14 claves nuevas bajo `authz.*` y `nav.session.*`. Aditivo puro: ninguna clave existente cambia de texto ni de posición |

**Smoke y pruebas**

| | Archivo | Qué es |
|---|---|---|
| `M` | `scripts/smoke_rutas.sh` | Acuña sesiones de `admin` y `operativo`, recorre las diez rutas con la primera y añade los dos casos negativos. **`RUTAS=(...)` y `TOTAL_ESPERADO` intactos** |
| `M` | `frontend/test/smokeRutas.spec.ts` | Cinco aserciones añadidas; las cuatro existentes intactas |
| `C` | `frontend/test/guarda.spec.ts` | 48 pruebas: la tabla exhaustiva de `decidirGuarda` y el pegamento del middleware con globales dobladas |
| `C` | `frontend/test/permisos.spec.ts` | 30 pruebas: contraste contra `docs/security.md`, cobertura de ramas y rutas, orden de roles y etiquetas |
| `C` | `frontend/test/estadoSinPermiso.spec.ts` | 8 pruebas: ausencia de reintento, a quién solicitar, foco y marca |
| `M` | `frontend/test/BarraLateral.spec.ts` | 35 pruebas. La aserción de conteo se sustituye por la tabla rol → módulos visibles |
| `M` | `frontend/test/configuracion.ts` | **Fuera del write-set declarado; ver (e).** Dobles de `useState` y `useRequestFetch` en el setup compartido |

**Documentación**

| | Archivo |
|---|---|
| `C` | `docs/manual-test/us-017.md` |
| `C` | `docs/us-backlog/08-destino-tras-re-login.md` |
| `C` | `docs/us-backlog/07-insignia-de-rol-en-la-guia.md` |
| `M` | `docs/us-backlog/README.md` (dos filas) |
| `M` | `docs/us-handoff/us-017.md` (este archivo) |

**Nada de `backend/`, `db/`, `ml/` ni `infra/`.** `git status --porcelain tests/ backend/` sin salida
al cierre; `db/schema.sql` intacto y sin migraciones.

### (c) Decisiones técnicas y por qué

**1. El generador lee el mapa de A3 del TypeScript en lugar de declararlo otra vez en Python.**
El plan §4.7 sólo fijaba `MAPA_RAMA_ENDPOINTS`, pero para resolver `SCOPE_POR_RUTA` hace falta saber
qué ruta pinta cada rama, y eso vive en `MODULOS`. Declararlo en Python habría creado una tercera
copia del mapa de sitio. `leer_mapa_de_navegacion()` lo extrae con una expresión regular sobre el
bloque `MODULOS` y **aborta** si el bloque no aparece, si no hay módulos, si no hay ramas o si una
rama cuelga de un módulo inexistente. El resultado: el generador cruza dos fuentes —la política del
backend y el mapa del frontend— y no teclea ninguna de las dos.

**2. `rolAlcanza` vive en `utils/guarda.ts` y no duplicado en el composable.** La comparación de
jerarquía la necesitan `decidirGuarda` (middleware) y `alcanza` (barra lateral, tarjetas de US-026 y
US-027). Escrita dos veces habría sido la misma clase de divergencia que esta US existe para cerrar.
Se exporta desde el módulo puro y `usePermisos().alcanza` la envuelve con el rol de la sesión.

**3. El discriminador de servidor es `useRequestEvent()`, no `import.meta.server`.**
`import.meta.server` no es sustituible en vitest sin `@nuxt/test-utils`, que este proyecto no tiene y
que una US de 0.5 SP no va a instalar. `useRequestEvent()` devuelve `undefined` en cliente por
construcción (`nuxt/dist/app/composables/ssr.js`), así que `if (evento !== undefined)` significa
exactamente lo mismo y **sí** se puede doblar. Con eso, M-5 —«en servidor fija 403, en cliente no
llama a la API de servidor»— es una prueba real y no un comentario.

**4. La expiración necesitaba una bandera, y encontrarlo fue el hallazgo de la fase.**
El plan describía `habiaSesion = sesion.value !== null` leído antes de cualquier llamada. Con ese
código la regla 2 de `decidirGuarda` —«sin sesión y había sesión → `?motivo=expirada`»— **es
inalcanzable desde el middleware**: si el estado tiene sesión no se recarga, y si no la tiene,
`habiaSesion` ya es falso. Quien detecta el 401 es una pantalla de datos, y al llamar a
`expirarSesion()` vacía el estado, de modo que en la navegación siguiente un lector expirado y un
visitante nuevo son indistinguibles. La solución es una bandera de un solo uso, `expirada`, que
`expirarSesion()` levanta y que el middleware **consume** al redirigir. Tres consecuencias que
importan:

- `cerrarSesion()` —salir a propósito, US-015— no la levanta, así que salir nunca produce «tu sesión
  expiró».
- Se dice **una vez**: `olvidarExpiracion()` corre en la rama de redirección, y hay prueba de que la
  navegación siguiente ya no lleva el motivo.
- Tras una recarga completa el `useState` es nuevo y el aviso no aparece. Es honesto: el navegador
  genuinamente no puede saberlo, y está escrito en el paso 5 del manual de pruebas.

**5. El estado «sin permiso» se pinta en el layout, con la URL intacta.** `setResponseStatus(evento,
403)` solo cuando hay evento; **no** se llama a `abortNavigation()`, porque la navegación tiene que
completarse para que la dirección siga siendo compartible. El `<slot/>` no se monta, así que ningún
`useFetch` de la pantalla bloqueada llega a dispararse.

**6. La barra lateral sin sesión es un `<nav>`, no un `<section>`.** Empezó como `<section>` y eso
puso en rojo `frontend/test/FranjaAlcance.spec.ts` —US-001—, que exige que el layout `portal`
contenga al menos un `<nav>`. La corrección no fue tocar esa prueba: es que sin sesión el enlace a
`/acceso` **es** la única navegación disponible, así que el elemento correcto era `<nav>` con su
`aria-label`. La prueba ajena detectó un defecto de semántica, que es para lo que sirve.

**7. Las facetas transversales desaparecen sin sesión.** No lo pedía ningún criterio. Sin sesión no
hay ninguna alcanzable, y nueve rótulos de capacidades inalcanzables son exactamente las «puertas que
se cierran» que la ambigüedad 8 del plan resuelve para los módulos.

**8. `scopeExigidoPor` devuelve `null` para una ruta que no está en el mapa.** Es decir: exige sesión
y nada más. No es fallo abierto: el backend gobierna cada endpoint con su `Security(...)`, así que
una ruta que falte aquí cuesta una pantalla que renderiza y falla sus propias llamadas, nunca una
fuga. La barrera contra el olvido es `permisos.spec.ts`, que exige cobertura exacta —sin faltantes y
**sin sobrantes**— del mapa de sitio y del contrato de rutas.

**9. Se cumplió la divergencia deliberada del enunciado frente a `portal-frontend-composables`.**
Cero `definePageMeta({ requiredRole })` y cero jerarquía tecleada en TypeScript:
`grep -rn 'requiredRole' frontend/app` devuelve cero líneas y el orden de roles llega generado desde
`ROLE_HIERARCHY`.

### (d) Claves i18n nuevas, una por una

14 claves, todas aditivas, en dos espacios de nombres disjuntos. **Ninguna coincide entre `es` y
`en`**, de modo que la aserción de `idioma.spec.ts` que fija en exactamente dos los mensajes
idénticos del catálogo (`brand.name` y `error.code`) sigue en verde. Paridad al cierre: **381 hojas
en cada catálogo, sin una sola divergencia** (367 + 14).

| Clave | `es` | `en` |
|---|---|---|
| `authz.noPermission.title` | No tienes permiso para ver esta pantalla | You do not have permission to view this screen |
| `authz.noPermission.body` | Tu perfil no tiene permiso para ver esta información. Solicita el acceso al administrador del portal. | Your profile does not have permission to view this information. Request access from the portal administrator. |
| `authz.noPermission.required` | Esta pantalla requiere el perfil {required}; el tuyo es {current}. | This screen requires the {required} profile; yours is {current}. |
| `authz.noPermission.requestTo` | Solicítalo en Administración › Solicitudes y aprobaciones, que atiende la administración del portal. | Request it under Administration › Requests and approvals, handled by the portal administration. |
| `authz.noPermission.exit` | Ir a mi espacio de trabajo | Go to my workspace |
| `authz.role.operativo` | Operativo | Operations |
| `authz.role.analista` | Analista | Analyst |
| `authz.role.directivo` | Directivo | Executive |
| `authz.role.admin` | Administrador | Administrator |
| `authz.signedOut.notice` | Los prototipos del portal piden sesión. Sin ella te devuelven a la pantalla de acceso; el índice y la guía de estilos quedan siempre abiertos. | The portal prototypes require a session. Without one they send you back to the entry screen; the index and the style guide stay open. |
| `nav.session.anonymous` | Sin sesión iniciada. Los módulos aparecen según tu perfil. | Not signed in. Modules appear according to your profile. |
| `nav.session.signIn` | Entrar al portal | Sign in to the portal |
| `nav.session.ariaLabel` | Estado de la sesión | Session status |
| `nav.session.profile` | Perfil {role} | {role} profile |

Tres notas para quien venga detrás:

- **`authz.noPermission.body` es carácter a carácter la fila `permisos_insuficientes` de
  `docs/security.md` §4**, en los dos idiomas, y `permisos.spec.ts` compara las dos cadenas contra el
  documento. Si US-016 reescribe la copia del 403, esta US se entera el mismo día.
- El plan preveía once claves; son catorce. Las tres extra son `nav.session.ariaLabel` (CA-16 exige
  un rótulo accesible propio para el estado «sin sesión»), `nav.session.profile` (§3.3 pide el rótulo
  del perfil activo, y sin él las cuatro láminas de la barra son indistinguibles entre sí) y
  `authz.signedOut.notice` (el aviso de `pages/index.vue` de §3.3, que necesitaba texto y no podía
  cogerlo de `screen.index.*` sin salirse del espacio de nombres acordado).
- Las cuatro etiquetas de rol **duplican en valor** a `access.demo.roles.*` de US-015, salvo `admin`
  («Administrador» aquí, «Administración» allí). No se unifican: hacerlo obligaría a reescribir
  claves que la especificación de US-015 asevera. `permisos.spec.ts` exige que exista
  `authz.role.<rol>` para cada miembro de `ROLES_EN_ORDEN`, así que un quinto rol no puede llegar sin
  su etiqueta.

### (e) Divergencias entre el plan y el repositorio real

**1. `frontend/test/configuracion.ts` se editó, y no estaba en el write-set.** Es la única salida del
write-set declarado y es necesaria, no cómoda. `pages/index.vue` pasa a leer la sesión, y
`frontend/test/indice.spec.ts` —de US-001, que **no** puedo tocar— lo monta sin doblar `useState`:
con el cambio, ese archivo fallaba con `useState is not defined`. Lo mismo con `useRequestFetch` en
`cargarSesion` y `frontend/test/sesion.spec.ts`, de US-015. El setup compartido existe exactamente
para eso —su propia docstring dice «Nuxt auto-imports these; the specs run outside a Nuxt runtime»—
así que se le añadieron dos dobles: un `useState` con mapa reiniciado en cada prueba (una sesión
filtrada entre archivos haría que el resultado dependiera del orden) y un `useRequestFetch` que lee
`globalThis.$fetch` **en el momento de la llamada**, para que los specs que sustituyen `$fetch` con
`vi.stubGlobal` sigan midiendo lo suyo. Ninguna aserción ajena se tocó.

**2. `pinia` **sí** está instalado.** §2.7 del plan afirma que `frontend/package.json` no declara
`pinia` ni `@pinia/nuxt`; los declara los dos, y `app/stores/sistemaDiseno.ts` existe desde US-UX-09.
La decisión no cambia —esta US no crea un store— pero el motivo escrito en el plan («habría que
instalarlo») ya no es cierto: el motivo real es que la sesión ya vive en `useState` y `guarda.bloqueo`
son dos campos.

**3. Los números de las entradas de backlog se corrieron.** El plan pedía
`05-destino-tras-re-login.md` y `06-insignia-de-rol-en-la-guia.md`; el `05` lo ocupó US-015 con
`05-limitacion-de-intentos-de-acceso.md`, así que son **06** y **07**.

**4. CA-6 y CA-10, tal como el plan escribe sus comandos, no pueden dar cero, y no por un defecto de
esta US.**

- **CA-10**: `grep -rn 'administrador' … | grep -v 'components/guia/'` devuelve **3 líneas**, las
  tres dentro de docstrings que explican por qué esa grafía se rechaza:
  `frontend/app/types/sesion.ts:14` y `frontend/app/utils/sesion.ts:93` (de US-015, que no puedo
  escribir) y el comentario del alias en `frontend/app/types/navegacion.ts:24`. Un comentario no es
  vocabulario de la aplicación. El comando que mide lo que CA-10 quiere decir busca el **literal
  entrecomillado**, y ese sí da cero:
  ```bash
  grep -rn "'administrador'" frontend/app --include=*.ts --include=*.vue | grep -v 'components/guia/'
  ```
- **CA-6**: la lista de exclusiones del plan olvida dos archivos. `frontend/app/utils/navegacion.ts`
  declara `rolSugerido` en las siete entradas de `PROTOTIPOS` —vocabulario del índice de A3, no
  matriz de permisos, y el propio §3.4 del plan manda dejar ahí `'admin'`— y
  `frontend/app/components/guia/LaminaCampos.vue:40` congela los cuatro roles en un arreglo de
  demostración de US-UX-09. Con esas dos exclusiones el comando da cero:
  ```bash
  grep -rnE "'(operativo|analista|directivo|admin)'" frontend/app --include=*.ts --include=*.vue \
    | grep -vE 'utils/permisos\.generated\.ts|types/sesion\.ts|utils/sesion\.ts|utils/navegacion\.ts|components/guia/'
  ```

**5. CA-12 no se puede cumplir con el mecanismo que el plan describe.** Está explicado en la decisión
técnica 4: la regla de expiración es inalcanzable sin una bandera. El criterio se cumple con
`expirada`, y la prueba que lo demuestra es `test/guarda.spec.ts` → «explica la expiracion despues de
que un 401 cerrara la sesion».

**6. `docs/us-backlog/` numeración aparte, el plan preveía 11 claves i18n y son 14.** Detallado en (d).

**7. Dos gates estaban en rojo **antes** de esta rama y siguen en rojo.** Ninguno lo toca esta US y
ninguno está en su write-set; se documentan aquí para que no se atribuyan:

- `frontend/test/proxyApi.spec.ts` → «apunta la plantilla al servicio de Compose que realmente
  existe» espera `NUXT_API_BASE=http://api:8000` y `frontend/.env.example:16` dice
  `http://localhost:8000`. Los tres archivos que esa prueba lee —`.env.example`, `docker-compose.yml`
  y `nuxt.config.ts`— están **intactos** en el árbol de trabajo (`git status --porcelain` sin
  salida), así que el fallo es de `22edbf7` (US-015).
- `make lint` → mypy encuentra **5 errores en `tests/backend/test_catalog_search.py` y
  `tests/backend/test_catalog_integracion.py`**, ambos de US-008 (`c311390`) y ambos sin modificar.
  `make check` muere ahí y no llega a `gitleaks`; el escaneo se corrió por separado y está limpio.

**9. La punta de la rama avanzo durante la fase.** Al abrir la Fase 3, `HEAD` era `c311390`, que es el SHA base que ancla todo este handoff. Mientras se programaba, la auditoria de seguridad de US-016 commiteo `69f205b` y `fd74ba3` sobre la misma rama, y el primero toca `backend/app/core/permissions.py` (anade la clase de violacion `RUTA_AJENA`). El mapa de permisos se regenero **despues** de esos dos commits y sigue sin producir diferencias, y `test/permisos.spec.ts` sigue en verde contra el `docs/security.md` de hoy. Los cambios de US-017 se apoyan por tanto en `fd74ba3`, aunque el ancla del diff declarada sea `c311390`.

**8. El entorno no estaba levantado.** `docker compose ps` mostraba solo `db`; ni `localhost:3000` ni
`localhost:8000` respondían (`%{http_code}` = 000 en las dos, por IPv4 y por IPv6). **CA-11 y CA-14
quedan sin ejecutar**, y `docs/manual-test/us-017.md` lo dice en su cabecera en lugar de fingir una
salida. El procedimiento completo, con los comandos y los valores esperados, está escrito ahí. El
orquestador dijo que había un `pnpm dev` en pie; no lo estaba al llegar esta fase, y no se levantó
otro para no pisar el suyo.

### (f) Salida real de los gates

**`cd frontend && pnpm lint`** — verde.

```text
$ eslint .
```

**`cd frontend && pnpm typecheck`** — verde.

```text
$ nuxt typecheck
ℹ Nuxt Icon server bundle mode is set to local
[nuxt:icon] ✔ Nuxt Icon discovered local-installed 1 collections: lucide
[nuxt] ℹ Compiled router.options.mjs in 1296.86ms
[nuxt] ℹ Compiled nuxt.config.mjs in 1320.36ms
ℹ Nuxt Icon client bundle consist of 51 icons with 13.70KB(uncompressed) in size
```

**`cd frontend && pnpm vitest run`** — 426 de 427, con el único fallo heredado de US-015.

```text
 FAIL  test/proxyApi.spec.ts > el destino es configurable sin reconstruir la imagen > apunta la plantilla al servicio de Compose que realmente existe
AssertionError: expected 'http://localhost:8000' to be 'http://api:8000'

 Test Files  1 failed | 18 passed (19)
      Tests  1 failed | 426 passed (427)
```

La suite pasa de 322 a 427 pruebas: +105. Ninguna de US-001, US-002, US-UX-09 ni US-015 se rompió;
`BarraLateral.spec.ts` se reescribió a propósito y documenta en su cabecera por qué la aserción
anterior dejó de ser cierta.

**Cobertura** (medida excluyendo `proxyApi.spec.ts`, que aborta el reporte; umbral 50 % en las
cuatro métricas):

```text
Statements   : 82.16% ( 645/785 )
Branches     : 80.52% ( 277/344 )
Functions    : 87.76% ( 165/188 )
Lines        : 81.99% ( 601/733 )
```

Archivos de esta US: `utils/guarda.ts` 100 % de líneas, `composables/usePermisos.ts` 100 %,
`middleware/auth.global.ts` 90.9 %, `layouts/portal.vue` 100 %.

**`make permisos-ui && git diff --exit-code frontend/app/utils/permisos.generated.ts`** — verde.

```text
poetry -P backend run python scripts/generar_permisos_ui.py
Escrito frontend/app/utils/permisos.generated.ts
EXIT=0 sin diferencias
```

Salvedad honesta: el archivo todavía está **sin seguimiento** (`?? frontend/app/utils/permisos.generated.ts`),
así que hoy `git diff` no puede ver diferencias en él por construcción. Lo que sí prueba algo hoy es
`sh scripts/verificar_permisos_ui.sh`, que compara la corrida contra la anterior byte a byte. El
`git diff` empieza a tener contenido en cuanto el orquestador commitee.

```text
Verificacion del mapa de permisos de la interfaz (US-017)

1. El archivo generado esta al dia
  [OK   ] sin diferencias            frontend/app/utils/permisos.generated.ts coincide con el registro de hoy

2. Idempotencia del generador
  [OK   ] dos corridas seguidas      el archivo queda byte a byte igual

3. El archivo se declara generado
  [OK   ] cabecera                   declara que nadie lo edita a mano

Mapa de permisos en verde: una sola fuente y una sola direccion de derivacion.
```

**Paridad i18n** — verde.

```text
381 381
SOLO ES []
SOLO EN []
```

**`make check`** — **rojo por los 5 errores heredados de US-008**, detallados en (e).

```text
poetry -P backend run ruff check --config backend/pyproject.toml backend ml scripts tests
All checks passed!
poetry -P backend run ruff format --check --config backend/pyproject.toml backend ml scripts tests
65 files already formatted
poetry -P backend run mypy --config-file backend/pyproject.toml --exclude '^tests/ml/' backend/app scripts tests
tests\backend\test_catalog_search.py:218: error: "Callable[...]" has no attribute "dependency_overrides"  [attr-defined]
tests\backend\test_catalog_integracion.py:177: error: Statement is unreachable  [unreachable]
tests\backend\test_catalog_integracion.py:238: error: Argument 4 to "search" has incompatible type "**dict[str, object]"; expected "Sequence[str] | None"  [arg-type]
tests\backend\test_catalog_integracion.py:238: error: Argument 4 to "search" has incompatible type "**dict[str, object]"; expected "bool"  [arg-type]
tests\backend\test_catalog_integracion.py:238: error: Argument 4 to "search" has incompatible type "**dict[str, object]"; expected "int"  [arg-type]
Found 5 errors in 2 files (checked 44 source files)
make: *** [Makefile:71: lint] Error 1
```

`ruff check` y `ruff format --check` incluyen `scripts/`, así que el generador nuevo pasa las dos.
Como `make check` muere en `lint`, la mitad de secretos se corrió aparte y está limpia:

```text
2:33AM INF scanned ~42823171 bytes (42.82 MB) in 625ms
2:33AM INF no leaks found

CA-7b en verde: el escaneo detecta lo que debe y no marca lo que no.
```

**Criterios verificables por `grep`**

| Criterio | Comando | Resultado |
|---|---|---|
| CA-1 | `ls frontend/app/middleware/*.global.* \| wc -l` | **1** |
| CA-4 | `grep -c '<button' frontend/app/components/comun/EstadoSinPermiso.vue` | **0** |
| CA-6 | comando corregido de (e) | **0 líneas** |
| CA-10 | comando corregido de (e) | **0 líneas** |
| SSR | `grep -n 'useRequestFetch' frontend/app/composables/useSesion.ts` | **2 líneas** (123 y 129) |

**Sin ejecutar, y dicho en voz alta**: CA-11 y CA-14, los dos marcados `[stack]`, porque el entorno no
estaba en pie. Ver (e), punto 8.

### Qué queda abierto para quien siga

1. **Ejecutar `docs/manual-test/us-017.md` con `make dev` levantado.** Los cinco bloques, y en
   especial el 2: la trampa de SSR es el único defecto de esta US que ninguna prueba unitaria puede
   ver.
2. **Contingencia del 403 en SSR**, decidida en planeación y todavía sin observar: si el 403
   impidiera la hidratación, se baja a 200 conservando el estado renderizado y se anota aquí.
3. **`frontend/.env.example` y los 5 errores de mypy de US-008** siguen rojos y tienen dueño ajeno a
   esta US.
4. **US-027 puede empezar**: `usePermisos().alcanza()` es su primitivo y está publicado. No abra
   `BarraLateral.vue`, `usePermisos.ts` ni `useSesion.ts` en esta ola.

## Fase 4 — Verificacion en navegador real (12-ago-2026)

El agente dejo CA-11 y CA-14 sin ejecutar porque no habia servidores en pie, y **hizo bien en no
fingir la salida**. El orquestador levanto el entorno y los ejecuto con Playwright sobre Chrome.

| Criterio | Evidencia |
|---|---|
| La guarda decide en el servidor | Visita anonima a `/administracion` -> redirige a `/acceso` antes de pintar nada |
| Ocultamiento por rol | Con sesion `operativo`, la barra lateral trae Inicio, Exploracion y Gobierno. **Administracion no aparece** |
| Estado sin permiso | `/administracion` con sesion `operativo` responde **403 real**, no un 200 con pantalla de disculpa. `role="status"`, foco puesto, y dice que perfil hace falta contra cual se tiene |
| Sin boton de reintento | Confirmado: solo «Ir a mi espacio de trabajo». Reintentar no cambia el rol |
| Bilingue | El cambio a ingles traduce la pantalla entera, incluidos los nombres de rol interpolados |
| Hidratacion | **0 errores y 0 avisos** de consola en `/acceso`, `/inicio` y `/administracion` |

**Un aviso de desarrollo queda, y no es un defecto**: `NUXT_E4011` avisa de que hay paginas sin
`<NuxtPage />`. Lo dispara la bifurcacion del `<main>` de `layouts/portal.vue`, que en el estado sin
permiso no renderiza el `<slot/>`. El propio texto del aviso declara ese caso como falso positivo.
No aparece en produccion.

### Lo que el orquestador corrigio al integrar

1. **`make check` estaba en rojo por US-008**, no por esta US: cinco errores de mypy en
   `tests/backend/test_catalog_*.py`. Corregidos —el `**extra: object` del ayudante de busqueda pasa
   a parametros explicitos, y el `sys.platform` se sustituye por buscar la clase de politica por
   nombre, porque mypy estrecha la comparacion y declara inalcanzable la otra rama—. `make check`
   verde.
2. **`proxyApi.spec.ts` estaba en rojo desde US-015**, y el defecto era de la prueba: exigia
   `NUXT_API_BASE=http://api:8000` en `frontend/.env.example`, que es la plantilla **del host**.
   `docker-compose.yml` fija ese valor en su bloque `environment`, que gana sobre `env_file`, y el
   comentario del propio archivo que la prueba leia lo explicaba. Con el valor que la prueba exigia,
   `pnpm dev` en el host pinta la pantalla y muere en cada llamada. La asercion se movio a
   `docker-compose.yml` y se anadio su complementaria sobre la plantilla.
3. **Colision de numeracion en el backlog**: la entrada `06` de esta US coincidia con la `06` que
   dejo la auditoria de seguridad. La de re-login pasa a **`08`**, con su fila del README y la
   referencia del handoff corregidas.


## Fase 4 — Auditoria de seguridad (13-ago-2026)

Alcance `c311390..106c58b`. **Sin bloqueantes ni mayores.** Dos apuntes sobre esta US:

**La deuda 2 estaba cerrada y el documento no se habia enterado.** `docs/security.md` la declaraba
abierta con dueno US-017 —`types/navegacion.ts` diciendo `'administrador'` donde el scope es `admin`—
y esta US la cerro: hoy es `export type RolSugerido = RolUsuario` y no queda un solo literal
`'administrador'` en el codigo. Ya figura como cerrada. Es el reflejo exacto del hallazgo del ciclo
anterior: alli una deuda se declaraba cerrada por una US que no la cerraba; aqui una US la cierra y el
documento no se entera.

**El detector de divergencia del mapa de permisos no estaba en el gate obligatorio.**
`scripts/verificar_permisos_ui.sh` es lo unico que compara `permisos.generated.ts` contra el registro
del backend, y vivia en `make verificar`, no en `make check`. Ya esta en `check`.

La segunda barrera tenia ademas un punto ciego: `frontend/test/permisos.spec.ts` filtraba las ramas sin
endpoint, que son siete, dejandolas fuera de la unica prueba que compara contra fuente externa. Editar
a mano tres lineas del archivo generado era internamente consistente, pasaba la suite entera y hacia
aparecer el modulo Administracion **para los cuatro roles**. Cubierto.

**Confirmado y sin cambios**: la guarda del frontend no es un control de seguridad y nadie la trata
como si lo fuera —la autorizacion vive en el backend, comprobado por los dos caminos—, y ninguna
pantalla expone dato que su rol no pueda pedir a la API.

## Bitácora de estado

| Fecha | Fase | Estado |
|---|---|---|
| 11-ago-2026 | 2 — planeación | `planning`. Plan en `docs/us-planning/us-017.md`. Nueve ambigüedades del enunciado resueltas; frontera con US-027 declarada archivo por archivo y sin dar por hecho que la escribirá el otro plan; mecanismo de tres barreras contra la duplicación de la matriz de permisos, con el archivo del frontend **generado** desde `SCOPE_REGISTRY` y contrastado en vitest contra `docs/security.md`; convivencia con i18n `no_prefix` verificada en `dist/module.mjs:1764` (no hay middleware de i18n que ordenar); trampa de SSR identificada y cerrada con `useRequestFetch()`; rotura del smoke de US-001 identificada en planeación y no en el gate; divergencia `administrador`/`admin` heredada de US-016 aceptada y acotada a tres líneas |
| 12-ago-2026 | 3 — programación | `cerrada con salvedades`. SHA base `c311390`. Mapa de permisos **generado** desde `SCOPE_REGISTRY` y desde el mapa de A3, con verificador de diferencia e idempotencia y contraste en vitest contra `docs/security.md`; guarda partida en función pura y middleware, con 105 pruebas nuevas (322 → 427); hallazgo de la fase: la regla de expiración del plan era **inalcanzable** desde el middleware y se cerró con una bandera de un solo uso que `expirarSesion()` levanta y la redirección consume; `administrador` erradicado del vocabulario de la aplicación; 14 claves i18n nuevas con paridad 381/381. Rojos heredados y no atribuibles: `proxyApi.spec.ts` (US-015) y 5 errores de mypy en `tests/backend/` (US-008). CA-11 y CA-14 sin ejecutar: el entorno no estaba levantado |
| 12-ago-2026 | 4 — verificacion | CA-11 y CA-14 ejecutados en navegador real: redireccion anonima, ocultamiento por rol, 403 con estado sin permiso y sin reintento, y bilingue. Consola sin errores ni avisos de hidratacion. Corregidos de paso dos rojos heredados: mypy de US-008 y la asercion invertida de `proxyApi.spec.ts` de US-015 |
| 13-ago-2026 | 4 — auditoria de seguridad | Sin bloqueantes ni mayores. Los hallazgos que tocan a esta US quedan corregidos y verificados sobre los archivos del diff |
