# Handoff US-016 — Autorización RBAC por scopes (alcance S4)

**Estado**: planning
**Epic**: E2
**Actividad**: A4 (dom 16-ago-2026) — aporte indirecto al apartado 3 (prototipos, 50 %); cierre del gate vencido del 6-ago
**Sprint**: S4
**Rama**: `us-016`, creada desde `115136b` (punta de `us-002`). El usuario fijó el 10-ago-2026 una rama por US con commits locales encadenados y sin PR; el estándar de la raíz (`feature/E2-US-016-rbac-scopes`) queda como discrepancia RU-11, igual que en US-001 y US-UX-09
**SHA base**: `115136b` — `git rev-parse --short HEAD` el 10-ago-2026. Ancla del diff: `git diff --name-only 115136b`, nunca `HEAD~N`
**Estimación**: 1 SP · **Día**: mar 11 · **Estado esperado al cierre**: cerrada
**Última fase**: 2 — planeación, 10-ago-2026. Plan completo en [`docs/us-planning/us-016.md`](../us-planning/us-016.md)

## Dominios y sub-tareas tocados

- [x] backend  - [ ] frontend  - [ ] ml  - [ ] agent  - [ ] infra  - [ ] db  - [x] docs
- **frontend, ml, agent e infra quedan fuera por completo.** Ni un archivo de `frontend/`, ni `Makefile`, ni `scripts/`, ni Compose, ni nube.
- **db: ninguna migración.** Lo que esta US aporta al esquema es una **restricción impuesta a la migración de US-015** (`CHECK (role IN (...))`, §8 del plan), no un archivo propio.
- **Esta US NO se reparte entre agentes paralelos, y es una decisión, no un olvido.** Son 1 SP y nueve archivos encadenados: vocabulario → política → pruebas → documento. Cada eslabón lee lo que el anterior escribió, y la regla del proyecto manda secuencial en ese caso. Repartirlo obligaría a congelar a mano la tabla que el renderizador debe emitir, que es justo la divergencia que CA-8 existe para impedir.
- **Un agente**, perfil `backend-engineer`, con `portal-auth-jwt` y `portal-security-audit` cargadas. Orden interno: `scopes.py` → `permissions.py` → pruebas de la ventana A → `docs/security.md` → (merge de `us-015`) → `main.py` → matriz de la ventana B → `docs/manual-test/us-016.md`.

### Las dos ventanas del martes

| Ventana | Cuándo | Write-set | ¿Corre sin US-015? |
|---|---|---|---|
| **A** | mar 11, primera hora. **No espera a nadie** | `backend/app/core/{scopes,permissions}.py` · `docs/security.md` · `tests/backend/permisos/{conftest,test_scopes,test_scope_coverage,test_security_doc}.py` | **Sí, entera.** Ninguna línea necesita PyJWT, PostgreSQL ni `core/auth.py` |
| **B** | mar 11, tras encadenar `us-015` (≈ 45 min) | `tests/backend/permisos/test_permission_matrix.py` · `backend/app/main.py` (3 líneas) · `docs/manual-test/us-016.md` | No |

Si US-015 resbala al miércoles, se cierra la ventana A y el estado pasa a `cerrada parcial`. **No se fabrica un `get_current_user` de juguete** para poner la matriz en verde: sería una prueba sobre andamiaje, prohibida por la regla NON-NEGOTIABLE del proyecto.

## Frontera con US-015, que se planea en paralelo

Regla dura: **durante la ventana A, US-016 no abre ningún archivo que US-015 escriba, y al revés.**

| Archivo | Dueño | Regla |
|---|---|---|
| `backend/app/core/scopes.py` · `core/permissions.py` | **US-016** | US-015 importa `Scope`, `enforce_scopes`, `unauthorized` y `forbidden`. No los edita |
| `docs/security.md` · `tests/backend/permisos/**` | **US-016** | Carpeta de pruebas propia: el `conftest.py` del padre se hereda sin tocarlo |
| `backend/app/core/auth.py` | **US-015** | US-016 **no lo escribe en la ventana A**. En la B corrige solo las líneas del contrato de §4.5 que la matriz señale incumplidas |
| `backend/app/api/auth.py` · `models/user.py` · `services/user_service.py` | **US-015** | Fuera de alcance |
| `backend/pyproject.toml` · `poetry.lock` | **US-015** | `pyjwt` y `pwdlib` los añade US-015; US-016 no toca el candado |
| `tests/backend/conftest.py` · `tests/backend/test_auth.py` | **US-015** | — |
| `db/migrations/*create_app_user*` | **US-015** | Con el `CHECK` de roles que le impone US-016 |
| `frontend/**` | **US-015** (acceso, proxy) y **US-017** (guarda, i18n) | — |
| `backend/app/main.py` | **US-015** en la ventana A · **US-016** en la B | Único archivo compartido, y por eso secuencial |
| `Makefile` | **nadie en US-016** | US-UX-09 le añade `tokens` esta misma semana |

**Contrato congelado que se exige a `core/auth.py`** (detalle en §4.5 del plan): `Scope` importado, no redefinido · reclamaciones `sub`/`scope`/`exp`/`iat` · `algorithms=["HS256"]` explícito con `require` de las tres · `auto_error=False` en el esquema OAuth2 · los cinco códigos de error · `enforce_scopes` en vez de reimplementar la comparación · **carga del usuario por dependencia inyectable**, sin la cual la matriz no puede correr sin PostgreSQL.

**Alternativa rechazada, para que no se reabra**: que US-016 fuese dueña de `core/auth.py` con `get_current_user` completo. Se descarta porque esa función necesita el modelo y el servicio de usuario, que son de US-015: el reparto inverso crea un ciclo entre las dos US y bloquea a las dos.

## Zonas sensibles

| Archivo | Por qué |
|---|---|
| `backend/app/core/scopes.py` | **Vocabulario canónico del portal.** Cambiar un valor o el orden de `ROLE_HIERARCHY` cambia el significado de la matriz entera y de la barra lateral de US-017. No importa nada de `app.api`, `app.models` ni `app.core.config`: esa pureza es lo que permite que la matriz corra sin base de datos |
| `backend/app/core/permissions.py` | **Fuente única de la política.** De aquí salen la guardia, la parametrización de las pruebas y la tabla del documento. Editar la tabla del `.md` sin tocar el registro pone la suite en rojo, y así debe ser |
| `backend/app/main.py` | Compartido con US-015. Solo tres líneas de US-016, y solo en la ventana B |
| `docs/security.md` (bloque entre marcadores) | **Generado.** Se regenera con `(cd backend && poetry run python -c "from app.core.permissions import render_permission_matrix; print(render_permission_matrix())")`. Editarlo a mano es exactamente cómo se perdió el gate del 6-ago |
| `backend/app/api/health.py` | **No se toca.** Su prueba de US-001 exige que siga anónimo y sin `WWW-Authenticate`. Queda fuera de la guardia por construcción: no cuelga de `/api` |

## Las siete ambigüedades del enunciado, ya resueltas en la planeación

1. **Catálogo (`scopes=[]`) y consulta puntual (`scopes=["operativo"]`) no son la misma regla**, aunque hoy den el mismo conjunto: no existe rol por debajo de `operativo`. La diferencia queda en la regla, no en el resultado.
2. **Jerarquía ordinal total** `operativo < analista < directivo < admin`, que es lo único que hace verdaderos los cuatro «+» del enunciado. Consecuencia escrita: `admin` lee los resúmenes directivos —separación de funciones imperfecta, deuda aceptada— y el modelo no puede expresar «solo directivo».
3. **`scope` es cadena delimitada por espacios con exactamente un rol hoy**, no una cadena única cerrada. Cuesta cero y no cierra la puerta a varios roles.
4. **«Endpoint de datos» = toda `APIRoute` bajo `/api`**, definición mecánica. Lista blanca de un solo elemento: `POST /api/auth/token`.
5. **Usuario desactivado → 401 `sesion_revocada`**, no 403: el 403 dejaría al frontend con una sesión que cree viva y sin salida; el 401 dispara el re-login limpio que US-015 y US-017 ya exigen.
6. **La «herencia al agente» del título de §14 no es criterio de esta US** en §26. Se documenta la regla y se reserva la fila `POST /api/chat`; la implementan US-020 y US-021.
7. **`docs/security.md` se escribe esta semana.** `plan_excelencia.md` §17 lo arrastra a A5 y §26 lo pone como criterio del martes 11: **§26 manda durante S4**. Lo que se arrastra a A5 es su verificación contra endpoints reales, que hoy no existen.

## Hallazgo de la planeación: el mecanismo que impide olvidar el `Security(...)`

Es la pregunta que justifica planear esta US con cero endpoints en el repositorio. La respuesta son **tres capas**, y las tres se entregan el martes:

| Capa | Qué es | Qué la hace confiable |
|---|---|---|
| **1 · Registro** | `SCOPE_REGISTRY` mapea `(método, plantilla)` a la regla, su US dueña y su estado | Obliga a escribir la razón, que es lo que acaba impreso en el documento |
| **2 · Guardia en pytest** | `audit_scope_coverage(app)` cruza `app.routes` con el esquema OpenAPI y devuelve violaciones de cuatro clases: `sin_scopes`, `fuera_del_registro`, `scope_divergente`, `ruta_oculta` | Se prueba **contra aplicaciones sintéticas** construidas en el propio test (G-1 a G-4). Probada solo contra la aplicación real —que hoy no tiene rutas `/api`— sería una prueba que no puede fallar, y eso es deuda |
| **3 · Arranque** | `create_app()` termina con `assert_scope_coverage(application)`: con violaciones, la aplicación **no arranca** | Una prueba se esquiva con `-k` o con un `skip`; el arranque no. En Cloud Run, la revisión no se promueve y el tráfico se queda en la anterior: falla cerrado |

**Lee el esquema OpenAPI, no `get_flat_dependant`**: el primero es API pública y estable; el segundo es interno y cambia entre versiones menores. El punto ciego conocido —`include_in_schema=False`— se cubre cruzando dos inventarios, y `ruta_oculta` es su nombre.

**Y la matriz crece sola.** Los casos se generan de `SCOPE_REGISTRY` × los cuatro roles, y la aserción es asimétrica: rol sin permiso → **exactamente 403**; rol con permiso → **ni 401 ni 403** (un 422 o un 404 son legítimos y no dicen nada de autorización). Eso permite interrogar endpoints futuros sin conocer su contrato ni fabricar cuerpos válidos, porque la dependencia de seguridad lanza antes de que se valide el cuerpo. La prueba M-9 exige que toda ruta viva bajo `/api` esté cubierta por la parametrización: el día que US-008 monte su router, o entra en la matriz o la suite se pone roja.

## Divergencia detectada en el repositorio, con dueño asignado

`frontend/app/types/navegacion.ts:14` declara `RolSugerido` con `'administrador'` bajo una docstring que afirma reflejar los scopes del JWT. **El scope es `admin`.** Hoy es inocuo —solo rotula el índice de prototipos— y mañana es `if (rol === 'administrador')` contra un token que dice `admin`. US-016 **no toca el frontend**: fija el vocabulario canónico en `core/scopes.py`, lo publica en `docs/security.md` §2 y deja la corrección escrita con dueño, **US-017**, que es quien edita ese archivo.

## Contrato para la interfaz bilingüe

El backend devuelve **códigos**, nunca prosa: con la interfaz en dos locales desde la decisión del 10-ago, una frase en el cuerpo de la respuesta existiría en un solo idioma. Los cinco códigos —`credenciales_ausentes`, `credenciales_invalidas`, `sesion_expirada`, `sesion_revocada`, `permisos_insuficientes`— van a `docs/security.md` §4 con su copia en **español y en inglés**, bajo claves `errores.autorizacion.*`. La fila de `permisos_insuficientes` es literalmente el texto del estado «sin permiso» que US-017 debe mostrar **sin botón de reintento**.

## Nube y datos

**Ninguna acción.** US-016 no ejecuta nada contra GCP; US-003 se movió al final de la cola el 10-ago y no hay nada desplegado. Tres efectos indirectos anotados: la comprobación de arranque convierte un despliegue mal gobernado en una revisión que no se promueve; `JWT_SECRET_KEY` debe venir de Secret Manager cuando US-003 corra (`gcloud secrets create karisma-jwt-secret --data-file=-` con `openssl rand -hex 32`); y el JWT sin estado es lo que hace viable el `scale-to-zero`, consecuencia útil de haber descartado los refresh tokens.

**Datos sintéticos**: ninguno. Los siete usuarios sembrados son de US-015.

## Schema

**Ninguna migración propia.** `db/schema.sql` intacto. Si un agente propone una migración dentro de esta US, está fuera de alcance: se rechaza y se anota aquí.

Lo que sí impone: la tabla `app_user` de US-015 lleva `role text NOT NULL CHECK (role IN ('operativo','analista','directivo','admin'))` y `disabled boolean NOT NULL DEFAULT false`. Sin el `CHECK`, un seed con errata mete `'administrador'` en la columna, el token sale con un scope que el código no reconoce, y el usuario queda encerrado fuera de todo con un diagnóstico lento. El `down` va **sin `CASCADE`**, como fijó US-002.

## Bloqueos y dependencias

- **La ventana A no tiene bloqueos.** Arranca de inmediato: no necesita PyJWT, ni base de datos, ni `core/auth.py`.
- **La ventana B depende de US-015** (mismo día, 1.5 SP, mar 11): `core/auth.py`, `POST /api/auth/token` y las dependencias `pyjwt`/`pwdlib`.
- **Esta US bloquea a**: US-017 (estado «sin permiso» y visibilidad de módulos por rol), US-008, US-025, US-009, US-018/019 y US-027 —todas montan endpoints que la guardia va a exigir declarados—, y a US-020/021 para la herencia del Bearer.
- **Gate que cierra**: el del **6-ago** (§20 del plan), vencido y sin cumplir desde hace cinco días. Es el único gate del calendario que esta US toca.
- **Riesgo de calendario declarado (R7 del plan)**: la regla de oro puede desviar el martes hacia A4. Por eso `docs/security.md` se escribe **en la ventana A y antes que la matriz**: es lo que está vencido y lo que menos depende de terceros. Si el martes se cae entero, el archivo ya existe.

## Siguiente acción

Ejecutar la **ventana A** con `backend-engineer`: escribir `core/scopes.py` en los primeros 45 minutos —antes que nada, para que US-015 lo importe en vez de inventarse una segunda enumeración de roles (R2)—, después `core/permissions.py`, las tres pruebas de la ventana y `docs/security.md`. Gate de salida de la ventana: `poetry -P backend run pytest -c backend/pyproject.toml tests/backend/permisos -q --no-cov` en verde y `grep -c '^## ' docs/security.md` → 12.

## Bitácora de estado

| Fecha | Fase | Estado |
|---|---|---|
| 10-ago-2026 | 2 — planeación | `planning`. Plan en `docs/us-planning/us-016.md`. Siete ambigüedades del enunciado resueltas; frontera con US-015 declarada archivo por archivo y en dos ventanas; mecanismo de tres capas contra el `Security(...)` olvidado, con la guardia probada sobre apps sintéticas para que no sea una prueba que no puede fallar; matriz 401/403 diseñada para crecer sola desde el registro; divergencia `admin`/`administrador` detectada y asignada a US-017 |
