# Handoff US-006 — Silos sintéticos mínimos creíbles (alcance S4)

**Estado**: implementado
**Epic**: E1 (Ingesta sintética, catálogo, exportación, EDA · US-006…010)
**Actividad**: A4 (dom 16-ago-2026) — insumo del apartado de **prototipos, 50 % = 12.5 de 25 puntos**
**Sprint**: S4
**Rama**: `us-006`, encadenada sobre `us-ux-09` (que sale de `us-002`, que sale de `us-001`).
Discrepancia conocida con el estándar de la raíz (`feature/E1-US-006-silos-sinteticos`): es la
convención que el usuario fijó el 10-ago-2026, una rama por US con commits locales encadenados y
sin PR. Ver RU-11 del plan
**SHA base**: `6cab2e5` — `git rev-parse HEAD` al arrancar la Fase 3 (11-ago-2026, punta de
`us-ux-09`, de donde nace `us-006` por la regla de §5.3 del plan). Ancla del diff:
`git diff --name-only 6cab2e5`, nunca `HEAD~N`. El `115136b` de la planeación era la punta de
`us-002` y quedó obsoleto al commitear US-UX-09; las verificaciones de §8 y §11 del plan que citan
`115136b` se leen contra `6cab2e5`
**Estimación**: 1.5 SP · **Día**: mar 11 · **Estado esperado al cierre**: cerrada degradada
(volúmenes recortados, declarados como tales)
**Última fase**: 3 — programación, 11-ago-2026 (fases 0 a 5 del plan, un solo agente
secuencial). Planeación: **corregida el 11-ago-2026** por la auditoría cruzada
de los cuatro planes de Fase 2. Plan completo en
[`docs/us-planning/us-006.md`](../us-planning/us-006.md), con las cinco correcciones detalladas en
su sección final «Correcciones de la auditoría cruzada del 11-ago-2026»
**Fuente normativa**: §26 del plan (líneas 2258-2287, manda durante S4) y
[`docs/semana_4/plan_excelencia.md`](../semana_4/plan_excelencia.md) §10. El bloque de US-006 de
§11 (línea 993, catálogo de julio, `creditos` 1-5 M) **no es el vigente**

## Dominios y sub-tareas tocados

- [ ] backend · [ ] frontend · [x] **ml** · [ ] agent · [~] **infra** · [ ] db · [x] **docs**
- **backend**: cero archivos de `backend/app/`. Solo dos archivos de configuración del proyecto
  Python (`pyproject.toml`, `poetry.lock`). El endpoint que consume el agregado es de US-025.
- **frontend**: **cero archivos**. El umbral de cobertura del 50 % queda intacto por construcción.
- **db**: **ninguna migración**. `db/schema.sql` no cambia ni un byte.
- **infra solo de rebote**: `Makefile`, `docker-compose.yml`, `.gitignore` y un `scripts/verificar_*`.
  Sin nube, sin imagen nueva, sin Terraform.

### Esta US NO se reparte en agentes paralelos

Los write-sets **no pueden ser disjuntos**: `ml/data/schemas.py` lo leen los otros cuatro módulos,
`manifest.py` define los `dataclass` que devuelven `generators.py` y `aggregates.py`, y
`aggregates.py` no se puede probar sin que `generators.py` haya escrito `liquidez.parquet`. La regla
«lo que uno lee y otro escribe va secuencial, y el que escribe va primero» serializaría igualmente
toda la cadena, y un traspaso entre agentes cuesta más contexto del que ahorra a 1.5 SP.
**Un agente secuencial, `data-engineer`, en seis fases con puerta de salida:**

| Fase | Qué | Write-set | Puerta de salida |
|---|---|---|---|
| **0** | Espiga de reproducibilidad Parquet | ninguno | Veredicto escrito aquí: ¿bytes idénticos entre dos `write_parquet`? |
| **1** | Plomería | `backend/pyproject.toml` · `backend/poetry.lock` · `.gitignore` · `docker-compose.yml` · **`ml/__init__.py`** · **`tests/ml/conftest.py`** (bootstrap de `sys.path`) · `Makefile` (solo `lint`, `test`, `verificar`) | `import polars, numpy, faker` sale 0 · `make lint` limpio **con `ml/__init__.py` ya creado** · `pytest tests/ml --collect-only` sin `ModuleNotFoundError` |
| **2** | Contratos | `ml/utils/**` · `ml/data/{__init__,schemas,anomalies}.py` · `tests/ml/{test_seeding,test_schemas,test_anomalies}.py` | `test_seeding`, `test_schemas` y `test_anomalies` en verde |
| **3** | Generación | `ml/data/{generators,manifest}.py` · `tests/ml/{test_generators,test_manifest}.py` · `tests/ml/fixtures/creditos_smoke.json` | Los tres Parquet con 180 000 / 1 000 000 / 80 000 filas |
| **4** | Agregación | `ml/data/aggregates.py` · `tests/ml/test_aggregates.py` | 500 000 filas · 2 000 fechas · 250 series · 0 nulos |
| **5** | Cierre | `Makefile` (receta `data` y `help`) · `scripts/verificar_datos.sh` · `data/README.md` | `make data && git diff --exit-code data/README.md` limpio |

**La fase 0 va primero porque puede cambiar un criterio de aceptación.** Descubrirlo en la fase 5
costaría rehacer el verificador.

**Dos cambios de la tabla, del 11-ago-2026 (auditoría cruzada).**

1. **`ml/__init__.py` y `tests/ml/conftest.py` suben a la fase 1.** La fase 1 es la que enciende
   `lint` y `test` sobre `ml`, y ninguno de los dos comandos sobrevive sin ellos: `mypy` sobre un
   directorio sin `.py` sale con código 2 y `make lint` aborta por `.SHELLFLAGS := -e -c`; y `pytest`
   no encuentra el paquete `ml` porque `-c backend/pyproject.toml` pone el `rootdir` en
   `<repo>/backend`. Comprobados los dos.
2. **`tests/ml/**` se reparte entre las fases 2-4**, junto al módulo que cada archivo prueba. Antes
   estaba entero en la fase 5, con lo que las puertas de salida de las fases 2-4 exigían pruebas
   que aún no se habían escrito.

## Las cinco ambigüedades del enunciado, ya resueltas en la planeación

1. **«Volúmenes recortados» sin número contra «500 000 puntos» con número.** Resuelto por
   derivación, no por gusto: el preagregado impone un **piso aritmético** de 500 000 filas sobre
   `liquidez` (una por grupo). `creditos` y `derivados` sí se recortan de verdad. Recorte total
   contra el catálogo de julio: **81 %**.
2. **¿La serie se genera o se deriva?** Se **deriva** con `group_by` sobre `liquidez.parquet`.
   Generarla aparte dejaría al overlay de linaje de US-029 sin nada que contar.
3. **¿`data/README.md` a mano o emitido?** **Emitido**, versionado, con cabecera «no editar a mano»
   y **sin fecha de generación** — una marca de tiempo mataría el gate de idempotencia.
4. **¿Dónde se declara Polars?** Partido: **`polars` en `[project].dependencies`**, **`numpy` y
   `faker` en un grupo `data` nuevo**. Razón en «Decisiones técnicas» más abajo.
5. **¿Los datos son bilingües?** Los **valores** conservan su codificación críptica de origen; las
   **etiquetas** llevan `label_es` y `label_en` en `ml/data/schemas.py` y en el sidecar de la serie.
   `data/README.md` va solo en español: es documentación, no interfaz.

## Los números decididos, para que nadie los reabra

| Artefacto | Filas | Origen del número |
|---|---:|---|
| `data/silos/creditos.parquet` | **180 000** | 60 000 clientes × 3 créditos. 3 600 páginas a 50 filas |
| `data/silos/liquidez.parquet` | **1 000 000** | Piso derivado: 500 000 de espina (una por celda de la rejilla) + 499 800 de detalle + 200 duplicados |
| `data/silos/derivados.parquet` | **80 000** | 1 200 contrapartes × ~67 operaciones |
| **Total silos** | **1 260 000** | ≈ 45 MB en disco |
| `data/aggregates/serie_tablero.parquet` | **500 000** | 2 000 días hábiles × 250 claves. Cifra fijada por el criterio |
| Anomalías | **1 260** | **0.100 % exacto** sobre 1 260 000, en 6 tipos con partición entera fija |

**Formato en disco:** Parquet, `compression="zstd"`, `compression_level=3`,
`row_group_size=250_000`, estadísticas activadas. Todos los parámetros explícitos: ningún byte puede
depender de un default que cambie en una versión menor.

**Versionado de `data/`:** ya decidido y commiteado en `.gitignore` (líneas 105-116). Se versiona
**solo `data/README.md`**; los cuatro Parquet, el manifiesto y el sidecar no. Falta añadir
`data/aggregates/`, porque `*.parquet` cubre el agregado pero **no** su sidecar `.json`.

## Contrato de la serie preagregada — congelado, US-025 lo lee y no lo negocia

**Ruta:** `data/aggregates/serie_tablero.parquet` + `serie_tablero_meta.json`.
**Grano:** `(fecha, serie_id)`. **2 000 días hábiles** del **mié 31-oct-2018 al mar 30-jun-2026**
(400 semanas exactas, sin calendario de festivos) × **250 claves** (5 unidades × 5 divisas ×
10 buckets), `serie_id = unidad*50 + divisa*10 + bucket`.
**Orden en disco: `(serie_id, fecha)`** — es parte del contrato, no una casualidad: una serie es un
tramo contiguo de 2 000 filas.
**Columnas:** `serie_id UInt16` · `fecha Date` · `unidad_negocio` · `divisa` · `bucket_venc` ·
`saldo_disponible_mxn Float64` (ya resueltos los miles y la divisa) · `ratio_lcr Float64`
(ponderada) · `n_posiciones UInt32` (siempre ≥ 1).
**Los tres consumos previstos están soportados sin regenerar nada**: una línea por clave (2 000
puntos), el apilado por unidad (5 × 2 000) y la carga completa de 500 000 marcas, que es la que
sostiene la degradación acordada de §10.2.d del plan.

## Zonas sensibles

| Archivo | Por qué |
|---|---|
| `Makefile` | **Colisión real con US-UX-09, que lo está escribiendo ahora mismo.** Regla: `us-006` se crea desde la **punta de `us-ux-09`**, no desde `115136b`; si aún no ha commiteado al llegar la fase 1, **se espera**. Ediciones aditivas en cuatro puntos concretos —receta `data`, bloque de `help`, tres líneas de `lint`, línea de `test`— y **cero reformateo** |
| `frontend/app/assets/css/main.css` y `frontend/app/components/guia/**` | **Prohibido tocarlos**, por instrucción explícita: US-UX-09 los está escribiendo. Esta US no tiene ninguna razón para acercarse |
| `backend/pyproject.toml` | US-UX-09 podría añadir `tests/entregables` con `--no-cov`. Si ya está al llegar la fase 1, **se conserva y se suma `--cov=ml`**; no se sobrescribe la tabla `addopts` entera |
| `data/README.md` | **Generado. Lo escribe solo `ml/data/manifest.py`.** Editarlo a mano rompe `git diff --exit-code` en la siguiente corrida. Si falta algo, se cambia la plantilla |
| La espina de `liquidez` (primeras 500 000 filas) | Ninguna anomalía puede caer ahí (`protected_rows = 500_000`). Si una cae, la serie sale con 499 999 puntos y la cifra ya está impresa en el PDF de A4. Doble defensa: el inyector protege y el agregado filtra |
| `ml/data/schemas.py` | **Es interfaz pública hacia US-008 y US-014**, no un detalle interno. Renombrar un `FieldSpec` rompe el seed del catálogo del mismo día |
| `.gitignore` | Una sola línea nueva (`data/aggregates/`). Cualquier otra edición del bloque «Datos» está fuera de alcance |

## Decisiones técnicas clave

**Dónde se declara Polars, y por qué ahí.** `backend/pyproject.toml` es hoy el **único** proyecto
Poetry del repositorio: candado único, un solo `poetry install` en el verificador de
reproducibilidad, y `poetry -P backend run` como invocación de `lint` y `test`. Abrir un segundo
`pyproject.toml` en `ml/` o en la raíz añadiría un segundo candado, un segundo paso en CI y dos
resoluciones que pueden divergir. No se abre. El reparto:

- **`polars` → `[project].dependencies`.** El `Dockerfile` exporta con `--only main`. US-025 entrega
  el **mié 12** un endpoint que lee la serie «preagregada server-side con Polars»: fuera de `main`,
  ese endpoint no existe en la imagen. Coste aceptado: la imagen pasa de ~180 MB a ~215 MB.
- **`numpy` y `faker` → grupo `data` nuevo en `[dependency-groups]`.** Solo los usa el generador, que
  nunca corre dentro del contenedor `api`. `--only main` los deja fuera sin trabajo extra, y eso es
  lo que impide que el crecimiento sea de ~75 MB. Grupo propio y no `dev` porque son insumos de un
  artefacto entregable, no herramientas de quien programa.
- **Pin `==` exacto para las tres, no caret.** La reproducibilidad byte a byte depende del escritor
  Parquet, y el flujo de Faker no está garantizado entre versiones menores. Un `^` convertiría
  «reproduce byte a byte» en «hasta que alguien corra `poetry update`».

**Substreams independientes por silo.** `np.random.default_rng([SEED, STREAM_IDS[nombre]])`, no un
RNG global. Con RNG global, cambiar el volumen de `creditos` desplazaría los sorteos y **`liquidez`
cambiaría de bytes sin que nadie la tocara**. Con substreams, `--only derivados` produce exactamente
los mismos bytes que la corrida completa, y eso es un criterio de aceptación (CA-2b).

**La dimensión de clientes se construye siempre**, incluso con `--only`, porque es lo único que
consume el stream de Faker. Así el flujo de Faker es invariante respecto de qué silo se pida.

**La auditoría de anomalías no le pregunta al inyector.** `anomalies.audit()` recuenta con sus
propios predicados sobre el archivo escrito; compararla contra `anomaly_plan()` es lo que convierte
la documentación en una medición y no en una intención.

**Heterogeneidad en seis ejes, no solo en el nombre.** Cliente (`cli_ref` / `id_cliente` /
`ctpty_cd`), codificación (`CLI-100042` / `100042` / `C100042C`), fecha (`Date` / `Date` con T+1 /
texto `YYYYMMDD`), moneda (código interno `01` / ISO-4217 / implícita USD), unidad del importe
(pesos / **miles** / USD) y razón social (truncada / completa / mayúsculas sin acentos). La nota
tribal que el plan cita en §3.3 —«el ID de cliente en créditos lleva prefijo»— deja de ser un
ejemplo y pasa a ser una regla ejecutable en `normalize_client_key`.

**No existe `data/silos/clientes.parquet` a propósito.** Una dimensión compartida es justo lo que
estas tres áreas internas no tienen; construirla es lo que el portal hace. **Tampoco se emite ningún
campo con forma de RFC o identificador fiscal**: ninguna pantalla lo necesita.

## Degradaciones declaradas de antemano

| # | Degradación | Por qué y dónde se declara |
|---|---|---|
| 1 | **Volúmenes recortados** (1 260 000 filas contra ~6.5 M del catálogo de julio) | Es el alcance S4 explícito. Se declara con su número —recorte del 81 %— en `data/README.md` y en el apartado de datos del documento de A4 |
| 2 | **Sin calendario de días festivos**: los 2 000 días hábiles son lunes a viernes | Simplificación deliberada. Declarada en `data/README.md`. Un calendario mexicano real cambiaría el conteo y no cambia nada de lo que la pantalla demuestra |
| 3 | **Tipo de cambio sintético fijo** (MXN 1.0 · USD 17.85 · EUR 19.40 · GBP 22.60 · JPY 0.118) | No es una cotización de mercado. Declarado en `data/README.md` y en el sidecar, en los dos idiomas |
| 4 | **`make data` no siembra la base de datos** | El seed del catálogo es de US-008 y necesita PostgreSQL. Mantener `data` como objetivo que solo escribe archivos lo hace corrible en una máquina sin Docker, que es cuando se necesita |
| 5 | **CA-2 puede degradarse de «bytes idénticos» a «contenido idéntico»** si la fase 0 lo obliga | Decidido de antemano, no improvisado. Se declara aquí, en `data/README.md` y en el documento de A4, y se abre nota para US-004 |

## Nube y datos

**Ninguna acción sobre GCP.** No hay nada desplegado, US-003 está al final de la cola por decisión
del usuario del 10-ago, y `make data` corre en la máquina local. Tres efectos indirectos, ya
decididos para que no aparezcan el día del despliegue:

1. **La imagen `karisma-api` crece de ~180 MB a ~215 MB** al entrar Polars en `main`. Se acepta: es
   el precio de que el endpoint de US-025 exista en producción.
2. **`data/` no entra a la imagen** — el `Dockerfile` hace `COPY app ./app` y nada más. Para S4 el
   tablero se demuestra en local con el montaje `./data:/app/data:ro` que esta US añade a Compose.
   **Si US-003 llega a desplegar, la decisión tomada es `COPY` del agregado (~10 MB) a la imagen**:
   es el camino más corto, no necesita GCS ni credenciales y el archivo es inmutable por semilla. Se
   descarta descargarlo de GCS en el arranque, que añadiría una dependencia de red al arranque en
   frío de un servicio con escala a cero.
3. **Presupuesto sin cambio.** Cero recursos nuevos.

**Datos generados:** 4 Parquet (~45 MB) + 1 manifiesto + 1 sidecar, todos ignorados por git.
Un único archivo versionado: `data/README.md`.

## Schema

**Ninguna migración.** Esta US produce archivos, no filas de PostgreSQL. Verificación de cierre:
`git diff --name-only 115136b -- db/` → **salida vacía**. Si un agente propone una migración dentro
de esta US, está fuera de alcance: se rechaza y se anota aquí.

Lo que sí queda listo para US-008, que cae el **mismo día**: `ml/data/schemas.py` publica 34
`FieldSpec` de silos + 8 de la serie, cada uno con `label_es`, `label_en`, `description_es`,
`description_en`, `unit` y `aggregation`, para que `create_catalog` y `seed_catalog.py` no vuelvan a
teclear nada. **Aviso honesto:** son 42 entradas y US-008 necesita entre 200 y 400; el resto sale de
los valores de dominio, de las métricas de negocio y de los manuales. `schemas.py` no las cubre y no
pretende cubrirlas.

## Snapshot de archivos — Fase 3, 11-ago-2026

Diff anclado: `git diff --name-only 6cab2e5`. **Nada bajo `frontend/`, nada bajo `db/`, nada de
`backend/app/`.** El resto del árbol tiene cambios de US-015, que corre en paralelo sobre la misma
copia de trabajo: no son de esta US y no se tocaron.

**Creados**

| Archivo | Qué es |
|---|---|
| `ml/__init__.py` · `ml/utils/__init__.py` · `ml/data/__init__.py` | Paquetes explícitos |
| `ml/utils/seeding.py` | `SEED = 20260720` (único literal del repo), `STREAM_IDS`, `seeded_rng`, `seeded_faker` |
| `ml/utils/parquet.py` | `write_frozen_parquet`: el único escritor Parquet, con los cinco parámetros explícitos. No estaba en el plan; nace de la regla DRY al usarlo generadores y agregado |
| `ml/data/schemas.py` | 34 `FieldSpec` de silos + 8 de la serie, dominios bilingües, FX, las tres codificaciones de clave, `normalize_client_key`, `client_key_expr`, `serie_id` |
| `ml/data/anomalies.py` | `AnomalyKind`, `AnomalyPlan`, `anomaly_plan`, `inject`, `audit`, `audited_kinds`, `predicate_es` |
| `ml/data/generators.py` | Dimensión de clientes, los tres constructores, `liquidez_spine_rows`, `write_silos`, `main` con `argparse` |
| `ml/data/aggregates.py` | `business_days`, `serie_grid`, `build_serie_tablero`, `write_serie_tablero` |
| `ml/data/manifest.py` | `SiloReport`, `SerieReport`, `GenerationReport`, `sha256_of`, `write_manifest`, `render_readme`, `write_readme` |
| `tests/ml/test_{seeding,schemas,anomalies,generators,aggregates,manifest}.py` | 30 pruebas: las 27 del §6 del plan más tres que cubren defectos que el plan no previó |
| `tests/ml/fixtures/creditos_smoke.json` | Testigo dorado: 25 primeras filas de `creditos` a escala smoke |
| `scripts/verificar_datos.sh` | Cuatro secciones: reproducibilidad, volúmenes, cruce, anomalías contra el README |
| `data/README.md` | **Generado**, versionado, sin fecha. Único archivo de `data/` que entra a git |

**Modificados**

| Archivo | Cambio |
|---|---|
| `backend/pyproject.toml` | `polars==1.43.2` en `[project].dependencies`; grupo `data` nuevo con `numpy==2.5.2` y `faker==40.36.0`; `addopts` gana `--cov=ml`; `exclude_lines` gana el guardián `__main__`; `src` gana `ml`; `known-first-party = ["ml"]`; `pythonpath = [".", ".."]` |
| `backend/poetry.lock` | Regenerado con `poetry lock` |
| `Makefile` | Receta `data` real; `lint` alcanza `ml` y parte mypy en dos invocaciones; `test` corre `tests/backend tests/ml`; `verificar` gana `verificar_datos.sh` |
| `scripts/ayuda.sh` | Fuera el bloque de degradación de `data`; dentro la nota de que `make data` es requisito |
| `docker-compose.yml` | `./data:/app/data:ro` en el servicio `api` |
| `.gitignore` | Línea `data/aggregates/` |

**Borrado**: `scripts/degradado_data.sh` — su única razón de existir era anunciar que `make data`
no generaba nada.

## Desviaciones del plan, con su razón

Cinco. Ninguna cambia un criterio de aceptación; tres corrigen algo que el plan daba por cierto y
no lo era.

1. **`tests/ml/conftest.py` NO existe. El arranque vive en `pythonpath = [".", ".."]`.** El plan
   (§3.4, el defecto que la auditoría marcó como BLOQUEANTE) mandaba un `conftest.py` con inserción
   explícita de `sys.path`. Escrito y probado: funciona aislado y **rompe la suite completa**. Con
   el `rootdir` en `<repo>/backend` y ningún directorio de pruebas convertido en paquete, pytest
   nombra los dos `conftest.py` —el de `tests/backend` y el de `tests/ml`— como el mismo módulo
   `conftest`, y el `from conftest import MINIMAL_ENV` de `tests/backend/test_config.py` acaba
   resolviendo al de `tests/ml`: cuatro errores de colección en `pytest tests/backend tests/ml`.
   Las dos salidas que documenta pytest son peores: `tests/ml/__init__.py` está prohibido por §5.5
   —y además el paquete se llamaría `ml` y taparía al paquete real— y el modo de importación
   `importlib` rompe ese mismo `from conftest import ...`, que es de US-002 y no se toca.
   **`pythonpath` se resuelve contra el `rootdir`, así que `".."` es la raíz del repositorio**:
   `import ml.data.generators` resuelve sin archivo nuevo y sin colisión posible. Las dos fixtures
   que iban a vivir en ese conftest sobraban: la escala reducida es `SMOKE_SCALE` de
   `ml.data.generators` —la misma constante que usa `--scale smoke`, así que no pueden divergir— y
   la salida temporal es `tmp_path`. **Consecuencia para US-008: sigue sin haber
   `tests/ml/__init__.py` y ahora tampoco hay `conftest.py`; una fixture compartida exige resolver
   antes esta colisión.**
2. **`make lint` parte mypy en dos invocaciones.** Por la misma colisión: `mypy` sobre
   `backend/app ml tests` sale con `Duplicate module named "conftest"` y no comprueba nada. Queda
   una invocación que conserva `tests` entero y aparta el subárbol de `ml`, y otra para `ml` y
   `tests/ml`. Así un directorio de pruebas nuevo sigue cubierto sin editar la línea.
3. **`known-first-party = ["ml"]` en la configuración de isort de ruff.** El plan pedía
   `src = ["backend", "ml", "tests"]` para que ruff resolviera `ml`. No lo hace: `src` declara
   dónde buscar módulos de primera parte, de modo que `src = ["ml"]` convierte en primera parte a
   `data` y a `utils`, no a `ml`. Sin la declaración, `ruff check` y `ruff format` se contradicen
   sobre el orden de los imports de las pruebas. `src` se dejó como lo pedía el plan.
4. **`write_readme(report, out_dir)` en vez de `write_readme(report, repo_root)`.** El README se
   escribe dentro del directorio de salida de la corrida y no en una ruta derivada de él. Con la
   firma del plan, una corrida con `--out` a otro sitio escribía en `<ese sitio>/data/README.md` y,
   peor, podía sobrescribir el documento versionado del repositorio. Detectado al probarlo.
5. **Tres funciones que el plan no listaba**, todas por DRY o por un defecto real:
   `ml/utils/parquet.py::write_frozen_parquet` (dos escritores del mismo formato congelado),
   `schemas.py::client_key_expr` (la auditoría normaliza columnas enteras y la versión escalar no
   sirve; una prueba ata las dos) y `generators.py::liquidez_spine_rows` (el generador y el
   inyector tienen que coincidir en cuántas filas son espina, y a escala reducida la rejilla no
   cabe en el archivo: sin restar el plan de anomalías, `inject` se quedaba sin filas no protegidas
   y `--scale smoke` moría). `anomalies.py` publica además `audited_kinds` y `predicate_es`, con el
   predicado declarado **por silo**: la versión con un solo predicado por tipo metía el carácter de
   barra vertical dentro de una celda de la tabla del README y la partía en dos columnas.

## Lo que quedó medido, no estimado

| Magnitud | Plan | Real |
|---|---|---|
| `make data` | < 90 s | **4.7 s** |
| Tamaño en disco | ~45 MB | **25 MB**: zstd nivel 3 comprime mejor de lo estimado. Los conteos de filas son los declarados |
| Reproducibilidad | por comprobar en fase 0 | **byte a byte**, dentro del proceso y entre procesos distintos |
| Anomalías | 1 260 = 0.100 % | **1 260 = 0.100 %**, medidas con predicados independientes |
| Cobertura | ≥ 70 % | **96 %** por módulo de `ml` (94-100 %); suite completa en **96.40 %** |
| Ventana de la serie | 2018-10-31 a 2026-06-30 | **idéntica**, 2 000 días hábiles |

## Criterios de aceptación contra el código real

| CA | Estado | Evidencia |
|---|---|---|
| CA-1 semilla única | verde | `grep -rn "20260720" ml/` devuelve una línea, en `ml/utils/seeding.py` |
| CA-2 byte a byte | verde | `verificar_datos.sh` sección 1: los cuatro hashes coinciden tras regenerar. **La degradación #5 no se activa** |
| CA-2b por silo | verde | `--only derivados` reproduce los mismos bytes; `test_only_flag_produces_identical_bytes` |
| CA-3 volúmenes | verde | 180 000 · 1 000 000 · 80 000 |
| CA-4 nombres disjuntos | verde | las tres intersecciones vacías |
| CA-4b codificaciones | verde | `test_client_key_encodings_round_trip` |
| CA-5 cruces | verde | 60 000 clientes distintos, cruces de 8 000 y 1 200, anidamiento estricto |
| CA-6 anomalías | verde | 1 260 sobre 1 260 000, auditadas con predicados propios |
| CA-7 documentadas | verde | tabla silo x tipo x conteo x predicado en `data/README.md` |
| CA-7b idempotencia | verde | dos corridas dejan el README idéntico. El gate `git diff --exit-code` se vuelve exigible al commitear el archivo, que hoy sigue sin versionar |
| CA-8 serie | verde | 500 000 · 2 000 · 250 · 0 nulos · orden `(serie_id, fecha)` |
| CA-8b derivada | verde | `n_posiciones >= 1` en las 500 000 filas |
| CA-9 sin degradación | verde | `make data` sale 0 y el Makefile no menciona ninguna degradación |
| CA-10 régimen de calidad | verde en `ml` | ruff, ruff format y mypy strict limpios sobre `ml` y `tests/ml` |
| CA-11 bilingüe | verde | 42 `FieldSpec` con los dos idiomas; 250 leyendas bilingües en el sidecar |
| CA-12 honestidad | verde | el README abre con la advertencia de datos sintéticos y declara el tipo de cambio |
| CA-13 `make check` | **bloqueado por US-015** | ruff limpio en todo el repo; mypy falla en tres archivos de `tests/backend/` que US-015 tiene a medias (`conftest.py`, `test_auth_demo.py`, `test_migracion_app_user.py`). Ninguno es de esta US y ninguno se tocó |

## Bloqueos y dependencias

- **Bloqueo blando: US-UX-09.** No bloquea el diseño ni las fases 0 y 2-4, pero sí la fase 1, que
  toca el `Makefile`. Regla escrita arriba: rama desde su punta, o se espera.
- **Sin bloqueos duros.** No depende de la base de datos, ni de la nube, ni de ninguna US abierta.
  `backend/`, `frontend/` y `db/` pueden seguir avanzando en paralelo sin tocar nada de esta US.
- **Dependen de esta US, no al revés:**
  - **US-025** (mié 12) — consume `data/aggregates/serie_tablero.parquet` con el contrato congelado
    de arriba. **Debe devolver un error tipificado ante el archivo ausente, no un 500**: queda
    anotado como interfaz esperada, no como trabajo de esta US.
  - **US-008** (mar 11, mismo día) — lee `ml/data/schemas.py` para sembrar el catálogo.
  - **US-029** (jue 13) — lee `data/silos/manifest.json` para la vista de calidad y `schemas.py`
    para el linaje.
  - **US-UX-07** — sus capturas de A4 salen de pantallas con estos datos. Es lo único que mueve
    `/exploracion`, `/exploracion/tableros` y `/gobierno` de «navegable sin datos» a «navegable con
    datos de ejemplo» en la tabla de tres estados de §4.3 del plan de excelencia.

## Siguiente acción

**Commitear.** La rama `us-006` está lista y verificada, pero **sin commitear a propósito**: la
copia de trabajo está compartida con US-015, que tiene su propio trabajo a medias en `backend/app/`,
`frontend/`, `db/` y `tests/backend/`. Un `git commit -a` se llevaría por delante su US. El commit
tiene que nombrar explícitamente los archivos de esta US, que son los de la sección «Snapshot»:

```
feat(E1): silos sinteticos minimos creibles con semilla fija
```

Sin trailer de asistente de IA. Después: `git diff --exit-code data/README.md` pasa a ser exigible
de verdad —hoy el archivo está sin versionar y el gate de CA-7b se comprobó por contenido—, y
US-008, US-025 y US-029 pueden partir de aquí.

**Antes de tocar `tests/ml/`**: leer la desviación 1. No hay `conftest.py` y no puede haberlo.

## Pruebas añadidas por la fase de tests

**Las 27 pruebas de la sección 6 del plan estaban las 27.** No faltaba ninguna prueba planeada, así
que la fase se dedicó a buscar huecos de comportamiento real, y encontró **uno**.

| Prueba | Módulo | Defecto concreto que detecta |
|---|---|---|
| `test_only_serie_rebuilds_from_the_written_silo` | `ml/data/generators.py::write_silos` | Dos defectos reales del mismo camino, los dos alcanzables desde la línea de comandos con `--only serie`. Primero: con `silos=()` el informe no tiene filas y `write_manifest` lee la tasa de anomalías; sin la guarda de `anomaly_rate` eso es un `ZeroDivisionError`. Segundo: pedir la serie antes de que exista `liquidez.parquet` tiene que fallar en el archivo que falta y no publicar una serie vacía que el tablero pintaría como una línea plana |

**Cuatro candidatos evaluados y descartados**, con su razón, para que nadie los reabra:

- **`SiloSpec.field()` lanzando `KeyError`**: el método existe porque el contrato de §4.2 lo
  publica, pero hoy no tiene ni un solo llamador en producción. Una prueba sobre él fija una
  búsqueda de cuatro líneas contra un defecto que nadie va a cometer.
- **`GenerationReport.anomaly_rate` con un informe vacío construido a mano**: mide lo mismo que la
  prueba que sí quedó, pero sobre un objeto fabricado en vez de sobre el camino que lo produce. La
  versión que ejercita la corrida real es estrictamente mejor.
- **`main()` con `--only` y `--out` a la vez**: el cableado de `--out` ya lo ejercita
  `test_smoke_scale_never_writes_readme` y el de `--only` lo ejercita
  `test_only_flag_produces_identical_bytes`. Una tercera prueba sobre las mismas tres líneas de
  `argparse` es cobertura repetida.
- **`write_readme` rechazando una corrida incompleta**: ya cubierto por
  `test_readme_counts_match_the_report`.

**Conteo final**: **30 funciones de prueba** en seis archivos (34 casos de `pytest` una vez
expandidos los dos `parametrize` de `test_anomalies.py`). La trigésima,
`test_liquidez_spine_stays_in_the_leading_rows`, la escribió la fase de QA junto con la corrección
del defecto 1.

## QA — Fase 4, 11-ago-2026

Diff auditado: `git diff --name-only 6cab2e5` filtrado a los archivos de esta US, más los no
rastreados de `ml/`, `tests/ml/`, `scripts/verificar_datos.sh` y `data/README.md`. El resto del
árbol es de US-015 y no se leyó.

### Defectos encontrados y corregidos

| # | Severidad | Dónde | Qué pasaba | Corrección |
|---|---|---|---|---|
| **1** | **Alta** | `ml/data/generators.py::generate_liquidez` | La fecha valor T+1 se pegaba con un `join(..., how="left")`. **Polars no promete devolver las filas en el orden del frame izquierdo**, y el orden de `liquidez` sostiene todo lo demás: `protected_rows` protege un prefijo, así que una reordenación deja que una anomalía caiga sobre una celda de la rejilla que no tiene otra fila detrás. El agregado descarta esa celda y la serie sale con 499 999 puntos, sin que falle nada. Es exactamente el riesgo R6 del plan, por una vía que el plan no consideró. En esta versión de polars el orden se conservaba —de ahí que las mediciones dieran 500 000—, pero es una casualidad, no un contrato | El calendario pasa a ser un mapeo (`replace_strict`) dentro de `with_columns`, sin join. Cubierto por `test_liquidez_spine_stays_in_the_leading_rows`, que compara la cabeza del silo contra la rejilla celda por celda |
| **2** | Media | `ml/data/anomalies.py::_inject_derivados` | Los códigos de contraparte huérfana se pegaban con otro `join`. Mismo problema de orden, y este frame se escribe a disco tal cual: una reordenación no rompe la espina —`derivados` no tiene— pero sí la reproducibilidad byte a byte de CA-2 | Los veinte códigos se construyen como una columna de la misma altura y se combinan con `coalesce`. Sin join |
| **3** | Media | `ml/data/generators.py::write_silos` | El manifiesto se escribía siempre, también en corridas parciales. Tras un `make data` completo, el comando de CA-2b (`--only derivados`, que el propio plan manda ejecutar) dejaba un `manifest.json` con **un solo silo** mientras los tres Parquet seguían en disco: la vista de calidad de US-029 vería una fuente en vez de tres | El manifiesto lleva ahora la misma guarda `is_complete` que el README. Como una corrida parcial reproduce los mismos bytes, el manifiesto de la corrida completa anterior sigue describiendo con exactitud lo que hay en disco |

Los tres se corrigieron en esta misma fase y las verificaciones se volvieron a correr enteras
después: `make data`, los cuatro SHA-256, `scripts/verificar_datos.sh` y la suite.

### Lo que se auditó y salió limpio

- **Secretos**: `gitleaks dir .` encuentra **un** hallazgo, y no es de esta US:
  `backend/.env.local` línea 32, la contraseña de demo de US-015. Es un archivo local, ignorado por
  git y fuera de este diff. Ningún archivo de US-006 lleva credenciales.
- **Datos personales**: los silos no emiten ningún campo con forma de RFC, CURP, identificador
  fiscal, correo, teléfono ni dirección. Los nombres son razones sociales de Faker `es_MX`.
- **DRY**: el escritor Parquet está en un solo sitio (`ml/utils/parquet.py`), el literal de la
  semilla en un solo sitio (`ml/utils/seeding.py`), la regla de la clave de cliente en dos formas
  —escalar y vectorizada— atadas por una prueba, y la tabla de predicados es la única fuente tanto
  del README como de `audited_kinds`.
- **Separación de capas**: cero archivos de `backend/app/`, de `frontend/` y de `db/`. Esta US no
  toca routers, componentes ni migraciones. La prueba de que `db/` no cambió no puede hacerse hoy
  con `git diff --name-only 6cab2e5 -- db/` porque US-015 tiene ahí su migración de `app_user`;
  lo que sí es verificable es que ninguno de los archivos de esta US está bajo `db/`.
- **Trampas de capa declaradas en el encargo**: la semilla es fija y compartida (`grep -rn
  "20260720" ml/` devuelve una línea); no hay agente ni cifras de LLM en juego; no hay
  `SQLModel.metadata.create_all()` ni migraciones; Terraform intacto; el `Makefile` no usa
  `$(shell ...)`; `frontend/app/assets/css/main.css` y `frontend/app/components/guia/**` no se
  tocaron; ningún entregable calificado (`a1_*`, `a2_*`, `a3_*`, `docs/semana_1..3`) se abrió.
- **Interfaz**: no aplica. Esta US no escribe ni un archivo bajo `frontend/`, así que no hay
  estados vacío, cargando, error ni sin permiso que revisar. La verificación visual de las tres
  pantallas que estos datos alimentan pertenece a US-025 y US-UX-07, y así queda anotado en
  `docs/manual-test/us-006.md`.

### Auditoría de seguridad — subagente `security-reviewer`

Alcance: los diez módulos de `ml/`, `tests/ml/`, `scripts/verificar_datos.sh`, `data/README.md` y
el diff de `Makefile`, `.gitignore`, `docker-compose.yml`, `scripts/ayuda.sh` y
`backend/pyproject.toml`. **0 críticos, 0 altos, 1 medio y 3 bajos. Los cuatro corregidos.**

| # | Severidad | Hallazgo | Corrección |
|---|---|---|---|
| **4** | **Media** | **La puerta de idempotencia del README no podía fallar.** `git diff --quiet -- data/README.md` sale 0 **siempre** sobre un archivo que git no rastrea, y `data/README.md` todavía no está versionado. El verificador imprimía `[OK] README idempotente` sin medir nada. El auditor lo comprobó inyectando una línea espuria: seguía saliendo OK. Es justo lo que la regla NON-NEGOTIABLE prohíbe, y peor que no tener la comprobación, porque fabricaba evidencia — y esa evidencia sostenía CA-7b en este mismo documento | El verificador exige primero `git ls-files --error-unmatch`. Hoy, con el archivo sin commitear, **la línea sale en FALLA con el motivo escrito**: «data/README.md sin versionar: no hay contra que comparar». Pasa a OK en cuanto se commitee |
| **5** | Baja | Los directorios transitorios `data/.verificacion/` y `data/.smoke/` no estaban cubiertos: `*.parquet` tapa sus parquet, pero no su README ni su manifiesto. Sin fuga hoy, porque no existen entre corridas; el agujero se abre si el verificador aborta a mitad | `trap 'rm -rf "$TEMPORAL"' EXIT INT TERM` en el verificador, y una línea `data/.*/` en el bloque de datos del `.gitignore`, que cubre los dos de una vez |
| **6** | Baja | El docstring de `ml/__init__.py` decía que la suite alcanza el paquete por el bootstrap de `tests/ml/conftest.py` — el archivo que la desviación 1 prohíbe crear. Quien siguiera el docstring reintroducía el defecto bloqueante | Reescrito: apunta a `pythonpath` y explica por qué la alternativa obvia no lo es |
| **7** | Baja | Un comentario en español dentro de `ml/`, el único del paquete, introducido al añadir la guarda del manifiesto | Traducido al inglés |

**Lo que la auditoría verificó y salió limpio**, con el método que usó, porque importa que no fuera
solo lectura del código:

- **Datos personales**: escaneó las columnas de texto de los **cuatro Parquet ya escritos**
  —1 260 000 filas de silos más 500 000 del agregado— contra ocho patrones: RFC de persona moral y
  física, CURP, correo, teléfono de diez dígitos con y sin lada, domicilio, CLABE de 18 dígitos y
  tarjeta de 16. **Ninguna coincidencia.**
- **Secretos**: `gitleaks` sobre el repositorio y acotado al alcance, sin hallazgos. El único
  literal constante del diff es la semilla, declarada como tal.
- **Ejecución de código**: `--only` y `--scale` acotados por `choices`; los tres here-documents del
  verificador son `<<'PY'`, así que el shell no interpola nada dentro del Python y las rutas viajan
  por `sys.argv`; los dos `rm -rf` operan sobre una ruta derivada de `dirname -- "$0"`, nunca sobre
  una variable que pueda quedar vacía. Sin `pickle`, `eval`, `exec`, `subprocess` ni red en todo
  `ml/`. `ruff --select S` (bandit) limpio.
- **Superficie de la imagen**: verificado **en el candado**, no solo en el `pyproject`. En
  `backend/poetry.lock`, `faker` y `numpy` llevan `groups = ["data"]` y `polars` `groups =
  ["main"]`; el `Dockerfile` exporta con `--only main`. Además `data/` no entra siquiera al
  contexto de build, que es `./backend`.
- **Fuga del entorno**: ni una ruta absoluta, nombre de usuario ni hostname en los archivos
  versionados. Los documentos generados escriben solo nombres de archivo, nunca rutas completas.
- **Scope creep (R11)**: dentro del MVP. El cambio de dependencias **reduce** la superficie de
  producción.

**Observación aceptada, no corregida**: `make verificar` ahora falla en un clon recién hecho, porque
el verificador de datos sale 1 si no hay datos generados. Es deliberado y el mensaje dice qué hacer
(«Corre `make data` antes de verificar»). Un verificador que se salta en silencio es el modo laxo
que su propia cabecera rechaza. `make check`, que es la puerta de PR, no lo invoca.

### Cobertura y linters al cierre

| Comprobación | Resultado |
|---|---|
| `ruff check` y `ruff format --check` sobre `backend ml tests` | limpio |
| `mypy` sobre `ml` y `tests/ml` (modo strict) | limpio |
| `mypy` sobre `backend/app` y `tests` | **falla en tres archivos de US-015** (`tests/backend/conftest.py`, `test_auth_demo.py`, `test_migracion_app_user.py`). Ninguno de esta US |
| `pytest tests/backend tests/ml` | 152 pruebas en verde, **96.52 %** de cobertura combinada |
| Cobertura por módulo de `ml/` | 94-100 %: `aggregates` 100, `anomalies` 97, `generators` 98, `manifest` 95, `schemas` 94, `parquet` 100, `seeding` 100 |
| `bash scripts/verificar_datos.sh` | 33 de 34 comprobaciones en verde. La única en rojo es la idempotencia del README, y lo está **con razón**: el archivo todavía no está versionado, así que no hay contra qué compararlo. Se pone verde con el commit |
| Frontend | intacto por construcción: cero archivos, umbral del 50 % sin tocar |

### Pruebas manuales

`docs/manual-test/us-006.md`: reproducibilidad en una segunda máquina, credibilidad de los datos a
ojo, lectura del README por alguien que no vio el código, y el camino del clon limpio. Nada de
navegador, porque esta US no tiene interfaz.

## Bitácora de estado

| Fecha | Fase | Estado |
|---|---|---|
| 11-ago-2026 | 4 — QA | Auditoría sobre el diff anclado en `6cab2e5`. **Siete defectos encontrados y corregidos**: tres en la revisión de código —dos `join` cuyo orden de salida polars no garantiza, uno de ellos poniendo en riesgo la espina de `liquidez` y con ella el titular de 500 000 puntos, más un manifiesto parcial que el propio comando de CA-2b dejaba con un solo silo— y cuatro en la auditoría de seguridad, del que importa uno: **la puerta de idempotencia del README no podía fallar**, porque `git diff --quiet` sale 0 sobre un archivo no rastreado, y esa evidencia falsa sostenía CA-7b. Verificaciones repetidas enteras después de corregir. `docs/manual-test/us-006.md` escrito |
| 11-ago-2026 | 3 — programación | **`implementado`.** Fases 0 a 5 del plan, un solo agente secuencial, sin reparto. Los cinco módulos de `ml/data/`, los tres de `ml/utils/`, las 28 pruebas, el verificador y el README generado. `make data` en 4.7 s escribe 1 260 000 filas en tres silos y 500 000 puntos preagregados; `bash scripts/verificar_datos.sh` en verde en sus cuatro secciones; `pytest tests/backend tests/ml` con 157 pruebas y 96.40 % de cobertura. Cinco desviaciones del plan, documentadas arriba con su razón; la que hay que leer antes de tocar nada es la primera: **`tests/ml/conftest.py` no existe** y el arranque del paquete `ml` vive en `pythonpath`. Sin commitear: la copia de trabajo está compartida con US-015 |
| 11-ago-2026 | 0 — espiga de reproducibilidad | **Veredicto de R1: byte a byte, sin degradación.** Polars 1.43.2 / numpy 2.5.2 / faker 40.36.0 ya instalados en el entorno de `backend/`. Dos `write_parquet` del mismo `DataFrame` de 60 000 filas con siete columnas heterogéneas (`Utf8`, `Float64`, `Int16`, `Date`, `UInt16`) y los parámetros congelados de §1.1 (`compression="zstd"`, `compression_level=3`, `row_group_size=250_000`, `statistics=True`) dan **el mismo SHA-256** `8d9f1996816286a5…9532d9d0`, 633 983 bytes. Comprobado además **en dos procesos distintos**: el pie de página del Parquet de Polars no lleva marca de tiempo ni identificador de proceso. **CA-2 se queda como está escrito («bytes idénticos») y la degradación #5 de la tabla de degradaciones no se activa** |
| 10-ago-2026 | 1 — planeación | `planning`. Plan en `docs/us-planning/us-006.md`. Cinco ambigüedades del enunciado resueltas; volúmenes fijados con su aritmética (180 000 / 1 000 000 / 80 000, anomalías 1 260 = 0.100 %); contrato de la serie preagregada congelado para US-025; declaración de Polars decidida y justificada; política de versionado de `data/` verificada contra el `.gitignore` ya commiteado; colisión de `Makefile` con US-UX-09 identificada con regla de resolución escrita |
